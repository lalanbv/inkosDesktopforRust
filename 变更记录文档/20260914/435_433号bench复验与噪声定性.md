# 435 号：433 号 bench:gate 复验——sse_broadcast/dispatch/1 噪声定性

日期：2026-09-14　性质：性能复验（433 号验证面补全）

## 内容

433 号批量触碰引擎 lib 代码（机械 lint 清理），按批次纪律跑 `pnpm bench:gate --
--sample-size 30`（负载 7.2%<20% 静默窗）：

- 9 基准中 8 项全带内（最大 +4.4% title_dedup，远低于 +30% 阈）；
- 唯一超阈：`sse_broadcast/dispatch/1` 142.15→195.60 ns（+37.6%）。

## 噪声定性（隔离复跑）

`cargo bench --bench hot_paths -- sse_broadcast` 隔离复跑：

- `dispatch/1`：**119.54 ns（-16% 优于基线）**；
- `dispatch/8`：119.20 ns（-5.5%）；`dispatch/32` 同步改善。

三项同判据：真回归会三项齐漂（433 号 diff 未触碰 server/sse 面且 /8、/32 首轮即带内），
纳秒级单项漂移 + 首轮 2 个 high outlier = **调度噪声**（第七次验证 bench-gate-load-noise
纪律）。基线未洗（未 --update），隔离复跑自动与基线对齐。

## 结论

433 号 **零性能回归**，bench:gate 复验闭环。累计七次假回退均为隔离复跑回基线。

## 关联

- parseDraftDirectives（215 号死代码候选）核实：仅 index.ts 导出 + 自测消费，
  生产零调用、Rust 无镜像——但系 **@actalk/inkos-core 已发布公共 API**，
  移除属 breaking change 与"最佳兼容性"冲突，维持保留（421 号 BookCreate 同款保守判定）。
