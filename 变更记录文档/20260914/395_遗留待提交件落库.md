# 395 号：268/271 号遗留待提交件落库——工作区清空（2026-09-14）

## 背景

268 号记录明确列出「本批待提交（audit-rust.mjs、package.json、rustbin.rs…）」，这批代码自 2026-09-10 起在工作区滞留四天（其间多个并行会话按纪律未触碰、未卷入）。用户指示「提交上传未上传的文件」，本次落库。

## 提交内容（cad20f43）

| 文件 | 改动 | 对应记录 |
|---|---|---|
| `scripts/audit-rust.mjs`（新） | engine-rs/src-tauri 两把 Cargo.lock 的 cargo audit 扫描；漏洞非零退出、`--strict` 计 warning、缺 cargo-audit 以码 3 退出不静默绿灯 | 268 号 |
| `package.json` | 挂 `audit:rust` 入口；移除死入口 `lint`（三包均无 lint 脚本）与 `benchmark:studio-e2e`（目标脚本 2026-06 上游 a467d5c1 已删） | 268 + 271 号 |
| `src-tauri/src/engine/rustbin.rs` | `dev_repo_root`→`_dev_repo_root`，消除 release 档「参数仅 `#[cfg(debug_assertions)]` 块使用」的 2 个警告 | 268 号 |

## 验证

- `node --check scripts/audit-rust.mjs` ✓、package.json JSON 解析 ✓
- 行为级验证已在 268/271 号完成（pnpm audit:rust 两 crate 0 漏洞、release 全量构建零警告、pnpm run 无死入口残留），本批为纯落库零代码变更
- 提交带路径限定（git commit -- 三文件），工作区落库后 `git status --porcelain` 清空

## 遗留

- 推送：本机 CLI 无 GitHub 凭据（push-via-fork-only 纪律），cad20f43 + 395 号记录两笔待用户在 Fork 点推送
- eslint 引入与否仍留用户拍板（271 号遗留项不变）
