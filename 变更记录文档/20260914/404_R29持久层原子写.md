# 404 · R29 持久层原子写（v5 五轮规划 P2 末件）

日期：2026-09-14　规划：v5（396 号五轮立项）　思路来源：WNW v6.2.1 `atomic_write_json` 文件锁重试

## 现状盘点（383 号防重复教训）

- Rust 侧已有 `write_file_atomic`（199 号，temp+rename，utils/atomic_file_set.rs），
  主要直写面在 199/221/225 号已收敛——**缺的只是文件锁重试**；
- TS 侧三处各自为政：`logger.ts`（renameSync，日志轮转特例）、
  `state/manager.ts` 与 `studio server.ts`（两份相同的 `writeFileAtomic`
  副本，均无重试）。

## 交付

### A. core 契约层（第 33 守门域）

`utils/atomic-write.ts`：`writeTextAtomic` / `writeJsonAtomic`——

- temp+rename 原子替换（同目录隐藏临时名，含 pid+uuid）；
- **锁重试**（WNW v6.2.1 思路）：rename 遇 `EPERM`/`EBUSY`（Windows 文件锁）
  线性退避重试（`attempt × 50ms`，至多 3 次），放弃时清理临时文件再抛错；
- `atomicWriteRetryDecision` 纯函数（golden 锁）+ options 注入
  （retries/delayMs/sleep/rename）供测试。

Rust `write_file_atomic` 同构加重试：`atomic_write_retry_decision` +
`atomic_write_error_kind`（raw_os_error：EPERM=1/EBUSY=16，
PermissionDenied 兜底映射 EPERM）+ `tokio::time::sleep` 线性退避。
调用方 60+ 处零改动（公开签名不变）。

### B. golden 第 33 守门域

`atomic-write-vectors.json`：contract（maxRetries 3 / delayMs 50 /
retryableCodes [EPERM, EBUSY]）+ retryDecision 11 例（预算边界 0/1/2 重试、
3/5 耗尽、EBUSY、ENOSPC/EACCES/无码不重试、负 attempt 防御）。
TS+Rust 首跑全绿。

### C. TS 收敛（两副本 → 单点）

- `server.ts` `writeFileAtomic` 与 `state/manager.ts` `writeFileAtomic`
  均改为委托 core `writeTextAtomic`（5+2 个调用点行为不变，tmp 名统一
  为点前缀隐藏形态）；
- `logger.ts` 轮转保持 sync 版不动（日志面特例，v5 未列）。

## 测试与门禁

core 245 文件 2111 用例 + tsc 净（golden 2 例 + 注入行为测 2 例：锁重试
线性退避落位 / 不可重试立即抛且 tmp 清理）；studio 96 文件 820 用例 +
tsc 净；INKOS_DUEL=1 **39 运行单元 1786 用例** exit 0（+1 单元）；
bindings 186 不变。

## 教训（第三次）

studio server.test.ts core mock 显式白名单——server.ts 动态 import 的
新导出 `writeTextAtomic` 必须逐个加透传（391/392/404 三次同款）。凡
server 端点/helper 引用 core 新导出，白名单同步是验收前置动作。

## v5 收官

**五轮规划 R25–R29 全部落地**（R25 接管链/R26 遥测/R27 雷达开书/
R28 防复读门/R29 原子写）。四轮+v1–v5 规划无遗留代码项；
剩余=R16 G12 重评与 R19 真实反馈项（待真实长跑数据）+ 待决两项默认值/ja A/B。
