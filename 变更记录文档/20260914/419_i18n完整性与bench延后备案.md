# 419 · i18n key 完整性机械对照 + bench 延后备案

日期：2026-09-14　性质：轻量质量面复核（零负载静态对照）

## i18n 对照结论（零缺陷）

- `use-i18n.ts` 注册 454 keys；前端 `t("…")` 实际使用 390 keys；
- **缺失 = 0**——含 402（radar.createBook/Hint）、412 等近期新增 key 全部
  注册；初稿正则无单词边界产生 31 个误捕（import/toImport/split 等尾部
  `t("…")` 误配），加 `\b` 边界后清零。
- 结论：不存在「界面显示 key 原文」的回归。

## bench:gate 延后备案

距上次基线复验（408 号前）已跨 409–414 六批（write_next 投影重建/
原子写/路由注册/纯复核 docs），按纪律应复验；但机器负载
4.43/18 核 = 24.6%/核 > 20% 门限（PerfPowerServices 93.9% 失控），
按 257 号纪律拒跑不强跑。负载静默窗（或 `-- --sample-size 30` 逃生口）
补验 409–414 批次基线。

零代码变更，纯复核记录。
