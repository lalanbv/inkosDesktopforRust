# 418 · 前端消费 API 面 vs Rust 路由全量对照（轻量纯复核，零新缺陷）

日期：2026-09-14　性质：轻量质量面复核（411/412 模式的消费端系统化）　方法：前端 181 条 API 路径 × Rust 170 条注册路由归一匹配

## 对照结论

前端消费的 API 面（fetchJson/postApi/useApi 全量提取）在 Rust 引擎下
**全部有路由供给**，411/412 修复后无「前端调用、Rust 缺端点」残留。

9 个初筛未匹配项逐一定性：

| 项 | 定性 |
| --- | --- |
| /api/v1、/api/v1/ | 基础 URL 拼接串，非调用 |
| /api/v1/backup/export、/backup/import | 386 号书面备案 TS-only（Rust 不镜像备份面） |
| /api/v1/prompt-packs/../../bad | server.test.ts 路径穿越安全测试（噪声，测试文件未排除） |
| /api/v1/project/files/interactive-films/… | Rust `project/files/*file` 通配路由覆盖 ✓ |
| /api/v1/projects/:id*（story-graph/export 家族） | Rust 全注册 ✓（`/projects/:id` 裸路径仅 TS server 中间件自引用） |

## 与既有复核的关系

至此双端对照覆盖面：memory.db 全表（410）、doctor（415）、SSE 事件面
（416/417）、路由注册对照（413）、高频端点协议体检（411）、前端消费
API 面（本批）——**引擎 HTTP 面双端一致性复核体系闭环**。后续仅随新
端点增量运行 `check-routes.mjs` + 本批对照思路即可。

零代码变更，纯复核记录。
