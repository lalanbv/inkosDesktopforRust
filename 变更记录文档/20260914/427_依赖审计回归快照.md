# 427 · 依赖安全审计回归快照（轻量复核，备案不动）

日期：2026-09-14　性质：轻量质量面复核　对照基线：411 号（23 条）

## 审计快照（官方源）

当前 **31 条**（16 high / 11 moderate / 4 low）vs 411 号基线 23 条——新增
8 条，主源为 **vitest 3.2.7 内嵌 vite 7.3.1** 的 dev/test 链 advisory
（`server.fs.deny` bypass 与任意文件读，>=7.0.0 <=7.3.4 区间，4 high +
2 moderate），路径全部为 `packages__cli>vitest>…`（测试链，非生产运行时：
桌面产物是构建后静态文件，vite dev server 不随桌面分发）。

## 处置决策

- **升级 vitest 链**会牵动全部包的测试基建（vitest 3.x→新版可能连带
  vite 7.3.5+），回归成本超出轻量复核批——**备案不动**，留待依赖健康
  专项批统一处理；
- 生产运行时路径确认无 vite：桌面产物为构建后静态文件 + Rust 引擎直启；
- 期间尝试的 `pnpm -F studio up vite@7` 因 semver 约束未生效，工作区
  已确认干净（package.json 仍 ^6.0.0，无变更）。

## 其余新增 advisory

basic-ftp ×2 / fast-xml-builder / brace-expansion / picomatch 等——均为
ai / gateway 等传递依赖锁定，与 411 基线同源，维持备案（升级 ai 链影响
面大，不在轻量批）。

零代码变更，纯复核快照记录。
