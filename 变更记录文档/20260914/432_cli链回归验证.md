# 432 · cli 链回归验证（429 override 的 EPUB 导出面补验）

日期：2026-09-14　性质：轻量回归验证（429 号验证面补全）

## 内容

429 号的 `brace-expansion@2.0.2 → ^2.1.4` override 落在
`epub-gen-memory > ejs` 链上（EPUB 导出依赖）——该链的运行时归属是
`packages/cli`（@actalk/inkos，Node sidecar 回退端）。本批补验证：

- `pnpm -F @actalk/inkos build`（tsc）：通过；
- `pnpm -F @actalk/inkos test`：**42 文件 232 用例全绿**。

## 结论

brace-expansion / picomatch 升级对 EPUB 导出链零影响——429 号 override
修复的验证面补全（此前覆盖 core / studio，本批补 cli）。

## 推送提醒

origin/develop=e555d6f3；剩余积压（402–432，含 7+ 真实缺陷修复）请继续
在 Fork 图形端推送，核验 origin/develop 追平本地 develop（432 提交）。
