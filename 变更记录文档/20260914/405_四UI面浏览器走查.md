# 405 · 四个新 UI 面真实浏览器走查——含 #/radar 深链真缺陷修复

日期：2026-09-14　走查对象：396/402/403 三批新增 UI 面　环境：生产引擎（debug 二进制）+ walkthrough-mock LLM + studio dist + 真实浏览器

## 走查环境

- `node scripts/walkthrough-mock.mjs 1234`（LLM mock）+
  `INKOS_PROJECT_ROOT=/tmp/inkos-walk404 INKOS_LLM_BASE_URL=… INKOS_STATIC_DIR=…/dist INKOS_PORT=8787 inkos-engine-server`；
- fixture 书「镜花水月」：引擎 API 建书 + 手写真相源（14 列台账带分类、
  3 章摘要、章节文件、genre 资产）+ `POST /resync/1`（settle→validate 链，
  mock 需补 settler/validator 分派）+ `POST /write-next`（全链 → memory.db
  与 run-log 21 条真实记录）；
- `promises` 端点实测：4 条 timeline entries 的 `kind` 全部从台账真相源补齐
  （suspense/emotion/artifact/worldview）——396 号「台账补齐」设计端到端工作。

## 走查结论（4 面通过 3 + 1 面抓到真缺陷并修复）

1. **伏笔池（PendingHooksView）✓ 浏览器实测**：分类筛选 chips 全部出现
   （全部/悬念/物品/情感/世界观——动态收集台账实际分类）；每卡分类徽标
   正确（悬念/情感/物品/世界观）+ 活跃/种子/核心徽标；点击「悬念」→
   chip 置 active、卡片集合收窄为 H01 一条 ✓。
2. **承诺账本（PromiseTimelineCard）✓ 浏览器实测**：分类 chips + kind 徽标；
   排序正确（open H04 置顶 → advancing 按开启章序）；点击「物品」→
   active + 只剩 H03 ✓；开启/推进/期望元数据与后端一致。
3. **运行遥测（RunLogPanel）✓ 浏览器实测**：聚合行「累计 21 · 失败 0 ·
   接管 0」；明细时间/agent（writer/planner/state-validator）/model/耗时
   （0–6ms）/「成功」徽标逐条渲染，与后端 /run-log 快照一致。
4. **雷达→一键开书 ⚠️→✓**：浏览器交互链被 IAB guest 崩溃中断
   （截图管线卡死→tab 快照 len=0，重开 tab 不恢复，浏览器平台问题）；
   但走查过程**抓到真缺陷**——`#/radar` 直达/刷新回落 Dashboard：
   `parseHash` 缺 `radar` 分支（落到 dashboard 兜底）且 `routeToHash`
   缺 case（nav.toRadar 写空 hash，刷新即丢）。已修复 + `radar-route.test.ts`
   3 例锁定（parse/round-trip/未知路径仍回 dashboard）；雷达历史端点协议
   验证通过（fixture scan json 被正确返回）；按钮渲染与预填文本由
   radar-prefill 4 用例 + RadarView 冒烟 2 例锁定。

## 走查基建增强（scripts/walkthrough-mock.mjs）

- 补「状态追踪分析师」（settler）与 "continuity validator"（state-validator）
  两路分派——resync 链此前在 mock 下无法走通；
- SETTLER 输出含完整 HookRecord 形状的 `hookOps.upsert`（**带 kind**）——
  顺带端到端验证了 394 号「settler 产 kind→归一化→落库/落台账」链路
  （hooks.json 实测 kind=suspense/emotion/artifact/worldview）。

## 门禁

studio 97 文件 823 用例全绿（+radar-route 3 例）+ tsc 净。
core/Rust 本批无变更（走查与前端路由修复）。

## 遗留

- 雷达按钮→预填→发送的浏览器端到端交互，待浏览器平台恢复后补一次
  点验（逻辑面已四层锁定：纯函数/组件冒烟/路由/tsc）；
- 走查环境脚手架经验：engine 静态面走查建议把「手写台账 fixture +
  resync + write-next」固化为一键脚本（本批手工执行，未入库）。
