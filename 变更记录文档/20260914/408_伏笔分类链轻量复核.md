# 408 · 伏笔分类链轻量质量面复核——TS 投影同款缺陷修复

日期：2026-09-14　性质：轻量质量面复核（推送前增量）　前置：407 号 Rust 投影修复

## 复核动机

407 号修复了 Rust `render_hooks_projection` 丢分类列。按双端镜像纪律反查
**TS 侧 settle 落盘的同路径**——`state/state-projections.ts` 的
`renderHooksProjection`（`writer.settleChapterState → runtime-state-store
hooksMarkdown` 的产出函数）。

## 发现与修复

**TS 侧存在同款 13 列缺陷**（表头与 cells 均无第 14 列）——即 TS 引擎
（Node 回退端）的 settle 落盘台账同样丢分类。修复：

- 表头补「kind」（en）/「分类」（zh）；
- 行 cells 末尾补 `hook.kind ?? ""`（无 kind 空单元格）；
- 与 Rust `projections.rs render_hooks_projection`（407 号）逐列对齐。

测试：既有英文排序用例期望更新为 14 列；新增「kind 存在 → 第 14 列输出
规范 id（suspense）」用例（含 payoff_timing 由 expectedPayoff 推断的中程
回填行为锁定）。

## 双端渲染面对照矩阵（复核结论）

| 渲染面 | TS | Rust |
| --- | --- | --- |
| settle 落盘投影 | state-projections renderHooksProjection（本次补 14 列） | projections.rs render_hooks_projection（407 补） |
| 晋级/consolidator 落盘 | renderHookSnapshot（394 起 14 列） | 同名函数（14 列） |
| 台账解析（13/14 列兼容+kind 归一） | parsePendingHooksMarkdown | parse_pending_hooks_markdown |
| promises 端点 kind 补齐 | 台账真相源读 | 同（406 号投影重建前置） |

## 门禁

core 245 文件 2112 用例全绿 + tsc 净（+1 用例）。studio/Rust 本批无变更。

## 遗留

无新遗留。407 候选缺陷已双向闭环（Rust 407 / TS 408）。
