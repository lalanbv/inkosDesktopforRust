# 413 · 路由注册机械对照工具入库——412 模式沉淀落地

日期：2026-09-14　性质：轻量质量面复核（纯工具+复核记录）　前置：412 号模式沉淀

## 交付

`scripts/check-routes.mjs`：「路由注册 vs 处理函数」机械对照工具——

- 提取 `server/mod.rs` 注册的 handler（get/post/put/delete/patch ×
  `模块::函数` 路径，185 个）；
- 提取七路路由模块（ops/books/books_state/book_create/write_next/
  service/agent_production）的 handler 形态函数（`pub async fn …(…) ->
  impl IntoResponse`，跨行非贪婪，108 个）；
- 差集报告 + 已核实白名单（4 项）+ 孤儿存在时退出码 1（可作 CI/验收门禁）。

## 对照结论（人工核实）

| 孤儿 | 定性 |
| --- | --- |
| `books_routes::effective_router` | 正则误报（内部缓存辅助函数）→ 白名单 |
| `get_writing_stats` | **清理候选**：tuple 序列化不符前端 ChapterStatRow 期望 + 索引路径可疑；411 的 rows 版为正确链路，保持不挂 |
| `save_radar_scan` | 备用 API（组合 scan 入口包装，前端未调用）→ 白名单 |
| `peek_create_status` | 备用 API（建书状态轮询，前端走 SSE/session）→ 白名单 |

**无用户可见故障残留**（411/412 修复已覆盖前端在调用的面）。

## 门禁

零代码变更（新工具脚本 + 本记录）；duel 最后一次全绿（412 号）仍有效。

## 后续

- 412/413 模式固化：新端点验收 = `node scripts/check-routes.mjs` 退出码 0；
- `get_writing_stats` 清理（删除或对齐重写）留待下批顺手项。
