# 437 号：走查环境一键编排脚本——405 号遗留第 2 项收口

日期：2026-09-14　性质：走查基建（405 号遗留清零）

## 背景

405 号遗留：「engine 静态面走查建议把手写台账 fixture + resync + write-next 固化为一键脚本」。盘点发现数据面已由 405/406 号入库（`scripts/walkthrough-fixture.mjs`：fixture 书 + 14 列台账 + 摘要 + genres + 雷达历史 + resync/write-next + 断言，383 号防重复核查后确认**不重写**），真正缺口是**进程编排**——mock 与引擎仍需人工起两窗、配 env、等就绪。

## 内容

1. **新增 `scripts/walkthrough-env.mjs`（编排器）**：一条命令拉起三件套——
   ①启动 walkthrough-mock；②启动 inkos-engine-server（env 全配齐，含
   `INKOS_LLM_BASE_URL` 带 `/v1`、`INKOS_BUILTIN_GENRES_DIR` 指向项目根
   assets/genres）；③双端就绪探测后调 walkthrough-fixture.mjs 写数据+断言；
   ④打印走查地址；SIGINT/SIGTERM 统一收子进程。前置缺失时给出精确构建命令，
   `--build` 可代跑；`--port/--mock-port/--root` 参数化。**编排器只编排不造数据**，
   数据面单点仍在 fixture 脚本。
2. **修正两份既有脚本头注释的 BASE_URL**（mock/fixture）：补 `/v1`——436 号
   实测模型列表与 chat/completions 都走 mock 的 `/v1` 面，原注释缺 `/v1` 会让
   照抄者模型选择器空转。

## 真机验证

`node scripts/walkthrough-env.mjs` 一键运行：

- mock 就绪 → 引擎就绪 → fixture 断言全过（**promises timeline 4 条、kind=
  worldview/suspense/emotion/artifact 齐全**——407 号回归哨兵在位；run-log
  total=11 kept=11）；
- 浏览器点验编排器所印地址：书详情 `#/book/镜花水月` 数据在位、伏笔池入口正常；
- 停止：SIGTERM 后 8787/1234 端口全释放、mock/引擎进程零残留。

## 效果

两轮走查（405/436）的手工环境步骤（起两进程、配 6 个 env、写四件种子、
建 fixture 书、resync、write-next）收敛为一条命令；后续浏览器走查与
demo 环境搭建零手工前置。

## 关联

- 436 号备案的确认卡「已执行」不区分执行成败 UX 瑕疵：涉前端确认卡状态机
  （restore 派生 `deriveResolvedProposals` 与 live 确认两条路径需一致改动+
  测试），留独立批次，本批不动。
- 推送：433–437 五笔请继续在 Fork 图形端推送核验。
