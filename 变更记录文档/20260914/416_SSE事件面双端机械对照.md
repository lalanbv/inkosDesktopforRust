# 416 · SSE 事件面双端机械对照（观察清单，29 差异待定性）

日期：2026-09-14　性质：轻量质量面复核（222 号 66 事件对照的增量复盘）　方法：全量正则提取 `broadcast("…")` 事件名求差

## 机械对照结果

- TS 侧 69 个事件（server.ts + core 广播点）；
- Rust 侧 41 个事件（engine-rs hub.broadcast）；
- **仅 TS 有 29 个**；仅 Rust 有 1 个（`engine:shutdown`，Rust 独有关机广播，合理）。

## 仅 TS 清单（29）

agent:start/aborted/complete/error、audit:complete/error、chapter:deleted、
consolidate:complete/error、context:compression、daemon:chapter、
draft:complete/error、foundation:error、llm:progress、log、
repair-state:complete/error、revise:start/complete/error、
rewrite:start/complete、session:title、spinoff:start、
thinking:start/delta/end、write:complete

## 定性（初步，未逐事件终裁——417 候选）

- **架构差异候选**：`thinking:start/delta/end`（333 号 Rust 思考流走
  /api/v1/agent 响应面透传而非 SSE）；`write:complete/agent:*` 等
  生产任务事件（Rust 走任务快照 + SSE 阶段广播 + on_log 的 `log` 通道，
  222/238 号备案「Rust 仅快照收批」差异已存在）；`session:title`、
  `daemon:chapter` 等 studio 编排事件；
- **疑似真实缺漏**（需 417 专项逐事件定性）：`context:compression`
  （367 号压缩留痕的实时广播）、`llm:progress`、`revise:*`/`rewrite:*`/
  `repair-state:*`/`consolidate:*` 阶段完成事件（Rust 侧对应能力存在，
  前端若消费这些事件则在 Rust 引擎下无实时反馈）；
- 222 号教训依旧适用：Rust 多行调用会致 grep 漏报，本清单是「正则提取
  口径」下的结果，终裁需逐事件读代码。

## 结论

不武断定缺陷。观察清单入档，417 候选=逐事件定性 + 按消费端（前端实际
on 的 事件名）裁剪——**前端没监听的事件不需要补**，判缺陷以消费端为准。

零代码变更，纯复核观察记录。
