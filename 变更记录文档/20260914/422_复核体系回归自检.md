# 422 · 复核体系回归自检（轻量，零新缺陷）

日期：2026-09-14　性质：413/418 工具与对照的回归确认

## 自检结果

- `check-routes.mjs`：注册 handler 185 × handler 形态函数 **107**（414 删除
  get_writing_stats 后 -1），孤儿 0，exit 0 ✓；
- 前端 API 面对照（418 同口径复跑）：未匹配 9 = 已知豁免 8（backup×2 豁免、
  穿越测试噪声、基础串）+ `/api/v1/projects/:id`（418 已定性——TS server
  自身中间件自引用，非前端运行时调用；影游 story-graph 子路径 Rust 全注册）；
  **新增 0** ✓。

## 结论

413–421 批次期间无新增漏挂端点与消费面缺口，复核体系状态健康。

零代码变更，纯回归自检记录。

## 推送提醒

origin/develop=e555d6f3；剩余积压（402–422，含 7+ 真实缺陷修复）请继续在
Fork 图形端推送，核验 origin/develop 追平本地 develop（422 提交）。
