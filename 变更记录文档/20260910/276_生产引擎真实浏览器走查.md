# 276 号：生产引擎真实浏览器走查——canon 导入全链/聊天回路/服务配置通过（242 号后增量面）

- 日期：2026-09-10
- 分支：develop
- 关联：242 号（上次全页走查——本批覆盖其后新增面）、273 号（canon 两端点首次经真实 UI 验证）、256 号（authoring 聊天面首次浏览器验证）
- 编号衔接：查当日目录最大号 275，顺延 276
- 推送核验：origin/develop = a1b34a33（273 号）；本地领先 274/275/276 待推

## 一、环境

生产形态：engine-rs debug bin 直启（`INKOS_STATIC_DIR` 指 studio dist，dist 现构建）+ 临时 project root + 本地 mock LLM（OpenAI 兼容 `/v1/chat/completions` SSE + `/v1/models`，LM Studio 同构）+ 真实浏览器（IAB）。

## 二、走查面与结果（全部通过）

| 面 | 结果 |
|---|---|
| 应用启动 + 项目配置缺失错误面 | ✓ 双语错误 + 「重试」按钮可用；补 inkos.json 后一键恢复 |
| SSE 实时连接 | ✓ 页脚「sse-ok 实时」徽标 |
| 书列表 + 契约报错质量 | ✓ fixture status 写错枚举时引擎报错**直接列出全部合法枚举**（incubating/outlining/active/...）——排障体验佳 |
| **导入管理器·母本导入全链（273 号）** | ✓ 注入 TXT → 「上传外部母本」→ `/import/canon/upload` → `/books/:id/import/canon-file`（storedPath 点目录过 ingest 守卫）→ mock 导入器 → 「母本导入成功」；盘上三段产物齐全（uploads/canon/ → materials/{id}.md+json → story/fanfic_canon.md，正典文档结构完整） |
| **服务商管理页**（38 服务分组） | ✓ LM Studio 配置 → 「测试连接」→ 「连接成功，1 个模型，已自动匹配 lm-mock-model / Chat / 流式」→ 保存持久化 |
| **聊天面全链（256 号面）** | ✓ 未配模型时「请先选择一个模型」守卫（失败历史保留）；配置后同会话重发 → 引擎 agent 路由 → LLM → SSE 渲染回复 + 模型署名「LM Studio (本地) · lm-mock-model」；会话持久化为 `interactive-film` 类型、标题取自首条消息 |

## 三、行为链洞察（非缺陷）

1. **服务保存以探测成功为门**：`保存` 先 POST `services/:id/test`（探测 /models 与协议），失败则中止不落盘——走查初版 mock 缺 `/models` 时保存被正确拦下，补齐后一次通过。该链路防呆设计合理。
2. 面板打开即自动探测一次（「无法自动确定模型」提示属挂载探测结果），保存再探测一次——hook 记录中的「双请求」实为两轮合法调用，已排除重复提交缺陷。

## 四、验证性质与遗留

纯走查批，零代码改动；工作区仅并行会话在途三件（package.json/rustbin.rs/audit-rust.mjs，第五轮未落库，未触碰）。console 深度错误监听未做（IAB 无原生 console 桥），以功能流+渲染验证为准。遗留：274/275/276 待推送；两项默认值、ja A/B、历史瘦身待用户决策。
