# 283 号：影游试玩态 TS 侧对照——结构性关闭（282 号遗留清偿）

- 日期：2026-09-10
- 分支：develop
- 关联：282 号（试玩走查——本批清偿其「TS 侧未对照」遗留）、69 号（play 域移植）
- 编号衔接：查当日目录最大号 282，顺延 283
- 推送核验：origin/develop = a1b34a33 未动；本地领先 274–283 共 10 提交待推

## 一、结论：无需运行时对照，一致性为结构性成立

读 `StoryPlayer.tsx` 实现定性：

1. **影游试玩器是纯客户端实现**——唯一服务端触点是 `GET /projects/:id/story-graph`（原样回显），剧情推进完全跑在浏览器内的 core evaluator（`visibleChoices/applyEffects/initVarState`，来自与两端同一份 `@actalk/inkos-core`）。因此「试玩态是否持久化」与后端无关：282 号观察到的刷新重置是**双端同构的设计行为**，不存在 TS/Rust 分歧面。
2. story-graph 端点双端均为原样回显（TS server.ts L6814 `JSON.parse(raw)` 直返 vs Rust 同语义），276–282 各轮已验证。
3. 顺带核查 book 分支互动的 play 面（PlayHud 消费）：Rust 四条路由（`runs/:worldId/:runId` GET / `image-settings` PUT / `generate-image` POST / `images/:file` GET）与 TS 四条**全对齐**，无缺口。

## 二、验证

纯代码走读批，零改动零运行。另：root package.json（并行会话在途）中死字段 `pnpm.overrides` 的清理顺延至其落库后（278 号已备案）。

## 三、遗留

274–283 共 10 提交待推送；并行会话三件第十二轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
