# 265 号：发布门禁脚本全量验证——audit:semantic-patterns 误报修复 + 构建链/负载门实测

- 日期：2026-09-10
- 分支：develop
- 关联：259 号（bench 负载门）、263/264 号（wasmtime 升级）、bench-gate-load-noise 记忆（负载噪声协议）
- 推送核验：origin/develop 仍停 6bc65564；本批落库后本地领先 68 提交。两项默认值无新答复。

## 一、此前各轮未覆盖的验证面，本轮补齐（全绿）

| 验证项 | 结果 |
|---|---|
| `verify:publish-manifests` | ✓ core/cli/studio 三包发布清单 OK |
| `verify:engine-bindings` | ✓ 158 bindings 导出全绿 + 170 关联测试通过 |
| `pnpm -r build` | ✓ core/studio/cli 全包构建通过（studio 21.5s） |
| `bench:gate` | ✓ 负载门按设计拒跑（实测 loadavg 8.79/18=49%/核 > 20% 阈值）——满载拒跑即正确行为，性能基线维持 259 号静默时段全绿结论，不强跑不洗基线 |

## 二、发现并修复：audit:semantic-patterns 误报（exit 1 → 0）

- 现象：语义/模板反模式审计报 4 个候选并失败——CLI TUI 斜杠命令解析（`/short` `/cover` `/play` `/write`，agent-input.ts）。
- 判定：**误报**。这些是用户显式字面命令的字面路由（`entryRoute` → `actionSource:"slash"`），非自由文本语义意图推断（该审计的真实目标）；脚本内已有 25+ 条同风格形态豁免。
- 修复：按既有风格补窄豁免（`agent-input.ts` + `/^\/` 形态），附理由注释。复跑 **0 候选 exit 0**——恢复审计信噪比，未来真发现不再被淹没。

## 三、编号碰撞处理备案

并行会话与本会话同时为 wasmtime 升级取号 263（其文档未提交）。按「先提交占号」其文档让位改编 **264 号**归并入库（保留其升级动机/audit 数据：cargo audit 21→0 漏洞、20 条 wasmtime 家族含沙箱逃逸级；h2→0.4.19），并更正其遗留 #1：`tests/wasm_plugin_execution.rs` 即真实 component 执行集成测试，升级后 3/3 真跑。

## 四、门禁汇总

本批代码变更仅 audit 脚本豁免一行；其余为验证与文档。各套件门禁维持 261–263 号轮次全绿（engine lib 1335 + duel 10/10 真跑 + clippy 0；Node 2943 + typecheck；src-tauri 576 + clippy 0；e2e 36/36）。

## 五、遗留

- bench:gate 全量真跑需静默时段（负载门会自动拒绝满载执行）。
- 推送（255–265 共 68 提交）、两项默认值、ja A/B 仍待用户决策。
