# 261 号：259 号交叉评审——panic 韧性修复与负载门复验通过，协助落库（fbcf2933）

- 日期：2026-09-10
- 分支：develop
- 关联：259 号（被评审对象，并行会话批次）、260 号（本会话上批）
- 推送核验：origin/develop 仍停 6bc65564；本地 HEAD = fbcf2933，领先 63 提交。两项默认值无新答复。

## 一、交叉评审结论：批准，零缺陷

| 改动 | 评审结论 |
|---|---|
| `load_raw_book_config` 非对象根守卫 | ✓ 单点守卫策略合理：book.json 为合法 JSON 但非对象根（`[1,2,3]`/`42`）→ InvalidData → 四调用方（get/put_review_mode、get/put_timeline_auto_beats，1349/1395/1484/1515 行）既有 `Ok(..) else → 404` 分支承接；原 `as_object_mut().unwrap()` 成为不可达防御代码 |
| 韧性回归测试 | ✓ GET/PUT × 两端点四路径全断言 404，修复前必红 |
| bench-gate 负载门 | ✓ 退出码语义实测正确：拒绝路径 exit=3、非法阈值 exit=2、报文清晰 |

**已记录的语义取向**（非缺陷）：非对象根 GET 由「静默回退项目默认」改为 404——对齐 GET 路由「book.json 不可解析 → 404」文档语义，对损坏文件显式失败优于静默兜底，与 TS 行为有意的文档化分歧。

**自纠**：首轮实测负载门误报 exit=0，系管道后 `$?` 取到 head 的退出码——改无管道重测后 exit=3 正确，非产品缺陷。

## 二、全量门禁（含 259 修复的合并工作区，全绿）

- engine：lib **1335**（含 259 新增韧性测试）+ e2e 197 + golden 76 + INKOS_DUEL=1 duel **10/10 真跑**（41.5s）；clippy --all-targets 0。
- Node 侧与 src-tauri 本批未触及（260 号轮次门禁仍有效）。

## 三、协助落库

259 号（books_state_routes.rs + 文档）按其「待提交」意图带路径限定提交为 **fbcf2933**。

## 四、本批自身代码变更

无（纯评审/复验/落库协助批）。
