# 271 号：根脚本门禁完整性审计——两个死入口移除（lint 空壳 + benchmark 指向已删脚本）

- 日期：2026-09-10
- 分支：develop
- 关联：265/266 号（e2e/wasm 测试同类的"入口承诺 vs 实际交付"审计线索）
- 编号衔接：承接 270 号顺延 271
- 推送核验：HEAD 6b3341c6；本批待提交（package.json + 本记录）

## 一、审计方法

根 `package.json` 每条 scripts 承诺逐一核兑现：目标文件存在性、子包脚本存在性、实际可运行性。

## 二、发现与处置

| 入口 | 状态 | 处置 |
|---|---|---|
| `lint` → `pnpm -r lint` | **死路**：core/cli/studio 三包均无 lint 脚本，实跑 `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT`；全仓（CONTRIBUTING/README×3/docs）零引用 | 移除。引入 eslint 属新工具链决策，留用户拍板；在此之前保留一个" doors 通向虚无"的入口只会误导 |
| `benchmark:studio-e2e` → `scripts/studio-e2e-benchmark.mjs` | **死路**：目标文件 2026-06 上游提交 a467d5c1（"neutralize public prompt wording"）删除，入口残留 3 个月；零文档引用 | 移除 |
| build/dev/test/typecheck/release | ✓ 真实可用（typecheck/test 本会话多次实跑全绿） | 保留 |
| audit:semantic-patterns / verify:publish-manifests / verify:engine-bindings / bench:gate | ✓ 目标文件在位（bench:gate 本会话实跑；audit-semantic-patterns 刚经 265 号修复） | 保留 |
| audit:rust | ✓ 本会话新增（268 号） | 保留 |

另核实：根 1.8.0 与桌面壳 0.2.0 的版本差属**有意双产品线**（主线 CHANGELOG v1.8.0 / 桌面壳独立版本，163 号发 v0.2.0），非漂移，不动。

## 三、验证

- package.json JSON 完整性 ✓；`pnpm run` 列表无 lint/benchmark 残留 ✓；`audit:rust` 接线完好 ✓
- 移除为纯删除性改动，不触及任何运行路径；`pnpm -r test`（2943）/`typecheck` 本会话已多次全绿，无需重跑

## 四、遗留

1. 若需真 lint（eslint）：三包统一配置属工具链决策，留用户。
2. 历史瘦身、推送、两项默认值、ja A/B——待用户决策。
