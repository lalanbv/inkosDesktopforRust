# 275 号：双端全门禁整合复跑——HEAD 全绿+推送进度核验（274 号后整合回归）

- 日期：2026-09-10
- 分支：develop
- 关联：273/274 号（本批为其后的双端整合回归）、267 号（上一轮 HEAD 整合冒烟先例）
- 编号衔接：查当日目录最大号 274，顺延 275
- 推送核验：**origin/develop = a1b34a33（273 号）**——用户已完成本轮 Fork 推送（领先 3→1），本地仅余 274 号待推

## 一、TS 侧全门禁复跑（距上轮自跑已跨多批，273/274 均为 engine-rs 改动，本批验证 monorepo 整合面）

| 门禁 | 结果 |
|---|---|
| `verify:publish-manifests`（core/cli/studio workspace-protocol） | ✓ 三包 OK |
| `verify:engine-bindings` | ✓ 158 bindings 导出全绿 |
| `audit:semantic-patterns` | ✓ 0 候选 |
| `pnpm -r test` | ✓ core 202 文件 + studio 90 文件/793 测 + cli 42 文件/232 测，EXIT 0（日志中的 abort/analytics 报错行均为用例内预期噪声） |
| `pnpm -r typecheck` | ✓ 0 错 |
| **Playwright e2e 36 规格**（tsx Node 参考端 + vite dev + INKOS_AGENT_LLM_STUB=1） | ✓ **36/36 通过（2.5m）**——255 号 e2e「缺失误判」教训后的整装复跑，确认 skip 掩盖为零 |

## 二、Rust 侧门禁（273/274 号批内已全绿，此处汇总引用）

全量 `cargo test`（INKOS_DUEL=1 真跑）lib 1338 + e2e 200 + duel 10 + golden 70 等 **1624 通过 0 失败**；clippy --all-targets 0 警告；生产 bin 真实 HTTP 冒烟 4 项（273 号）。

## 三、并行会话状态

`package.json` / `src-tauri/src/engine/rustbin.rs` / `scripts/audit-rust.mjs` 连续第四轮核查仍在途未落库，272 号 cargo-audit 边界提醒维持待其收尾，本批未触碰。

## 四、遗留

1. 274 号待用户推送（唯一本地领先提交）。
2. 两项默认值、ja A/B、历史瘦身——待用户决策。
