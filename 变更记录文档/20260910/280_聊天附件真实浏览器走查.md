# 280 号：聊天附件真实浏览器走查——/agent 3.4MB body 通过，273 号三域验证闭环

- 日期：2026-09-10
- 分支：develop
- 关联：276 号（聊天文字链走查——本批补附件维）、273 号（/agent 64MB 读取上限——本批经真实 UI 验证）、279 号（translation/style 两域已验）
- 编号衔接：查当日目录最大号 279，顺延 280
- 推送核验：origin/develop = a1b34a33 未动；本地领先 274–280 共 7 提交待推

## 一、走查面与结果（全部通过，无产品缺陷）

| 面 | 结果 |
|---|---|
| **/agent 大 body（273 号最后未验域）** | 2.4MB 附件经 UI 序列化 → **POST /api/v1/agent body 3,400,434 字节 → 正常进入业务层**（旧代码 `Bytes` 提取器 2MB 即 413 纯文本）；hook 实测 bodyLen 佐证 |
| 附件登记与 UI 呈现 | 附件「reference-notes.md (text/markdown, 2.4 MB)」消息内正确显示；重试按钮随失败出现 |
| **文本注入上限契约** | 2.4MB 文本（~110 万字符）超 `MAX_AGENT_ATTACHMENT_TEXT_CHARS=120,000` → 拒绝注入，报错与 TS **逐字同源**（"is too large to inject without semantic compaction"）——双端一致的有意契约，非缺陷 |
| **小附件 happy path** | 108KB 附件 → 引擎归一/落盘 → 注入当轮 prompt → mock 回复渲染 + 模型署名「LM Studio (本地) · lm-mock-model」 |
| 模型配置状态机复验 | 未配模型时守卫；`测试连接` 成功（依赖 mock /models，279 号清理曾误删 mock 脚本导致一次探测失败——环境侧自愈）→ `保存` → 守卫解除 |

## 二、结论

273 号三个放宽域（translation / style / agent）**全部经真实 UI 验证闭环**；附件注入 120k 字符上限与 TS 契约逐字对齐得到实证。纯走查批，零代码改动。

## 三、遗留

274–280 共 7 提交待推送；并行会话三件第九轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
