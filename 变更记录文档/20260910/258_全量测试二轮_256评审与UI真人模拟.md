# 258 号：全量测试与代码审查日（第二轮）——256 号评审修复 + 生产 UI 真人模拟全链走查

- 日期：2026-09-10
- 分支：develop
- 关联：256 号（被评审对象）、255 号（第一轮全量审查日）、242 号（浏览器走查先例）
- 推送核验：origin/develop 仍停 6bc65564——**201–257 共 60 提交待推送**（本地现含并行会话 255/257 批）；本批随 258 后本地领先 61。两项默认值无新答复。

## 一、全量测试（全绿）

| 门禁 | 结果 |
|---|---|
| cargo test（INKOS_DUEL=1 全目标） | ✓ lib 1336 + e2e 197 + golden 76 + duel **10/10 真跑**（42.3s） |
| cargo clippy --all-targets | ✓ 0 告警 |
| pnpm -r test | ✓ 2943 通过（core 1918 / studio 793 / cli 232；日志中的 failed 行为 mock 错误注入的预期输出） |
| pnpm -r typecheck | ✓ 三包全过 |

（批次内两处修复后回归：lib/集成/clippy 仍全绿；255 号清理调试测试后 lib 计数 1336→1334 为并行批次所致。）

## 二、代码审查（256 号逐文件自审）发现与修复

1. **[已修] generate_node_image size 无枚举校验**：TS 在 TypeBox 边界拒绝非法 size，Rust 执行器原样透传生图服务。补参数边界校验（1536x1024/1024x1536/1024x1024，校验位于图谱加载之前——对齐 TS「schema 校验先于 execute」），+错误路径断言。
2. **[已修] phaseRevs 保留条件过严**：TS `phaseRevs !== undefined` 即保留任意形态，Rust 多加 `is_object` 过滤——去掉过滤逐字对齐。
3. **[核实非缺陷] POST /sessions bookId 疑 null**：系冒烟复用 sessionId 幂等创建所致，UI `createSession({bookId, sessionKind})` 与 Rust 端点匹配（session_routes 读 `bookId` 键）。

## 三、模拟真人测试（生产引擎 4782 + studio dist + mock LLM 4781 + 浏览器）

| 链路 | 结果 |
|---|---|
| UI 入口 `#/film-author/p1` | ✓ 影游项目列表/创作页/SSE 实时连接正常（无 inkos.json 时给出明确错误卡+重试，符合预期） |
| 模型门控与配置 | ✓ 「请先选择一个模型」→ 服务商管理 → 自定义服务（Base URL/Key/协议/流式）→ 测试连接（引擎 live /models 探测成功）→ 保存 → inkos.json `llm.*` 持久化 → 回到创作页自动选中「本地Mock · mock-model」 |
| authoring 会话自动创建 | ✓ mock 请求体核验：系统提示词=authoring 向导（含项目 id「p1」）、第 2 条消息=完整权威图谱注入（`[以下是当前互动影游…` 前缀）、工具表=七件作者工具+propose_action+use_skill、**无** material/文件/research/import |
| set_world_anchor 全链 | ✓ 工具调用→真实落盘（storyCore/durationMinutes 更新、rev 推进）→收尾回复渲染 |
| fill_node 快乐路径 | ✓ 「补写」指令→fill_node→内层纯 JSON 解析→节点 n2 真实补写（title/sceneDesc/dialogue）→rev+1 |
| fill_node 错误路径 | ✓ LLM 回垃圾 JSON→「fill_node: LLM returned no JSON」错误工具卡（意外由 mock 缺陷轮验证，引擎行为正确） |
| 工具执行卡 | ✓ 展开显示工具名 set_world_anchor |
| Enter 发送疑失效 | ✓ **核实为自动化焦点时序假象**：合成 keydown 探针 `defaultPrevented=true` 且成功发送——React onKeyDown 一直正常 |

## 四、遗留

- 无新增。255 号已记录的提交信息互换（8a775c1c/868097b3）维持备案不 rebase。

## 五、变更文件

- `engine-rs/src/interaction/film_authoring_tools.rs`：size 枚举边界校验 + 错误路径测试断言。
- `engine-rs/src/server/interactive_film_routes.rs`：phaseRevs 逐字保留。
