# 279 号：翻译/文风页真实浏览器走查——273/274 改动面经 UI 全链验证通过

- 日期：2026-09-10
- 分支：develop
- 关联：276 号（走查先例——本批覆盖其未达的翻译/文风页）、273 号（body 上限修复——本批经真实 UI 大载荷验证）、274 号（translations/upload 解析收敛——同）
- 编号衔接：查当日目录最大号 278，顺延 279
- 推送核验：origin/develop = a1b34a33 未动；本地领先 274–279 共 6 提交待推

## 一、环境

engine bin 直启（`INKOS_BUILTIN_GENRES_DIR=packages/core/genres`——277 号补文档的 env 实用先例）+ **278 号新依赖构建的 studio dist**（mermaid 11.17.2/dompurify 3.4.13 等首次真机渲染）+ mock LLM + 临时 project root + 真实浏览器。

## 二、走查面与结果（全部通过，无产品缺陷）

| 面 | 结果 |
|---|---|
| **翻译译介·上传** | 页内注入 4.4MB TXT → 「上传源文件」→ **POST /translations/upload body 5,898,732 字节 → 200**（旧代码 axum 2MB 即 413 纯文本）——273 号 128MB 读取上限 + 274 号 base64-only 解析收敛经真实 UI 载荷验证；「已上传：.inkos/uploads/translation/...」 |
| **翻译译介·建项目** | 「创建翻译项目」→ 项目「剑气长歌」建成，详情页源文分段/译文占位/审校报告骨架齐全 |
| **文风·分析** | 文本样本 120 万字符（UTF-8 body ≈3.6MB）→ 「分析」→ StyleProfile 渲染（平均句长/词汇多样性/主要模式）——`/style/analyze` >2MB 经 UI 验证 |
| **文风·导入到书籍** | 选演示书 → 「导入文风指南」→ `story/style_guide.md` + `style_profile.json` 落盘实证 |

## 三、过程观察（非缺陷）

1. 翻译建项为两步流（上传 → 创建），按钮禁用态随步骤正确切换。
2. dev bin 跑文风导入需 `INKOS_BUILTIN_GENRES_DIR` 指向 `packages/core/genres`（题材档案随 core 分发、桌面壳打包；缺失时报错文案精确指出 fallback `other.md` 缺失——契约清晰）。
3. `#/style` 不在 HASH_PAGES（文风页仅侧栏入口、无深链）——与路由表设计一致，备案。

## 四、验证性质与遗留

纯走查批，零代码改动。273/274 全部三个放宽域（translation/style/agent）中 translation、style 已经真实 UI 验证（agent 附件链留待有附件的真实使用反馈）。遗留：274–279 共 6 提交待推；并行会话三件第八轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
