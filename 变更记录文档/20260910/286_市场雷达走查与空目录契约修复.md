# 286 号：市场雷达页走查+radar/history 空目录契约偏差修复

- 日期：2026-09-10
- 分支：develop
- 关联：285 号（走查系列——本批补齐最后一个 UI 面）、281 号（同类「UI catch 掩盖的状态码契约差」先例）
- 编号衔接：查当日目录最大号 285，顺延 286
- 推送核验：origin/develop = a1b34a33 未动；本地领先 274–286 共 13 提交待推

## 一、市场雷达页走查（最后一个未覆盖 UI 面，通过）

- 页面渲染 + 空历史优雅呈现（「点击扫描市场分析当前趋势和机会」）。
- 触发扫描：mock 非 JSON 输出 → 「Radar output format error: no JSON found」错误在 UI 内干净呈现，无崩溃——错误路径健康（LLM JSON 契约由解析器把关）。

## 二、真偏差修复：radar/history 空目录 500 vs TS 200

- TS `loadRadarHistory`：`readdir` 目录缺失 → catch → 返回 `[]` → **200 `{items:[]}`**（server.ts L2158-2165）。
- Rust `load_radar_history`：`read_dir` NotFound → Err 上抛 → **500 平铺错误**。
- UI 两侧都 catch 掩盖、视觉一致，但状态码契约不同——与 281 号（arcs 形状）同类：**对 UI catch 的依赖把契约差藏进了错误路径**，API 消费者（第三方/脚本）会感知。

**修复**：`load_radar_history` 目录缺失（NotFound）→ `Ok(vec![])`，其余错误仍上抛；逐文件过滤/降序/timestamp 回退逻辑经比对与 TS 一致，不动。新增单测 `radar_history_missing_dir_returns_empty`。

## 三、验证

单测 ✓（新断言 + 既有 radar 历史两测）；全量 `cargo test`（INKOS_DUEL=1）**1625 通过 0 失败**；clippy 0；生产 bin 实测 **200 `{"items":[]}`**（修复前 500）。

## 四、遗留

274–286 共 13 提交待推送；并行会话三件第十五轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
