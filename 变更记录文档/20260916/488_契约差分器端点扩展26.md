# 488 号：契约差分器端点扩展 14→26——六处分歧定性与豁免入册

日期：2026-09-16　分支：develop　基线：f3638852（487 号）

## 选题与实现

Xcode 许可仍未解除（cargo 欠账继续挂起），按候选将 487 号差分器端点扩展 **14→26**：活体探测确认 sessions/translations/skills/style-profiles/radar-history/genres/asset-library 另两库/doctor/chapters 内容/context-lens 单章/roster-candidates 双端 200 后入列。配套两项基建：

- `apiFor` 增加**根路径归一化**（doctor 等端点回显各自临时根绝对路径，按根字符串替换为 `%ROOT%` 后再解析）；
- Rust 腿 env 补 `INKOS_BUILTIN_GENRES_DIR`（内置题材目录缺省相对 CWD，编排 cwd 与桌面壳不同需显式钉路径——桌面壳启动对应关系备案）。

## 新增 6 处分歧定性（全部入豁免表/备案）

1. **/skills 清单 16 vs 1（真缺口备案）**：内置技能包 TS 16 个、Rust 引擎仅 1 个——技能目录双端不对齐影响 agent 可用技能面，需移植评估（等 cargo 解阻）；
2. **/genres 清单 15 vs 2（构建态待重验）**：TS 列出 core 内置 15 题材，Rust 仅项目级 2 个；编排已显式钉 `INKOS_BUILTIN_GENRES_DIR` 到 core/genres 仍未凑效——疑既有二进制早于 builtin 合并面，重建后重验；
3. **/asset-library/progression-mode 种子内容分叉**：与 genre-base 同族（364 号双端各自撰写）——三库种子 canonical 化待产品决策；
4. **/doctor 回退端缺 `retrieval.chunkCount` 键**：Rust 侧补齐需 cargo；
5. **/context-lens/2 entries 装配差异**（source/rank 随内部实现波动）：resync 管线内部差异备案；
6. **/roster-candidates currentChapter 3 vs 2**：resync 架构分歧备案余波。

豁免后终态：**26 端点对照 0 分歧，exit 0**。

## 门禁

- 全量 `pnpm -r test`：core 2113 + studio 906 + cli 218 = 3237 全绿（本号零产品代码改动，纯差分器扩展）；
- cargo 链接仍被 Xcode 许可阻断（481 号欠账待用户 `sudo xcodebuild -license accept`）。

## 改动

- `scripts/engine-contract-diff.mjs`：端点 14→26、根路径归一化、Rust 腿 builtin 题材 env、豁免表 +6 条（均注明备案缘由）。

## 后续

- cargo 门禁解除后三连：481 号全量 cargo test、skills/genres 内置包移植评估、bench:gate；
- 三库种子 canonical 化待产品拍板；npm 接受风险余 8 条滚动跟踪。
