# 499 号：pi-ai 0.73.1 类型迁移落地——496 摸底转正，双 pin 升级完成

日期：2026-09-16　分支：develop　基线：9c54a702（498 号）

## 实现内容

将 496 号"回退待专项"的 pi-ai 0.73 升级**直接落地**（运行时兼容已由 496 实测：core 2113 全绿）。类型层 6 断点的根因与修复：

1. **play-agents.ts 的 `Type` 导入源**：pi-ai 0.73 起转售 typebox 1.x——本文件 schema（submit_world_mutation/play_scene/graph_reconciliation 三工具的 parameters）改从 `@sinclair/typebox` 导入，与自家 `WorkerResultTool<TSchema>`（typebox 0.x TSchema）同源；运行时对象形态兼容（typebox 0/1 的 schema 对象均为普通 JSON-Schema 结构，pi-ai 仅序列化消费——496 已实测 2113 全绿）；
2. **agent-tools.ts sub_agent execute 具名参数收窄**：pi-ai 0.73 起 execute 形参为 `unknown`——改 `rawParams: unknown` + 函数体内 `const params = rawParams as SubAgentParamsType`。

升级内容：`@mariozechner/pi-ai` 与 `@mariozechner/pi-agent-core` 双 pin 0.67.1→**0.73.1**（core/cli package.json + workspace override）。

## 语义保持与收益

- **语义保持**：golden 提示词（239 号）、mock 锚点、LLM 错误分类（218 号）均未触及——pi-ai 仅是 provider 传输层；
- **收益**：pi-ai 0.73 移除 protobufjs（434 号 GHSA 诱因消失）、工具泛型对齐 typebox 1.x 上游、protobufjs override 兜底保留；
- **连带**：fast-xml-parser 仍经 @aws-sdk/client-bedrock-runtime 3.1030 链（5.5.8 < 5.7.0）——2 条上游钉死备案维持，后续随 aws-sdk/ai major 滚动。

## 门禁

- core build 0 错（typebox 迁移后）；全量 `pnpm -r test`：core 2113 + studio 886 + cli 218 = 3217 全绿；studio tsc 0 错；`pnpm audit:npm` 2 条白名单 exit 0；
- cargo 链接仍被 Xcode 许可阻断（481 号欠账 + 489 号 2 新单测持续排队）。

## 改动

- `packages/{core,cli}/package.json` + `pnpm-workspace.yaml`：双 pin 0.67.1→0.73.1；`pnpm-lock.yaml`；
- `packages/core/src/play/play-agents.ts`：Type 导入源切换（+注释）；
- `packages/core/src/agent/agent-tools.ts`：sub_agent execute 参数收窄（+注释）。

## 后续

- Xcode 许可解除后：481 号全量 cargo test（含 489 号 2 新单测）、duel、bench:gate、skills/genres 移植评估；
- 三库种子 canonical 化待产品拍板；npm 余 2 条上游钉死项滚动跟踪。
