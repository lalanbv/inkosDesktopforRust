# 506 号：npm 接受风险清零——最后两条 override 出清，白名单退役

日期：2026-09-16　分支：develop　基线：282fa080（502 号）；编号修正：原误编 503（撞 pi-ai 回归号），实为 506

## 出清

最后两条上游钉死项经**同 major 补丁线 override** 出清（无需 major 升级）：

- `fast-xml-parser 5.5.8 → 5.7.0`：经 @aws-sdk/xml-builder（精确钉 5.5.8，但 5.7.0 为同 major 补丁）——Bedrock 非默认启用且 5.x 内语义化兼容；
- `@ai-sdk/provider-utils 4.0.23 → 4.0.33`（补丁线 >=4.0.33）：ai 6.0.x 精确钉，同 major 补丁——**推翻 467 号"需 provider 3.0.16（镜像缺）"的判断**：provider-utils 可独立 override，不必动 ai 主包。

`audit-npm.mjs` 白名单**清空退役**（维护规则注释保留）：0 advisory、exit 0。

npm 接受风险自 454 号建档以来的清算闭环：454 建档 4 条 → 467 脚本化 8 条 → 482 vitest 系出清 → 491 三条出清 → 本号最终清零。

## 验证

- `pnpm audit` 0 advisory；audit:npm exit 0；
- 全量 `pnpm -r test`：core 2113 + studio 886 + cli 218 = 3217 全绿；studio tsc 0 错；
- override 风险评估：fxp 仅 Bedrock XML 面（非默认启用）；provider-utils 4.0.23→4.0.33 同 major 补丁（ai 未在仓内源码直连 import）。

## 改动

- `pnpm-workspace.yaml`：`"fast-xml-parser": 5.7.0`、`"@ai-sdk/provider-utils@4.0.23": 4.0.33`；lockfile 同步；
- `scripts/audit-npm.mjs`：白名单清空退役（注释保留清算史）。

## 后续

- Xcode 许可解除后：481 号全量 cargo test（含 489/500 号 4 个新单测）、duel、bench:gate、resync Rust 侧残余评估（唯一遗留队列）。
