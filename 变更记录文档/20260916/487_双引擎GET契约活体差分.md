# 487 号：双引擎 GET 契约活体差分——14 端点扫描，两处契约对齐修复

日期：2026-09-16　分支：develop　基线：8886abfd（486 号）

## 选题与实现

新增 `scripts/engine-contract-diff.mjs`：同时起 Rust 引擎与 TS 回退端（各自独立临时根 + 共享 mock LLM + 各跑一遍 fixture 保证数据形态），对 14 个稳定 GET 端点做**归一化深比对**——递归剪除易变键（时间戳/耗时/计数）、键排序规范化后逐字节比较；首个分叉点以 JSON 指针输出精确定位。已知行为差异走**豁免表**（每条注明备案缘由：resync 架构分歧 483/484、审计计数波动、种子内容分叉 364）。

## 扫描发现（首轮 7 处分歧 → 定性分类）

| 分歧 | 定性 | 处置 |
| --- | --- | --- |
| /books 列表与 /books/:id 多 `version` 字段 | TS 侧死负载（前端零消费） | **已修**：两处构建点剔除 version |
| /codex 多 `seeded` 键 | TS 侧死负载（CodexPanel 只读 cards） | **已修**：响应剔除 seeded |
| /books/:id chapters[].auditIssues 明细 3 vs 2 | 双端机检维度实现差异（审计内部面） | 豁免备案 |
| /promises currentChapter 3 vs 2 | resync 架构分歧（483/484 备案） | 豁免备案 |
| /quality-trend issueCount 3 vs 2 + null-vs-缺键 | 审计计数波动 + 359 号序列化先例（Rust 侧修复需 cargo） | 豁免备案 |
| /context-lens 章节 3 vs 2 | TS resync 产出 ch1 意图文件（483 备案） | 豁免备案 |
| /asset-library 种子内容完全不同 | 双端种子包各自撰写（364 号），canonical 化需产品决策 | 豁免备案（重要：**切引擎会改变种子创作指引**，建议用户拍板以一侧为准） |

## 门禁

- 差分器：14 端点对照 **分歧 0**（exit 0，豁免全为已备案差异）；
- 全量 `pnpm -r test`：core 2113 + studio 906 + cli 218 = 3237 全绿；studio tsc 0 错；
- cargo 链接仍被 Xcode 许可阻断（481 号欠账待用户 `sudo xcodebuild -license accept`）。

## 改动

- 新增 `scripts/engine-contract-diff.mjs`（双引擎契约差分器：编排 + 剪枝 + 豁免表 + 分叉指针）；
- `packages/studio/src/api/server.ts`：books 列表/详情与 codex 响应剔除死负载字段。

## 后续

- cargo 门禁解除后：481 号全量 cargo test、Rust resync 残余评估、bench:gate；
- 种子内容 canonical 化待产品决策；npm 接受风险余 8 条滚动跟踪。
