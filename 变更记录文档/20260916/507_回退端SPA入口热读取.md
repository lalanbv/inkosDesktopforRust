# 507 号：回退端 SPA 入口热读取——449 号对齐（消除重建后白屏窗口）

日期：2026-09-16　分支：develop　基线：c8bcd629（506 号）

## 缺陷与修复

TS 回退端 `startStudioServer` 在**启动时一次性缓存** `dist/index.html`。前端重建后（vite 清空 dist 重写 hash 资源），缓存旧入口引用的 hash 文件已不存在 → **白屏直到服务器重启**——与 449 号在 Rust 引擎侧修复的同类问题，回退端一直存在。

修复：SPA fallback 改为**按请求时读** `index.html`（低流量回退场景开销可忽略；读取失败 → 404），no-store 头保持（483 号）。

## 活体验证

tsx 启动服务器 → GET / 返回原入口 → 改写 dist/index.html → 立即 GET / 返回新内容（无需重启）——热读生效。

## 门禁

- studio tsc 0 错；全量 `pnpm -r test`：core 2113 + studio 886 + cli 218 = 3217 全绿；
- cargo 链接仍被 Xcode 许可阻断（481 号欠账 + 489/500 号 4 个新单测持续排队）。

## 改动

- `packages/studio/src/api/server.ts`：SPA fallback 启动缓存 → 请求时读（注明 507 号）。

## 后续

- Xcode 许可解除后：481 号全量 cargo test（含 489/500 号 4 个新单测）、duel、bench:gate、resync Rust 侧残余评估；
- 三库种子 canonical 决策已在 505 号证实可撤销；npm 余 2 条上游钉死项滚动跟踪。
