# 493 号：回退端补 /api/v1/health 探针 + 门禁清单纳入双引擎套件

日期：2026-09-16　分支：develop　基线：f384fdf5（489 号）

## 改动

1. **TS 回退端补 `GET /api/v1/health`**（对齐 Rust 62 号超集端点，483 号曾豁免的 404 项）：响应 `{ok:true, version, backend:"node-fallback"}` 与 Rust `{ok,version,backend:"rust-engine"}` 同形——监控/编排双端统一探此面；version 从 studio package.json 读取（createRequire，跟随包版本不手抄）。挂点在 no-store 中间件之后（健康响应同样禁缓存）。
2. `scripts/node-fallback-smoke.mjs`：node 腿新增健康探针断言（200 + ok + backend 标识 + version 类型）——套件 10 断言。
3. `packages/studio/TESTING.md` 门禁清单补两条机械门禁：`node scripts/node-fallback-smoke.mjs`（485/486 号）、`node scripts/engine-contract-diff.mjs`（487/492 号）。

## 验证

- 全量 `pnpm -r test`：core 2113 + studio 906 + cli 218 = 3237 全绿；studio tsc 0 错；
- node-fallback-smoke：10/10 断言绿 exit 0（含新 health 断言；rust 腿 health 本就恒 200）；
- 一次 require 相对路径错层（../ vs ../../）即修。

## 后续

- Xcode 许可解除后：481 号全量 cargo test（含 489 号 2 新单测）、duel、bench:gate、skills/genres 移植评估；
- 三库种子 canonical 化待产品拍板；npm 接受风险余 2 条上游钉死项滚动跟踪。
