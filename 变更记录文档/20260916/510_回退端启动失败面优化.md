# 510 号：回退端启动失败面优化——双语可行动指引

日期：2026-09-16　分支：develop　基线：f17e89fa（509 号）

## 缺陷与修复

483/505 号活体排障实测：在非项目目录启动 TS 回退端时，进程直接抛出**英文原始错误 + 完整堆栈**后 exit 1——功能上正确（拒绝启动），但中文用户面对堆栈无从下手。

修复（`packages/studio/src/api/index.ts`）：捕获 `inkos.json not found` 这一已知启动失败类，输出**双语可行动指引**（改用项目根作首参/设 INKOS_PROJECT_ROOT/先 inkos init），无堆栈噪音；未知错误保留完整堆栈供排障；两种路径均 exit 1。

## 活体验证

- 非项目目录启动：输出双语指引，exit 1 ✓（原始堆栈不再出现）；
- 正常项目根启动：/api/v1/health 返回 `{ok:true,version,backend:"node-fallback"}` ✓（493 号探针面不受影响）。

## 门禁

- 全量 `pnpm -r test`：core 2113 + studio 886 + cli 218 = 3217 全绿；studio vite7 构建 + tsc 0 错；
- cargo 链接仍被 Xcode 许可阻断（481 号欠账 + 489/500 号 4 个新单测持续排队）。

## 改动

- `packages/studio/src/api/index.ts`：启动失败分类处理（已知类双语指引 / 未知类保留堆栈）。

## 后续

- Xcode 许可解除后：481 号全量 cargo test（含 489/500 号 4 个新单测）、duel、bench:gate、resync Rust 侧残余评估；
- 三库种子 canonical 决策已在 505 号证实可撤销；npm 余 2 条上游钉死项滚动跟踪。
