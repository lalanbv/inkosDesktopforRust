# 511 号：依赖使用面审计（负结果确认）+ 统一门禁复验

日期：2026-09-16　分支：develop　基线：d5571eca（510 号）

## 审计过程

对三包（core/cli/studio）全部声明依赖（dependencies + devDependencies）做**零引用扫描**：收集包内所有 ts/tsx/js/mjs/cjs/css/json 文本（含包根 vite/tailwind/postcss/tsconfig 配置），逐依赖做文本包含检查。

- 初版扫描（仅 src/）误报 17 条——@types 系列、@tailwindcss/vite、@vitejs/plugin-react、autoprefixer、tailwindcss、tw-animate-css、@fontsource-variable/geist、shadcn、epub-gen-memory、ink-text-input 等实为**包根配置或 CSS @import 消费**（vite.config.ts / postcss.config / tailwind styles / CLI bin 入口），不在 src/ 文本内；
- 精化扫描（src + 包根配置）后：**零死依赖**。审计方法与结论入册备查。

## 结论

依赖声明面健康：无待移除条目。结合 482/491/499/506 号的依赖升级与出清批，依赖树当前处于**精炼且全部活跃**状态。

## 门禁复验

- `pnpm gate:ts` 全量：**7/7 步绿**（typecheck 16.0s / test 65.6s / audit:npm 1.6s / build 16.7s / node-fallback-smoke 33.4s / engine-contract-diff 35.0s / export-epub-smoke 33.5s），exit 0；
- 测试口径：core 2113 + studio 886 + cli 218 = 3217 全绿。

## 状态汇总（当前已知未清偿项全景）

1. **cargo 门禁（用户动作）**：481 号全量 cargo test、489/500 号 4 个新单测运行、duel、bench:gate、resync Rust 侧残余评估、skills/genres builtin 合并面重建重验——全部待 `sudo xcodebuild -license accept`；
2. **Fork 推送（用户动作）**：433–511 共 79 笔提交；
3. **上游钉死 2 条**：fast-xml-parser（pi-ai→aws-sdk，override 已出清）、@ai-sdk/provider-utils（ai 钉，override 已出清）——advisory 已零，仅 pi-ai 0.74/ai 7 的 major 跟进评估；
4. **三库种子 canonical**：505 号证实双端内容一致，无需决策（可关闭）。

## 后续

- cargo 解阻后一次性清算（上述第 1 条队列）；
- 用户推送与种子决策关闭。
