# 513 号：用户文档对齐——建书流程与导演驾驶舱说明更新

日期：2026-09-16　分支：develop　基线：d5571eca（512 号）

## 选题与实现

497 号移除 BookCreate 孤儿页面后，用户文档若仍描述旧表单建书流即已过时。核对并更新（docs/ 本地不入库，gitignore 16 行既有约定）：

1. **QUICK_START「方式二：通过新建入口」**：旧文描述"填写书名/题材…点击「创建」"的表单流（即已删除的 BookCreate 页面）→ 更新为现役**对话式建书**流程（侧栏/⌘K 进入建书对话 → 描述需求 → 确认卡 → 建书 + 生成设定），并补"建书完成即写导演灵感卡"说明（498/500 号）；
2. **USER_GUIDE** 新增「导演驾驶舱（书籍详情）」小节：一句话灵感 + 运行模式编辑保存、**生成方向候选**（508b 号新 UI）、已选用方向回显、阶段/已存章节/续跑建议实时显示、建书自动写灵感卡说明。

## 验证

- 文档与现役行为逐条核对（QUICK_START 建书流 = ChatPage 对话流；导演驾驶舱 = DirectorPanel 现有功能清单）；
- `pnpm gate:ts:fast`（typecheck/test/audit:npm）全绿 exit 0——本号零产品代码改动，3217 全绿延续；
- cargo 链接仍被 Xcode 许可阻断（481 号欠账 + 489/500 号 4 个新单测持续排队）。

## 改动

- `docs/QUICK_START.md`：方式二建书流程改写（本地文档，不入库）；
- `docs/USER_GUIDE.md`：新增导演驾驶舱小节（本地文档，不入库）。

## 后续

- Xcode 许可解除后：481 号全量 cargo test（含 489/500 号 4 个新单测）、duel、bench:gate、resync Rust 侧残余评估；
- 三库种子 canonical 决策已在 505 号证实可撤销；npm 余 2 条上游钉死项滚动跟踪。
