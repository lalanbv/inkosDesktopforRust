# 508 号续：方向候选生成 UI 接线——354 号端点首次消费方落地

日期：2026-09-16　分支：develop　基线：d5571eca 之后系（前序 508 号 agent 补齐 79dfe7ce）

> 编号说明：本文件为 508 号工作的后续扩展（方向候选 UI），在同一循环内完成。

## 缺口坐实

`POST /api/v1/director/directions`（354 号双引擎端点：灵感卡 → LLM → 方向候选数组）此前**前端零消费**——驾驶舱核心价值（方向候选生成）一直停留在 API 面。本号补齐 UI 接线。

## 实现

`DirectorPanel.tsx` 新增：

- **生成方向候选**按钮：`premise` 必填守卫（空 → 提示不发包）→ `POST /api/v1/director/directions {inspiration:{premise,keywords}, count:3}` → 渲染候选列表（标题/题材/钩子/差异化/置信度百分比）；
- **选用候选**：点击候选 → `PUT /books/:id/director {patch:{selectedDirection}}` → 通知已选用；
- 失败路径：错误信息经 notice 透出。

## 验证

- 新增 `DirectorPanel.directions.test.tsx` 4 用例：生成渲染（标题/钩子/差异化/置信度）、选用 PUT selectedDirection、premise 为空守卫、失败透出；
- 既有 `DirectorPanel.interaction.test.tsx` 3 用例回归通过（7/7）；
- 全量 `pnpm -r test`：core 2113 + studio 890（+4）+ cli 218 = **3217 全绿**；studio tsc 0 错；vite 7 构建通过。

## 改动

- `packages/studio/src/components/DirectorPanel.tsx`：方向候选生成按钮、候选列表渲染（标题/题材/钩子/差异化/置信度）、生成/选用逻辑；
- 新增 `packages/studio/src/components/DirectorPanel.directions.test.tsx`（4 用例）。

## 后续

- Xcode 许可解除后：481 号全量 cargo test（含 489/500 号 4 个新单测）、duel、bench:gate、resync Rust 侧残余评估；
- 三库种子 canonical 决策已在 505 号证实可撤销；npm 余 2 条上游钉死项滚动跟踪。
