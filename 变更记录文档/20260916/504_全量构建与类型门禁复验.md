# 504 号：全量构建与类型门禁复验——依赖大版本变更后的发布路径验证

日期：2026-09-16　分支：develop　基线：8935f381（503 号）

## 背景与执行

近十个循环累计三轮依赖大变更（482 vitest 3→5、491 studio vite 6→7 + pi-ai 0.73 相关、499 pi-ai 0.73.1 + typebox 1.x），各号验证均为"本号受影响面"；本号做一次**完整发布路径**复验，确认桌面打包的上游产物（core dist / cli dist / studio dist + server）在全部变更后仍然健康。

## 结果

- `pnpm -r typecheck`：core / cli / studio（双 tsconfig）**全部通过，0 错**；
- `pnpm -r build`：core tsc ✓、cli tsc ✓、studio `build:client`（vite 7 构建 4.5s）+ `build:server`（tsc）✓——**发布产物链完整**；
- 全量 `pnpm -r test`：core 2113 + studio 886 + cli 218 = 3217 全绿；
- `pnpm audit:npm`：2 条白名单 exit 0；`protobufjs` 复核：434 号 override `^7.5.8` 下现解析 7.6.6（@google/genai 1.50 传递）——GHSA 诱因已随 pi-ai 0.73 消除；
- 用户文档抽查：USER_GUIDE/QUICK_START 无已删除 BookCreate 页面的过时引用；TESTING.md 门禁清单与实际脚本一致（`pnpm -r typecheck` 真实存在）。

## 结论

依赖三轮大变更（vitest 5 / vite 7 / pi-ai 0.73+typebox 1.x）后的**发布路径完整可用**。本号零代码改动（门禁复验，470/503 同型）。

## 后续（不变）

- Xcode 许可解除后：481 号全量 cargo test（含 489/500 号 4 个新单测）、duel、bench:gate、resync Rust 侧残余评估；
- 三库种子 canonical 化待产品拍板；npm 余 2 条上游钉死项滚动跟踪。
