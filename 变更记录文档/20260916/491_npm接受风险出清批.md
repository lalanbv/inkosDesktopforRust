# 491 号：npm 接受风险上游摸底出清——8→2 条（esbuild/brace-expansion/psp 全清）

日期：2026-09-16　分支：develop　基线：f384fdf5（489 号）

## 选题与上游摸底

对剩余 8 条 npm 接受风险逐条查上游补丁可达性（npm registry 实查，非记忆推断）：

| 模块 | 补丁线 | 上游现状 | 处置 |
| --- | --- | --- | --- |
| esbuild (low) | >=0.28.1 | **vite 7.3.6 已支持 esbuild ^0.28**；vite latest 8.3.0（rolldown，零 esbuild） | **出清** |
| brace-expansion (moderate) | >=5.0.6+ | 双链：5.0.5（ts-morph→minimatch10，^5.0.5 范围内可达补丁线！）+ 2.1.4（epub 链，既有 override 已盖） | **出清** |
| postcss-selector-parser (low) | >=7.1.3 | shadcn ^7.1.0 范围内锁文件刷新即达 7.1.3+ | **出清** |
| fast-xml-parser (moderate) | >=5.7.0 | pi-ai 0.67.1 钉 fxp 5.5.8；pi-ai latest 0.73.1 —— LLM 库 major 跳变需 duel/e2e | 维持备案 |
| @ai-sdk/provider-utils (low) | >=4.0.33 | ai latest 7.0.102 用 provider-utils 4.0.51 ✓ 但 ai 6→7 major | 维持备案 |

## 实施

- `pnpm-workspace.yaml` overrides：`vite@7.3.1` → **7.3.6**；新增 `brace-expansion@5.0.5: ^5.0.6`、`postcss-selector-parser@7.1.1: ^7.1.3`（语义化范围内直达补丁线）；
- `packages/studio/package.json`：vite `^6.0.0` → **`^7.3.6`**（studio 构建升 vite 7——@tailwindcss/vite 与 plugin-react 的 peer 本就指向 7，488 号时代的 peers 告警随之消失）；
- `scripts/audit-npm.mjs` 白名单：移除 esbuild/brace-expansion/postcss-selector-parser 三条，仅留 fast-xml-parser 与 @ai-sdk/provider-utils 两条上游钉死项。

## 验证

- `pnpm audit:npm`：**advisory 8→2 条，白名单 2 条，exit 0**（esbuild/brace-expansion/postcss-selector-parser 全部出清）；
- studio 生产构建 vite 7 通过（4.55s）；全量 `pnpm -r test` 3237 全绿；studio tsc 0 错；
- 双引擎套件终验：node-fallback-smoke 双腿绿、engine-contract-diff 34 断言 0 分歧 exit 0（写入面 + GET 面对 vite 7 构建的 dist 复验通过）；
- 共享 mock 悬挂修复（smoke 收尾杀共享子进程——486 号后台卡死根因补清）。

## 后续

- Xcode 许可解除后：481 号全量 cargo test（含 489 号 2 新单测）、duel、bench:gate、skills/genres 移植评估；
- pi-ai 0.73 / ai 7 两条 major 升级需专项（LLM 库大跳变 + duel/e2e 全量回归）；三库种子 canonical 化待产品拍板。
