# 498 号：导演灵感卡服务端写入——354 号语义回迁至现役建书完成点（497 备案清偿）

日期：2026-09-16　分支：develop　基线：dadd7576（495 号系）　本轮实际基线：509a78c0/9b939409 之后。

## 实现

497 号立案的"建书 → 导演灵感卡"服务端写入，TS 回退端部分本号落地：

- 新增导出助手 `writeDirectorInspirationCard(root, bookId, premise)`（server.ts）：写 `.inkos/director/{bookId}.json`——**已有灵感卡不覆盖**（幂等）、bookId 缺省盖章、updatedAt 刷新、stage 缺省 `directions`、inspiration `{premise, keywords:[]}`；
- 接线点：`POST /books/create` 的 agent 链完成回调（`resolveCreatedBookIdFromDetails` → `completeBookExists` 确认之后）——premise 取 `body.blurb ?? body.title`；**失败不阻断建书**（try/catch + console.error）。

语义依据：354 号原意（建书成功 → 驾驶舱非空、stage=directions 可直接生成方向候选），premise 用 blurb/title（对话内确认 premise 不经此路径，用户可在驾驶舱输入框修改）。

## 验证

- 新增 2 单测（server.test.ts）：完成点写入形状（bookId/stage/inspiration/updatedAt）+ 已有灵感卡不覆盖；
- 全量 `pnpm -r test`：core 2113 + studio 886（+2）+ cli 218 = 3217 全绿；studio tsc 0 错；
- **Rust 侧引擎建书链的对等写入仍备案**（需 cargo；镜像点=book_create_routes 创建完成处写同一 JSON 形状），随许可解除专项处理。

## 后续

- Rust 侧 director 卡服务端写入（镜像本号语义，等 cargo）；
- Xcode 许可解除后：481 号全量 cargo test、489 号 2 新单测、duel、bench:gate、skills/genres 移植评估；
- 三库种子 canonical 化待产品拍板；npm 余 2 条上游钉死项滚动跟踪。
