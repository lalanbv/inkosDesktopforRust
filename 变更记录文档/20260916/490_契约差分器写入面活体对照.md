# 490 号：契约差分器写入面扩展——8 个变更端点写后读活体对照全绿

日期：2026-09-16　分支：develop　基线：f3638852（487 号同族，488/489 后）

## 选题与实现

把 481 号的**静态** body 审计升级为**活体**写后读对照：差分器新增写入阶段（WRITE_SET），对 8 个变更端点双端写入同一合法载荷、断言状态码一致，持久化等价随后由既有 26 端点 GET 差分面覆盖（含 book 详情的 bestOfN/seriesId/reviewMode 字段）。apiFor 扩展支持 PUT init；探针载荷以 core zod schema（AntiAiRuleSchema/ExperienceEntrySchema）为准构造。

## 覆盖与结果

写入集（双端 200 全过）：task-routing、codex 卡片、anti-ai-rules 规则、experience 经验条目、timeline-auto-beats、best-of-n、chapter-review-mode、series-id。
读取面：34 断言全绿（8 写入 + 26 GET），**分歧 0，exit 0**。

## 过程发现（定性记录）

- **错误面契约分歧（备案）**：非法载荷下 experience 端点 node=500（zod 抛错）vs rust=422——双端都拒绝（防重放/校验语义一致），仅错误码不同；对齐需动 Rust 错误码面（等 cargo），低优先级；
- anti-ai-rules 非法载荷双端一致 400（校验语义一致性 ✓）；
- 一次假故障教训：连续调试的后台进程/端口残留会让下一轮差分产生无意义失败——跑差分器前先 `pkill` 三件套并确认端口空闲（已入脚本使用惯例）。

## 门禁

- 差分器：**34/34 断言绿，exit 0**；
- 全量 `pnpm -r test`：core 2113 + studio 906 + cli 218 = 3237 全绿（本号零产品代码改动，纯差分器扩展）；
- cargo 链接仍被 Xcode 许可阻断（481 号欠账 + 489 号 2 新单测运行持续排队）。

## 改动

- `scripts/engine-contract-diff.mjs`：写入阶段（WRITE_SET 八端点）+ apiFor init 支持。

## 后续

- Xcode 许可解除后：481 号全量 cargo test、489 号 2 新单测、duel、bench:gate、skills/genres 移植评估；
- 三库种子 canonical 化待产品拍板；npm 接受风险余 8 条滚动跟踪。
