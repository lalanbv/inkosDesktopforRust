# 291 号：UI 向导创建书籍全链走查——propose→confirm→生产链在生产引擎实证

- 日期：2026-09-11
- 分支：develop
- 关联：276 号（聊天面走查——本批补创建旅程）、231 号（confirm 链架构）、58/59 号（创建域移植）、281 号（走查系列）
- 编号衔接：查 20260911 目录最大号 290，顺延 291
- 推送核验：origin/develop = a1b34a33 未动；本地领先 274–291 共 18 提交待推

## 一、环境

生产 bin + 真实浏览器 + **意图级 mock**（按消息角色分派：首轮发 `propose_action` 工具调用携带 `createBook` 结构化载荷——SSE `delta.tool_calls` 线格式与 65 号 e2e 同构；architect 命令走 SECTION 地基文档；资深编辑走 PASS 审校）。

## 二、验证过程与结果

| 步骤 | 结果 |
|---|---|
| `#/book/new` 聊天向导 | ✓ 无书 clarify 提示渲染；发送消息自动建 `book-create` 会话 |
| **propose→confirm 生产链** | ✓ 意图工具调用经 agent 循环触发 `run_confirmed_production`：architect completed → **`books/剑气长歌/` 完整落地** |
| Step1 产物完整性 | ✓ book.json + chapters/index.json + story/ 八件地基（author_intent/book_rules/brief/character_matrix/current_state/emotional_arcs/pending_hooks/current_focus）+ outline/（story_frame/volume_map） |
| 模型守卫与服务配置 | ✓ 276 号流程复验（测试连接 → 连接成功自动匹配 → 保存）；服务页深链 `#/services/lmstudio` 可用 |
| 无 LLM 的纯客户端面 | ✓ 影游试玩（282/283 号）与书籍工作台（289 号）不依赖 LLM 配置即工作 |

## 三、自动化摩擦备案（非缺陷）

1. 聊天 composer 的合成 `.click()`/Enter 在**页面 reload 后首次提交**偶发静默失败（React 受控输入时序）——role 定位原生 click 更可靠；首条消息与后续会话均正常。
2. mock 按消息条数分派不可靠（会话带预置上下文）——按「末条消息角色」分派才稳定。

## 四、结论

最后一条未走查的大用户旅程（创建一本书）在生产引擎全链实证通过。e2e（6818 号用例）+ 生产 bin 走查双覆盖。至此创建/写作/导入/导出/影游/会话/配置七类旅程全部真实 UI 或生产引擎级验证。

## 五、遗留

274–291 共 18 提交待推送；并行会话三件第二十轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
