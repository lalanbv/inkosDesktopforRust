# 328 号：跨域组合链生产实测——fanfic_init → spinoff_create → write_next + 番外父链对齐修复

- 日期：2026-09-11
- 分支：develop
- 关联：301/303 号（四创建域三层）、317 号（五域意图矩阵——本批为其跨域组合延伸）、308 号（续写语义）、55 号（importCanon）
- 编号衔接：查 20260911 目录最大号 327，顺延 328
- 推送核验：origin/develop = 498253fa（300 号）未动；本地领先 301–328 共 28 提交待推

## 一、发现与修复：番外意图路径 parentBookId 不落盘

**TS 双路本就分叉**（跨读对齐时发现）：

| TS 路径 | 配置构建 | parentBookId 落盘？ |
|---|---|---|
| HTTP `POST /spinoff/init`（server.ts:6446） | `buildStudioBookConfig` | ✗ 不落 |
| 聊天/意图工具路径（server.ts:1442 → `createSpinoffBookTool` → agent-tools.ts:91 `buildAgentBookConfig`） | 展开传入 `parentBookId`（agent-tools.ts:115） | ✓ 落 |

Rust 此前两条路径都走 `build_studio_book_config`（无 parent 参数）→ 均不落。**修复**：`execute_spinoff_create`（意图执行器）构建配置后补 `parent_book_id = Some(...)`（agent_production.rs）；直连端点 `/spinoff/init` 维持不落——逐路径对齐 TS。

- 消费面核实：`parentBookId` 目前 UI/core 只写不读——数据保真缺口而非功能破损；book.json 为用户可见工件，按逐字对齐纪律修复。
- 新增 e2e `spinoff_intent_persists_parent_book_id`（sub303 模块）：parentBookId 持久化 + targetChapters/chapterWordCount/genre 三字段继承父书 + `story/parent_canon.md` 落盘。修复前该断言必挂。

## 二、生产引擎组合链实测（release bin + 入库 walkthrough-mock，全通过）

工作区 /tmp/inkos-328 + `INKOS_BUILTIN_GENRES_DIR=packages/core/genres` + mock:1241 + 引擎:1242。

| 步骤 | 结果 |
|---|---|
| **A. fanfic_init**（button 意图，sourceText+mode=canon） | ✓ 书 `斗破苍穹同人-药老传`：book.json `fanficMode:"canon"`、`fanfic_canon.md`、地基（outline/roles/rules/hooks）、控制文档齐整；无 parent（正确） |
| **B. spinoff_create** 挂接 fanfic 父书 | ✓ 书 `药老前传-丹塔风云`：**book.json `parentBookId:"斗破苍穹同人-药老传"`（修复生产生效）**；targetChapters=24 / chapterWordCount=2500 / genre / platform 四字段继承父书；`parent_canon.md` 带确定性 meta 块（parentBookId/parentTitle/generatedAt）——从 fanfic 父书正典导入闭环（读父书 story_frame + 控制文档，两域地基布局同构使组合可行） |
| **C. write_next** 对番外书写第 1 章 | ✓ `chapters/0001_风起.md` + index 落盘；**长度预算 `target: 2500` 即继承自 fanfic 父书的 chapterWordCount——跨域继承直达写作管线**；状态 state-degraded 如实记录（mock 写手 62 字远低于预算 + 状态校验 agent 无分支可得空响应——317 号已备案的 mock 保真度边界，非代码缺陷） |
| **边界探针** parentBookId=ghost | ✓ `AGENT_ACTION_FAILED`「正传书籍「ghost-book」不存在」清晰报错，无半成品目录残留 |

本轮首跑曾因引擎默认 `assets/genres` 相对 CWD 找不到题材档案报 `Genre profile not found`——显式 `INKOS_BUILTIN_GENRES_DIR` 后通过（引擎默认值语义备案：相对 CWD，桌面壳直启时由壳层解析绝对路径）。

## 三、门禁与结论

- engine-rs 全测 `INKOS_DUEL=1`：**1632 passed / 0 failed**（1631 + 新增 1）；clippy 0 警告。
- 价值定性：单域测试各域自洽，**接缝缺陷只有组合链能抓**——本次 parentBookId 缺口正藏在 fanfic→spinoff 的域间路径上（301–308 号四域单测全部通过、317 号五域单域实测也全通过，均未覆盖）。
- 遗留：28 提交待推送（Fork 图形端）；并行会话三件（package.json/rustbin.rs/audit-rust.mjs）在途未落库，保持未触碰；两项默认值、ja A/B、历史瘦身待用户决策。
