# 296 号：全量路由表交集扫描——通配符归一后零真实缺口

- 日期：2026-09-11
- 分支：develop
- 关联：273 号（canon/upload 缺失端点先例——本批为该类缺陷的系统性排查）、287/288 号（共享端点错误路径扫描——与本批合为端点级 parity 三部曲）
- 编号衔接：查 20260911 目录最大号 295，顺延 296
- 推送核验：origin/develop = a1b34a33 未动；本地领先 274–296 共 23 提交待推

## 一、方法

Python 提取 TS server.ts 全部 `app.(get|post|put|delete)` 的 (method, path)（139 对）与 Rust mod.rs 全部 `.route()` 块内方法组合（146 对），通配符语法归一（TS `:file{.+}` vs Rust `*file`）后双向求交。

## 二、结果

| 方向 | 原始差数 | 归一后真实差数 |
|---|---|---|
| TS 有 / Rust 缺 | 5 | **0**（truth/artifacts/files 三组均为 `*file` 通配语法差，Rust 全在） |
| Rust 有 / TS 缺 | 12 | **0 缺口 + 6 Rust 增补** |

Rust 增补 6 端点定性（前端零调用，全为基础设施/链内部）：

- `GET /health`——引擎生命周期探针（桌面壳健康检查目标，TS sidecar 时代走 `/` 静态探测）；
- `GET|PUT /project/input-governance-mode`、`POST /utils/{count-length,cap-context,derive-book-id}`——工具/治理面（TS studio 进程内直调 core 无需 HTTP，Rust 前端解耦后需显式端点）；
- `POST /books/:id/settle`——写链内部端点。

## 三、结论

端点级 parity 三部曲收口：①存在性（本批，零缺口）②共享端点错误路径（287/288，全对齐）③SSE 事件面（293，66 全对齐）。canon/upload 类「端点缺失」缺陷的系统性排查完成。

## 四、验证

纯机械比对批，零代码改动。门禁状态不变。

## 五、遗留

274–296 共 23 提交待推送；并行会话三件第二十五轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
