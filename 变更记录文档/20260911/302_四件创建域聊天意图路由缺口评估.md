# 302 号：四件创建域聊天意图路由缺口评估——fanfic/续写/番外/仿写立项（301 号延伸）

- 日期：2026-09-11
- 分支：develop
- 关联：301 号（schema 层补齐——本批发现其下游路由层仍未通）、231 号（四件走 propose→confirm 的架构备案）、59 号（HTTP 端点已移植可用）
- 编号衔接：查 20260911 目录最大号 301，顺延 302
- 推送核验：origin/develop = 498253fa（300 号）——274–300 号已被用户推送（领先 28→1 后本轮 +301/302）；301 号随本批后待推

## 一、生产引擎实测（缺口实证）

生产 bin + 意图 mock 实测：`POST /agent` 携带 `requestedIntent=fanfic_init` + `actionSource=button` + 完整 `actionPayload.fanficCreate` → **响应为普通聊天回复（PASS），零工具执行**——意图未路由到生产链。

## 二、根因（两层）

`is_confirmed_production_action` 白名单（agent_production.rs L239）仅含 **11 意图**；TS `CONFIRMED_PRODUCTION_INTENTS`（shared/confirmed-production.ts）为 **16 个**。差集恰为四件：`fanfic_init` / `continuation_import` / `spinoff_create` / `style_imitation`。

且 `run_confirmed_production_locked` 分派器**无四者分支**——即便放行白名单也会落空。即：这是 231 号备案「四件走 propose→confirm」的**完整功能面未移植**，301 号 schema 补齐只是第一层（LLM 能填字段），路由与分派是第二、三层。

## 三、影响面与替代路径

| 路径 | 状态 |
|---|---|
| UI 专用页（同人/续写/番外/仿写 → HTTP `/fanfic/init` 等四端点） | ✓ **已移植且走查/e2e 验证过**（59/279/291 号）——主用户路径可用 |
| 聊天 propose→confirm→生产链（四件） | ✗ 白名单+分派器双缺——对话式创建不可用 |

非数据/正确性缺陷；属「对话式入口缺失」，与 232 号 film authoring 评估同类形态。

## 四、立项评估（如移植）

- 白名单 +4、分派器 +4 分支（各挂既有内部链：`init_fanfic_book` / `import_chapters_chain`(resume 语义) / `init_spinoff_book` / `init_imitation_book`——内部链均已移植，缺的是编排接线：任务标签/SSE 事件/单任务闸门/后台 spawn 语义对齐 TS `runConfirmedProduction` 对应分支）；
- 估 300–500 行 + 每分支一个 e2e（mock 沿用 292 号生产链常量）；
- 建议单批完成（四分支同构，一次成型避免半通状态）。

**本批决策：立项不动工**——先归档评估，待并行会话三件落库、推送核验后作为下一个完整批次实施（避免与本会话已收口的门禁状态交叉）。

## 五、验证

生产引擎实测（缺口实证）+ 双端源码走读；零代码改动。301 号 schema/校验层修复保留（TS parity 正确且为后续移植的必要地基）。

## 六、遗留

两项默认值、ja A/B、历史瘦身待用户决策；并行会话三件在途未落库。
