# 321 号：翻译上传落地逻辑收敛——删除重复实现，对齐 TS 逐字（321 号遗留清偿）

- 日期：2026-09-11
- 分支：develop
- 关联：273 号（upload_common 共享件）、308 号（translation 解析收敛——本批为落地逻辑收敛的完成）
- 编号衔接：查 20260911 目录最大号 320，顺延 321
- 推送核验：origin/develop = 498253fa（300 号）未动；本地领先 301–321 共 21 提交待推

## 一、内容

`upload_translation` 的落盘逻辑收敛到 `upload_common::store_project_upload`（TS `storeProjectUpload` 逐字），删除本地重复的文件名清洗/落盘实现（约 90 行）。收敛后**顺带修正两处残余微差**：

1. **缺 dataUrl 文案**：`Translation upload is missing dataUrl` → TS 逐字 `Upload is missing dataUrl`（错误码 `INVALID_TRANSLATION_UPLOAD` 不变）；
2. **文件名清洗的连续段折叠**：本地实现逐字符替换，TS 正则 `[^…]+` 将连续非法字符折叠为**单个** `_`——共享实现对齐（如 `a///b` 现为 `a_b` 而非 `a___b`）。

## 二、验证

| 门禁 | 结果 |
|---|---|
| translations70 e2e | ✓ 3/3（上传/创建/详情/运行/导出链） |
| 全量 cargo test（INKOS_DUEL=1） | ✓ 1631 通过 0 失败 |
| clippy --all-targets | ✓ 0 警告（含删除后的未用导入清理） |

## 三、遗留

301–321 共 21 提交待推送；并行会话三件第四十七轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
