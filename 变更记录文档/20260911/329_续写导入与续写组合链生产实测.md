# 329 号：continuation_import → write_next 组合链生产实测——resumeFrom 语义矩阵落定

- 日期：2026-09-12
- 分支：develop
- 关联：308 号（resumeFrom 拒绝语义——本批为其正向路径生产实证）、303 号（四域执行器）、328 号（组合链方法论——本批延续）
- 编号衔接：查 20260911 目录最大号 328，顺延 329
- 推送核验：**origin/develop = 2c5b2b44（327 号）——用户已推送 301–327 共 27 提交**；本地仅剩 328 号 1 提交待推

## 一、生产引擎组合链实测（release bin + 入库 walkthrough-mock，全部通过）

工作区 /tmp/inkos-329；源文件走 `.inkos/uploads/{session}/` 附件真实落点（93 号形态）。

| 步骤 | 结果 |
|---|---|
| **A. continuation_import**（新书，两章源，意图 button 路径） | ✓ 「已导入 2 章到《武动乾坤续篇》」；`nextChapter: 3`；章节 0001/0002 status=`imported`；地基（outline/roles/rules/hooks）+ 真相文件（current_state/particle_ledger/pending_hooks）由导入链铺好 |
| **B. write_next**（对导入书写下一章） | ✓ **第 3 章《风起》无缝衔接**（编号自导入索引续算，非从 1 重开）；status=`ready-for-review`——比 328 号新书笔更好：导入链铺好的真相文件使状态校验有据可依（对照 328 号 spinoff 新书 state-degraded） |
| **C. 边界：有章无 resumeFrom 再导入** | ✓ 生产面精确拒绝「already has 3 chapter(s); resumeFrom is required.」（308 号语义活体） |
| **D. resumeFrom=3 + 两章源** | ✓ 「已导入 0 章」——**正确语义非缺陷**：resumeFrom=N 表示「源前 N-1 章已在书中，从源第 N 章起导入」；TS `for (i = startFrom - 1; ...)`（runner.ts:3164）与 Rust `skip(start_from - 1)` 逐字同构，读码定性后归档 |
| **E. resumeFrom=3 + 三章源**（修正探针） | ✓ 「已导入 1 章」：源第 3 章「血引启符」**同号替换**书内第 3 章（风起→血引启符，44 字），第 1–2 章原样保留——resume 语义正向闭环 |

## 二、resumeFrom 语义矩阵（生产面落定，双端一致）

| 场景 | 行为 |
|---|---|
| 新书（无 bookId） | 全量重建：地基 + 真相重置 + 空索引 + 逐章回放 |
| 已有书，未给 resumeFrom，已有章节 | **拒绝**（防静默追加破坏时间线，308 号） |
| 已有书，resumeFrom=N | 跳过源前 N-1 章；源第 N 章落到书内第 N 号位置；**同号替换**索引；跳过地基重建保留既有真相 |

## 三、结论与遗留

- 跨域接缝（导入链索引 → 续写编号续算；导入真相文件 → 续写状态校验）双方向均实证健康；本轮**零代码改动**（纯实测+归档）。
- 组合链方法论第二批：328 号抓到 parentBookId 缺口，329 号将 resumeFrom 的模糊语义在生产面钉死并归档——该语义此前 e2e 只测了拒绝分支，正向/替换/0 章三态从未有文档。
- 遗留：**328 号 1 提交待推送**（Fork 图形端）；并行会话三件在途未落库（保持未触碰）；两项默认值、ja A/B、历史瘦身待用户决策。
