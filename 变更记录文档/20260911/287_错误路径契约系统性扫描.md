# 287 号：错误路径契约系统性扫描——12 个可选资源读端点双端比对（radar 外全部对齐）

- 日期：2026-09-11
- 分支：develop
- 关联：281 号（arcs 形状偏差）、286 号（radar 空目录契约差——本批为同类系统性排查）、217 号（sessions 数据层）
- 编号衔接：新日期目录首篇，承接全仓最大号 286，顺延 287
- 推送核验：origin/develop = a1b34a33 未动；本地领先 274–287 共 14 提交待推

## 一、方法

281/286 两次都抓到同类缺陷——「UI catch 掩盖的错误路径契约差」。本批对**读取可能缺失资源**的 GET 端点做穷举式双端比对（TS handler 的 catch/missing 返回 vs Rust 的对应分支），覆盖状态码与响应形状两层。

## 二、比对结果（12 端点，radar 外全部 TS 忠实）

| 端点 | TS 缺失行为 | Rust | 判定 |
|---|---|---|---|
| `/sessions/:sessionId` | 404 `{error:"Session not found"}` 平铺 | 同 | ✓ |
| `/books/:id` | 404 `{"error":"Book \\"id\\" not found"}` | 同 | ✓ |
| `/books/:id/timeline` | 200 `{timeline:null}`（坏载荷同） | 同 | ✓ |
| `/books/:id/chapters/:num` | 404 `{error:"Chapter not found"}` | 同 | ✓ |
| `/books/:id/chapters/:num/versions/:versionId` | 任何错误 → 404 平铺 | 同（显式注释对齐） | ✓ |
| `/books/:id/truth/:file` | 白名单 400 → 200/缺失分支 | 同（53 号域已移植） | ✓ |
| `/project/files/*` | 404 纯文本（c.notFound） | 同 `(404,"404 Not Found")` | ✓ |
| `/project/artifacts/*` | 404 纯文本 | 同 | ✓ |
| `/play/runs/:world/:run` | 缺组件 → null 默认 200 | 同（normalize+默认） | ✓ |
| `/translations/:id` | 404 `{error:{code:"NOT_FOUND",message:"translation project not found for id"}}` | 同（LoadManifestError::NotFound → 同形状） | ✓ |
| `/skills/:skillId` | **TS 无此路由**（仅列表） | Rust 亦无 | ✓ |
| `/prompt-packs/:promptId` | **TS 仅 PUT/DELETE**（无单读） | Rust 同为 PUT/DELETE | ✓ |
| ~~`/radar/history`~~ | 200 `{items:[]}` | ~~500~~ → **286 号已修** | ✓（已清偿） |

## 三、结论

「UI catch 掩盖的错误路径契约差」这一缺陷类在**读面已穷举清零**——radar/history 是唯一离群值（286 号修复）。剩余未比对的错误路径集中在**写端点与 LLM 编排端点**（错误形态多为主链错误透传，281 号 analysis 属读面已清），如后续再有同类缺陷浮现，优先怀疑写面非 2xx 分支。

## 四、验证

纯静态比对批，零代码改动（radar 修复属 286 号）。全仓门禁状态：engine-rs 1625 绿（286 号）+ clippy 0；TS 侧 278 号全绿后无改动。

## 五、遗留

274–287 共 14 提交待推送；并行会话三件第十六轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
