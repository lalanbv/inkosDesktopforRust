# 289 号：书籍导出真实用户路径走查——UI→export-save→盘上产物全链通过

- 日期：2026-09-11
- 分支：develop
- 关联：214 号（EPUB 结构验证）、267 号（export HTTP 冒烟——本批补 UI→盘上产物闭环）、242 号（工作台走查——本批补导出按钮）
- 编号衔接：查 20260911 目录最大号 288，顺延 289
- 推送核验：origin/develop = a1b34a33 未动；本地领先 274–289 共 16 提交待推

## 一、走查面与结果（全部通过，无产品缺陷）

| 面 | 结果 |
|---|---|
| 书籍工作台渲染 | 带章节 fixture 打开 `#/book/demo`：章节列表「✓ 01 风起云涌 66」（状态徽章/字数）正确 |
| **导出 UI 全链** | 工具栏格式选择（TXT/MD/EPUB）+ approvedOnly 勾选 + 导出按钮 → `POST /books/:id/export-save` → **盘上 `books/demo/demo.txt` 落盘，内容完整**（书名 + 章节标题 + 正文全文，128 字节逐字正确） |
| API 直连复核 | `export-save {format:txt, approvedOnly:true}` → 200 `{chapters:1, path, ok:true}`——approvedOnly 与 approved 章节过滤链验证 |
| 附属观察 | ①`BookDetail.tsx` L528 `exportHref` 为无渲染死代码（真实流走 export-save 落盘而非浏览器下载）——纯清理候选，不涉行为；②「导出全书」为聊天指令（撞模型守卫属预期，与 276/280 同一守卫） |

## 二、验证性质与遗留

纯走查批，零代码改动。书籍工作台 UI 面至此闭环（工作台/深链 242 号 + 导出本批）。遗留：274–289 共 16 提交待推送；并行会话三件第十八轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
