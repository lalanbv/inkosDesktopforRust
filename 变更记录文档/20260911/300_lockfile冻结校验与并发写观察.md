# 300 号：lockfile 冻结一致性校验+并发双发写下一章行为观察

- 日期：2026-09-11
- 分支：develop
- 关联：278 号（依赖升级——本批验证其 lockfile 冻结一致性）、220 号（会话并发语义）、218 号（BOOK_BUSY 分类面）
- 编号衔接：查 20260911 目录最大号 299，顺延 300
- 推送核验：origin/develop = a1b34a33 未动；本地领先 274–300 共 27 提交待推

## 一、lockfile 冻结一致性（278 号依赖升级的新克隆/CI 对等性）

`pnpm install --frozen-lockfile --registry=官方源` → **exit 0，Already up to date**——278 号升级后的 pnpm-lock.yaml 与三包 package.json 完全一致，全新克隆安装可复现，无漂移。

## 二、并发双发写下一章行为观察（生产 bin + 意图 mock）

同书并发双发 `POST /books/b1/write-next`：

1. **两请求均 200** `{"status":"writing","bookId":"b1"}`——该端点为**异步受理**设计（后台任务执行），与 TS 服务端语义同构；防重复提交由 UI 按钮禁用承担（289/292 号走查所见），非 409 拒绝。
2. 两链并行执行后**章号安全顺序分配**：0001_风起、0002_风起：林动 双章落盘，index 无冲突——结算期分配章号的并发安全性实证。
3. 两章 `audit-failed`（「审稿输出格式异常，无法解析为 JSON」）：**本批 mock 缺陷而非产品缺陷**——291 轮重生成 mock 时遗漏审稿分支（生产审计要求 JSON 裁决，mock 误用 lib 测试的 `PASS\n95` 文本格式）；审计门正确拦截坏格式并落 status，恰为审计链工作的正向实证。289/292 号单发走查中同 mock 带审稿分支时为 `ready-for-review`，可交叉印证。

## 三、环境观察（非缺陷）

 Compound shell 的 `wait` 会等所有后台任务（含常驻 server）→ 前台 Bash 挂起超时；`pgrep -f "a\|b"` 的转义交替在部分实现下不匹配——均属工具链用法，已用 TaskStop + 精确模式收尾。

## 四、遗留

274–300 共 27 提交待推送；并行会话三件第二十九轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
