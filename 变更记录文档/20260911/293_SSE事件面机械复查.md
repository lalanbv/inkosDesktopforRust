# 293 号：SSE 事件面机械复查——66 事件全对齐+engine:shutdown 合理差异

- 日期：2026-09-11
- 分支：develop
- 关联：222 号（上次全量事件求交——本批为 273 号 canon-file 广播等新增点后的复查）、273 号（canon-file import:* 变体新增方）
- 编号衔接：查 20260911 目录最大号 292，顺延 293
- 推送核验：origin/develop = a1b34a33 未动；本地领先 274–293 共 20 提交待推

## 一、方法与结果

对引擎全部 `.broadcast("…")` 调用点与 TS server.ts 的 `broadcast/broadcastEvent/emitEvent` 调用点做全量字符串求交（Python 机械提取，避免 222 号教训中 grep 逐行漏报）：

| 端 | 事件数 |
|---|---|
| engine-rs | 67 |
| TS server | 66 |
| **仅 Rust 有** | `engine:shutdown` |
| **仅 TS 有** | **无** |

**判定：全对齐。** `engine:shutdown` 为 Rust bin 生命周期事件（SIGINT/SIGTERM → 广播告知前端 → flush 窗口，bin L218）——TS 进程退出即关停、无此需求，属合理的单端增强而非缺口。222 号时 TS=66 的基数未变，期间新增的 canon-file `import:start/complete/error` 变体（273 号）两端同增。

## 二、验证

纯机械比对批，零代码改动。`engine:shutdown` 发送方实证于 bin L218（signal 处理路径）。

## 三、遗留

274–293 共 20 提交待推送；并行会话三件第二十二轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
