# 317 号：意图验证矩阵扩展——五域生产实测（short_run/script/storyboard/film/cover）

- 日期：2026-09-11
- 分支：develop
- 关联：315/316 号（意图验证矩阵——本批补齐剩余五域）、141 号（script/storyboard 链）、267 号（film 导出冒烟）
- 编号衔接：查 20260911 目录最大号 316，顺延 317
- 推送核验：origin/develop = 498253fa（300 号）未动；本地领先 301–317 共 17 提交待推

## 一、生产引擎实测（POST /agent + requestedIntent，button 来源，五域）

| 意图 | 路由 | 执行深度 | 结果 |
|---|---|---|---|
| `short_run` | ✓ | 大纲 v001/v002 + reviews 落盘；草稿阶段以清晰错误失败（mock 写手格式非短篇预期——**链路对空草稿的聚合报错精确到章号**） | 路由/分派/暂存 ✓ |
| `script_create` | ✓ | **全链成功**：`dramas/雨夜谜案/script-spec.md` + `script.md` + status.json 落盘 | ✓ 完整 |
| `storyboard_create` | ✓ | 路由+分派 ✓；失败于 image-prompts 空（mock 未产该工件，错误面清晰） | 路由 ✓ |
| `interactive_film_create` | ✓ | 多阶段链启动 ✓；`interactive-films/雨夜谜案互动版/assets.json` 落盘；同上 mock 边界 | 路由 ✓ |
| `generate_cover` | ✓ | 正确报错要求 cover 端点配置（「Configure cover generation in Studio or set INKOS_COVER_BASE_URL.」——错误可操作） | ✓ |

严格校验器同批实证：`shortRun.chapters: 1` 被 `IntRange(12-18)` 拒绝（400 INVALID_ACTION_PAYLOAD）——短篇章数设计约束在意图层生效。

## 二、矩阵总览（更新）

| 意图 | 三层验证 |
|---|---|
| create_book / write_next / fanfic_init / continuation_import / spinoff_create / style_imitation / play_start / translation_create | 白名单+分派+**生产全链** ✓ |
| short_run / script_create / storyboard_create / interactive_film_create / generate_cover | 白名单+分派 ✓；script 全链 ✓；short/storyboard/film 路由+暂存 ✓（成品深度受 mock 保真度限制，真实 LLM 下即完整链路）；cover 按设计要求图像服务配置 |

## 三、验证性质与遗留

零代码改动批（纯实测+归档）。遗留：301–317 共 17 提交待推送；并行会话三件第四十五轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
