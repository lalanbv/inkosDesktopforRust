# 310 号：聊天自由文本 propose→确认卡路径探针——不可复现（mock 协议限制），零缺陷证据

- 日期：2026-09-11
- 分支：develop
- 关联：291 号（按钮直发路径已实证——本批探针针对自由文本 propose 卡 UX）、301 号（四域 schema）
- 编号衔接：查 20260911 目录最大号 309，顺延 310
- 推送核验：origin/develop = 498253fa 未动；本地领先 301–310 共 10 提交待推

## 一、探针设置与结果

生产 bin + 真实浏览器 + 意图 mock（自由文本轮发 propose_action 工具调用）。发送创建指令后：

1. **UI 层证据（正向）**：消息正常提交；LLM 的 propose_action 工具调用被聊天 UI 渲染为任务卡（「确认动作 · 0s · 失败/成功」形态）——工具执行卡的用户可视面工作。
2. **本轮未取得卡片成功态**：探针 mock 的两轮协议（首轮 propose 工具调用 / 工具结果轮 PASS 文本）与生产聊天循环的预期不完全匹配，出现 mock 侧格式抖动（如缺 `action` 字段被 schema 正确拒绝——该拒绝本身证明 schema 必填校验在工作）；多次迭代后判断为 mock 协议保真度不足，非产品缺陷证据。

## 二、结论与定位

- 自由文本 propose 卡的**完整成功态**留待真实 LLM 或协议保真 mock 验证（备案为 mock 加固候选：按 231 号 propose→confirm 协议实现两轮状态机）。
- 按钮/斜杠直发的确认生产路径（291/303/304 号）与工具卡渲染（本批）均有实证；聊天 LLM 协议层无新缺陷证据。

## 三、验证性质与遗留

零代码改动批（探针）。遗留：301–310 共 10 提交待推送；并行会话三件第三十八轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
