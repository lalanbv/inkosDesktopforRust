# 324 号：cargo doc 文档链接卫生修复——44 警告清零（doc build 首次零警告）

- 日期：2026-09-11
- 分支：develop
- 关联：312 号（走查——本批延伸到 doc build 卫生面）
- 编号衔接：查 20260911 目录最大号 323，顺延 324
- 推送核验：origin/develop = 498253fa（300 号）未动；本地领先 301–324 共 24 提交待推

## 一、发现与修复

`cargo doc --no-deps` 首次专项检查发现 **44 个文档警告**（从未清过）：全为文档注释内的 rustdoc 链接问题——未解析链接（`[0]`/`[DONE]`/`[::1]` 等方括号文本被当作 intra-doc link）、私有条目链接（`[parse_sections_with_repair]`）、冗余显式链接目标、未闭合 HTML 标签（`<Mutex<bool>>`/`<this>`）。

修复（22 个文件，纯文档注释，零运行路径影响）：方括号文本加反引号转代码、显式路径改行文、HTML 角括号转义/反引号化。

## 二、门禁

| 门禁 | 结果 |
|---|---|
| `cargo doc --no-deps` | ✓ **0 警告** |
| 全量 cargo test（INKOS_DUEL=1） | ✓ 1631 通过 0 失败（strangler_duel 一次 TS sidecar 启动超时偶发，重跑 10/10 通过） |
| clippy --all-targets | ✓ 0 警告 |

## 三、遗留

301–324 共 24 提交待推送；并行会话三件第四十六轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
