# 312 号：聊天驱动建书全链 UI 实证——propose→确认→建书→工作台完整闭环

- 日期：2026-09-11
- 分支：develop
- 关联：291 号（按钮直发路径）、301/303 号（schema/路由层）、310/311 号（探针与 mock 加固——本批为最终闭环）
- 编号衔接：查 20260911 目录最大号 311，顺延 312
- 推送核验：origin/develop = 498253fa（300 号）未动；本地领先 301–312 共 12 提交待推

## 一、全链实测（生产 bin + 真实浏览器 + 两轮状态机 mock，全部通过）

| 步骤 | 结果 |
|---|---|
| 模型配置 | ✓ LM Studio 测试连接 →「连接成功，1 个模型，已自动匹配」→ 保存持久化（服务列表显示「已连接」） |
| 自由文本创建指令 | ✓ 发送「创建一本玄幻小说《镜花水月》……」 |
| **propose_action 卡** | ✓ 「propose_action 0s 已完成」+ 卡片渲染（创建长篇书籍 + instruction 摘要） |
| **确认 → 生产链** | ✓ 「继续执行」→ 建书任务 → **「建书 0s 已完成」**：「Book 镜花水月 initialised successfully. Foundation files are ready.」 |
| **工作台落位** | ✓ 自动跳转 `#/book/镜花水月`：章节区（暂无）、角色（林动·主要）、核心文件（故事基石/卷纲规划/当前状态/伏笔池/情感弧线/世界观——「少年于微末中抬起头。」即 mock 地基正文） |

**310 号备案的「自由文本 propose 卡成功态」就此清偿**——此前失败的根因确认为 mock 缺 `action` 必填字段（schema 正确拒绝，校验链在工作）；两轮状态机 mock（自由文本轮 → propose 工具调用 / 工具结果轮 → 确认卡应答）补齐后全链一次通过。

## 二、结论

聊天驱动建书（**对话式创建主路径**）：自由文本 → LLM propose → 确认卡 → 用户确认 → 建书生产链（架构师地基 + 审核环 + 风格向导）→ 工作台落位——七层全部真实 UI/生产引擎实证。

## 三、遗留

301–312 共 12 提交待推送；并行会话三件第三十九轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
