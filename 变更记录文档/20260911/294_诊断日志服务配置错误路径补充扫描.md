# 294 号：doctor/日志/服务配置错误路径补充扫描——全对齐

- 日期：2026-09-11
- 分支：develop
- 关联：287/288 号（读写错误路径扫描主批次——本批为其补充覆盖）、195 号（doctor 预警）、210/211 号（日志轮转）
- 编号衔接：查 20260911 目录最大号 293，顺延 294
- 推送核验：origin/develop = a1b34a33 未动；本地领先 274–294 共 21 提交待推

## 一、比对结果（7 分支全部 TS 忠实）

| 端点/分支 | TS 行为 | Rust | 判定 |
|---|---|---|---|
| `GET /logs` 文件缺失 | 200 `{entries:[]}` | 同 | ✓ |
| `GET /logs` 单行非 JSON | 降级 `{message: line}` | 同 | ✓ |
| `GET /services/:service/secret` secrets 缺失 | `apiKey: ""` 默认 | 同（`unwrap_or_default`） | ✓ |
| `POST /services/:service/test` 未知服务商/无 baseUrl | 400 `{ok:false, error:双语}` | 同（语言感知 pick 语义） | ✓ |
| `POST /services/config/import-env` 缺 env | 400 双语提示（含 INKOS_LLM_API_KEY 指引） | 同 | ✓ |
| `GET /doctor` 各检查项缺失 | existsSync 布尔降级 + issues | 同（195/242 号已验） | ✓ |
| `GET /services/config` 配置缺失 | catch → 空配置对象 | 同 | ✓ |

## 二、结论

287/288 主扫描 + 本批补充覆盖后，**错误路径契约双端比对全部收口**（读面 12 端点、写面 9 端点、诊断/配置面 7 分支；唯二缺陷 radar 空目录与 arcs 形状均已修复）。错误路径质量面正式收口，后续转向新增端点的制度化断言。

## 三、验证

纯静态比对批，零代码改动。门禁状态不变（engine-rs 1625 绿 + clippy 0；TS 侧 278 号后无改动）。

## 四、遗留

274–294 共 21 提交待推送；并行会话三件第二十三轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
