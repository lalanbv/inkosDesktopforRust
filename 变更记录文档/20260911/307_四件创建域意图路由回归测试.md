# 307 号：四件创建域意图路由自动化回归测试——sub303 e2e 模块（303 号验证面制度化）

- 日期：2026-09-11
- 分支：develop
- 关联：303 号（实施——本批把手工实测沉淀为自动化回归）、301 号（schema 层）
- 编号衔接：查 20260911 目录最大号 306，顺延 307
- 推送核验：origin/develop = 498253fa（300 号）未动；本地领先 301–307 共 7 提交待推

## 一、新增 `sub303_creation_domains_e2e`（tests/e2e_write_next_contract.rs）

四条测试（分派式 mock：同人架构师/总架构师 → 5 段地基、素材分析师 → canon 文档、资深编辑 → PASS 审校、创作总编/作家/写手/审稿 → 既有常量）：

1. `fanfic_intent_routes_into_production_chain`——FanficInit 意图 → Ok + 新书 `story/fanfic_canon.md`（含同人正典结构）落盘；
2. `style_imitation_intent_creates_book_with_style_guide`——StyleImitation 意图 → Ok + `story/style_guide.md` 落盘；
3. `spinoff_intent_requires_parent_book`——缺 parentBookId → Err（中文错误面）；
4. `continuation_import_intent_new_book_and_chapters`——新书 + 2 章源文件 → index.json 双章 + 0001_北境.md 落盘。

另有 301 号 propose 层单测（7/7）已覆盖 schema 12 域宣传与四域必填断言。

## 二、测试基建发现（已修复）

| 问题 | 处置 |
|---|---|
| 并行测试共用 session_id → 单任务闸门（PRODUCTION_TASK_ALREADY_RUNNING）误撞 | 各测试独立 session_id |
| 测试根缺题材档案 | 补 xuanhuan.md + other.md（fallback 链需要） |
| `ProductionError`/`ProductionOutcome` 无 Debug | 断言改用 `err.message` / match 分支（不加派生） |

## 三、门禁

全量 `cargo test`（INKOS_DUEL=1）**1630 通过 0 失败**（+4）；clippy --all-targets 0 警告。

## 四、遗留

301–307 共 7 提交待推送；并行会话三件第三十五轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
