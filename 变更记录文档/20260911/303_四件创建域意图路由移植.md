# 303 号：302 号立项实施——四件创建域聊天意图路由移植完成

- 日期：2026-09-11
- 分支：develop
- 关联：302 号（立项评估——本批实施）、301 号（schema 层——本批为路由层闭环）、59 号（四条内部链已移植的前提）
- 编号衔接：查 20260911 目录最大号 302，顺延 303
- 推送核验：origin/develop = 498253fa（300 号）未动；本地领先 301–303 共 3 提交待推

## 一、实施内容（agent_production.rs + fanfic_routes.rs）

1. **白名单 +4**：`is_confirmed_production_action` 纳入 `FanficInit / ContinuationImport / SpinoffCreate / StyleImitation`（button/slash 来源；free-text 仍走聊天循环）——对齐 TS `CONFIRMED_PRODUCTION_INTENTS` 16 项。
2. **分派器 params 装配 +4 分支**：逐字段镜像 TS 确认卡生成（fanfic：title 必填 + sourceText/sourcePath 至少其一；continuation：sourcePath 必填 + bookId/title 二选一；spinoff：title + parentBookId（兜底 request.book_id）；imitation：title + storyIdea + referenceText/referencePath 至少其一）。
3. **tool_name / TOOL_LABELS +4**：`fanfic_init`（同人创建）/ `continuation_import`（续写导入）/ `spinoff_create`（番外创建）/ `style_imitation`（仿写项目）。
4. **四个执行器**（挂接既有内部链，零新逻辑）：
   - `execute_fanfic_init` → `init_fanfic_book`（BookConfig 构造同 /fanfic/init 处理器：derive id / Outlining / 默认 100 章 3000 字 / mode 解析）；
   - `execute_continuation_import` → 已有书 resume 起点 = 现有章数+1（或 payload.resumeFrom）；新书 → 建配置 + 全量导入（`import_chapters_chain_with_resume`，91 号语义）；
   - `execute_spinoff_create` → `init_spinoff_book`（parent 回退字段 + 字段回退 parent 配置 + 已存在冲突拒绝）；
   - `execute_style_imitation` → `init_book`（storyIdea 外部指令）+ `generate_style_guide_for_book` 强制风格向导（对齐 imitation_init 逐字）。
5. **可见性**：`init_fanfic_book` / `init_spinoff_book` / `parse_fanfic_mode` → `pub(crate)`。

## 二、验证

| 门禁 | 结果 |
|---|---|
| propose_action 单测 | ✓ 7/7（301 号基础 + 沿用） |
| 白名单新单测（四意图 × 三来源） | ✓ |
| 全量 cargo test（INKOS_DUEL=1） | ✓ **1626 通过 0 失败** |
| clippy --all-targets | ✓ 0 警告 |
| **生产 bin 实测**：`POST /agent` 携带 `requestedIntent=fanfic_init`（button） | ✓ **路由进生产链**：`books/剑影余音/` 完整落地（book.json + chapters/index + story/fanfic_canon.md 正典 + 八件地基 + outline/ + roles/主要角色/ + snapshots/0），响应「同人创作完成：《剑影余音》」 |

调试过程一次性修正：spinoff 签名漏 lang 参数、format 混用占位、`read_project_source` 缺 async、测试锚点顶掉原 `#[test]`（均已收口，clippy 0 佐证）。

## 三、遗留

301–303 共 3 提交待推送；并行会话三件第三十一轮在途未落库；两项默认值、ja A/B、历史瘦身待用户决策。
