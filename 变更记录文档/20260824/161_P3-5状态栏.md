# 161 号变更:P3-5 状态栏(P3 期全部工作包收口)

- 日期:2026-08-24
- 模块:packages/studio
- 类型:工作台深化功能(151 号 P3 末个工作包)
- 关联:157~160 号(P3-1..4)

## 一、变更内容

- **M `hooks/use-sse.ts`**:增加 `reconnect()` 手动重连通道(nonce 翻转重建 EventSource;EventSource 原生自动重连保留,手动通道供状态栏黄点点击)
- **N `components/StatusBar.tsx`**(h-6 常驻底条,**全部真实数据源**):
  - 左:当前书·章节 + 字数——书系页面显示书名(同源 /books);章节页 `书名 · 第 N 章 约 X 字`(章节内容前端统计,去空白字符数);非书页显示应用名
  - 中:**SSE 连接点**(绿=已连/黄=断连+呼吸动画,点击 reconnect)+ **daemon 运行点**(绿/灰,复用 /daemon 数据源)
  - 右:**生成中 spinner**(chat store isActiveSessionStreaming)+「查看」直达底部面板任务流
  - 深浅主题经语义色 token(emerald/amber/muted-foreground)天然可辨
- **M `App.tsx`**:主列底部挂 StatusBar;/daemon 数据源接入;`sse.reconnect` 透传

## 二、验证链

| 门禁 | 结果 |
| --- | --- |
| studio vitest | 80 文件 / 717 测试全绿 |
| typecheck(client + server) | 0 错误 |
| e2e 全量 | **30/30**(新增状态栏用例:书页书名/连接点/daemon 点可见;⌘P 章节直达后状态栏显示「书名 · 第 N 章 约 X 字」) |

## 三、P3 期收口总览(08-24 全部完成,原排期 09-07~09-11)

| 工作包 | 记录 | 提交 |
| --- | --- | --- |
| P3-1 活动栏与四区重组 | 157 | 26e555c7 |
| P3-2 侧面板增强 | 158 | ac5b625c |
| P3-3 标签页多任务 | 159 | 25f62803 |
| P3-4 右侧 dock 与底部面板 | 160 | 34610420 |
| P3-5 状态栏 | 161(本文) | — |

P3 收尾既定项中「全流程走查/打包 v0.2.0-beta.1」按 150 号日程留 08-28 节点与 alpha 打包同批执行;全量门禁(单测/typecheck/e2e 30 用例)本日已复跑通过。

## 四、调试备案

- 快速打开条目的 e2e 断言:cmdk option 宜用 `getByRole("option", {name})`,文本定位器在动画/异步到达窗口内可能"快照可见但定位不中"(role 走可访问性树更稳)
