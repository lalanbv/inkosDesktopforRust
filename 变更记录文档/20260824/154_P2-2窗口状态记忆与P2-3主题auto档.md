# 154 号变更:P2-2 窗口状态记忆 + P2-3 主题 auto 档

- 日期:2026-08-24
- 模块:src-tauri(窗口状态)/ packages/studio(主题体系)
- 类型:桌面融合层功能(151 号 P2 第 2/3 工作包)
- 关联:153 号(P2-1 标题栏融合,同周批次)

## 一、P2-2 窗口状态记忆(tauri-plugin-window-state,方案①)

- M `src-tauri/Cargo.toml`:`tauri-plugin-window-state = "2"`
- M `src-tauri/src/main.rs`:Builder 注册 `.plugin(tauri_plugin_window_state::Builder::default().build())`——main 窗口退出自动保存 bounds+maximized 至应用配置目录,启动恢复;多显示器拔插的越界矫理由插件内建(恢复前校验显示器可见区域)
- M `src-tauri/capabilities/main.json`:权限加 `window-state:default`(插件前端 API 通道;当前仅 Rust 侧使用,预授权避免后续 webview 调用被 ACL 拒绝——146/153 号同类经验)
- 门禁:`cargo check` 通过(拉取并编译新 crate);`cargo test` 基线见 153 号
- 真机验收(调整尺寸/位置/最大化→重启还原)与 153 号同列人工走查项(会话环境无辅助功能授权)

## 二、P2-3 主题 auto 档(三态)

- M `packages/studio/src/hooks/use-theme.ts`:
  - `Theme`(light/dark,生效值)**不变**,下游 6 处 type 导入零改动;新增 `ThemeMode = "light" | "dark" | "auto"`(存储三态)
  - 存储 key 不变(`inkos:studio:theme`),值域扩 "auto";`readStoredThemeMode` 三态读取
  - `resolveEffectiveTheme({mode, systemPrefersDark, hour})` 纯函数:显式档直出 / auto 跟随 `matchMedia("(prefers-color-scheme: dark)")` / **null(从未选择)保留旧版按时段默认**(6/18 点翻转,60s 轮询仅在 null 档保留,老用户零体感突变——151 号既定)
  - auto 档 = matchMedia change 监听即时跟随,替代轮询
  - `cycleThemeMode(mode, current)` 纯函数:light→dark→auto→light;null 首击按当前生效主题取反(经典开关体感)
- M `App.tsx`:主题按钮三态循环,图标 Sun/Moon/**Monitor(auto)**,aria-label 随档位;commandCtx 改传 `setThemeMode`
- M `lib/commands.ts`:`CommandContext.setTheme/toggleTheme` → `setThemeMode(mode)`;主题命令三条(light/dark/**auto 跟随系统**),总量维持 37
- M `components/CommandPalette.tsx`:图标映射补 `monitor`
- M 测试:`use-theme.test.ts` 重写(时段函数保留;三态读取/解析矩阵/循环转移,10 用例);`commands.test.ts` ctx 同步 + themeAuto 断言

## 三、验证链

| 门禁 | 结果 |
| --- | --- |
| studio vitest | 70 文件 / 650 测试全绿(+4) |
| typecheck(client + server) | 0 错误 |
| e2e(command-palette + loading-skeletons) | 6/6 通过 |
| `cargo check`(src-tauri) | 通过(window-state 插件编译接入) |

## 四、备注

- 主题存储向后兼容:旧值 light/dark 原样识别;新值 auto 对旧版本未知但仅被读为 null(旧版 `readStoredTheme` 判非法回落默认),降级安全
- P2-2 与 P2-1 的窗口尺寸来源链:首次启动用 tauri.conf(1280×800),此后 window-state 优先生效——两配置文件(main/macos 覆写)的窗口字段仅作为首启与无状态文件时的回退
