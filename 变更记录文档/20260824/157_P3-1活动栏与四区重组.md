# 157 号变更:P3-1 活动栏与四区重组(nav 统一 + 双轨布局)

- 日期:2026-08-24
- 模块:packages/studio
- 类型:工作台深化功能(151 号 P3 首个工作包)
- 关联:150 号(总纲)、152 号(P1 空态/骨架基座)、156 号(P2 收官)

## 一、lib 层(纯函数,全量单测)

- **N `lib/nav.ts`**:`Nav` 接口(25 方法超集)+ `createNav(navigate)` 工厂——**消除 App.tsx 与 Sidebar.tsx 的 Nav 重复声明**(151 号自 P1 起挂账的债务);App 内 25 行内联对象收敛为 `createNav(setRouteTracked)` 一行,导航全部写最近访问
- **N `lib/nav-sections.ts`**:四区映射表单一事实源——
  - `NavSectionId = create(创作)/tools(工具)/manage(管理)/film(互动影视)`,每区含 lucide 图标名/排序/zh-en 标题/page 集合
  - 24 个路由 page **全覆盖且互斥**(测试断言无孤儿、无幽灵页);`sectionForRoute/sectionForPage` 纯函数,未知 page 兜底创作区(活动栏选中态永不落空)
  - 归属要点:题材(genres)归工具区(V1 侧栏在「系统」组,P3-1 起按映射迁移);play/film/flow/film-author/film-studio 归互动影视区

## 二、双轨布局(V2 默认关闭,旧轨保留到 P4 走查)

- **M `store/preferences/`**:`navLayoutV2`(仅显式存 "true" 启用,默认 V1 整栏)+ `activeNavSection`(用户手动切换持久化;null=跟随路由)——沿用 `inkos:studio:` 存储约定
- **N `components/ActivityBar.tsx`**:48px(w-12)窄列,四区图标按钮(选中态 aria-current + 高亮)+ 底部设置直达;标题 tooltip 双语
- **M `components/Sidebar.tsx`**:新增 `zone?: NavSectionId` prop——传入时仅渲染该区内容(`showZone` 守卫包裹六个区块);**V1(无 zone)行为逐字节不变**;题材项 zone=tools 时并入工具组、zone=manage 时隐藏;本文件 Nav 声明删除改 import
- **N `components/SidePanel.tsx`**:薄封装(Sidebar + zone)——双轨期同一组件两种形态,逻辑零重复,旧轨删除时一并收壳
- **M `App.tsx`**:布局分支 V2 = ActivityBar + SidePanel / V1 = Sidebar;`activeSection = 持久化选择 ?? sectionForRoute(route)`;nav 换 createNav 工厂
- **M `hooks/use-i18n.ts`**:补 `nav.activityBar` 键

## 三、验证链

| 门禁 | 结果 |
| --- | --- |
| studio vitest | 75 文件 / 684 测试全绿(+19:nav-sections 6 / preferences +6 / ActivityBar 3 / Sidebar zone 3 / nav 工厂经 App 回归) |
| typecheck(client + server) | 0 错误 |
| e2e 全量 | **24/24**(新增 nav-layout-v2:四区切换/按区渲染/题材迁移/设置直达/面包屑导航;V1 回归由存量 23 用例覆盖) |

## 四、验收对照(151 号 P3-1)

- ✅ 全部路由可达且归属正确(nav-sections.test 全 page 覆盖断言;e2e 工具区/管理区/影视区入口)
- ✅ 高频页 ≤2 次点击:活动栏(1)→ 书/会话/创建项(2);章节经书展开(2)或 ⌘P 快速打开(直达)
- ⏭ 首启导览气泡(4 条一次性)未做:属引导性打磨,归入 P4 精致化一并处理(避免双轨期新旧两套引导)
- 注:区标题暂未走 i18n key 而是内联 zh-en(与 commands.ts 同款约定,151 号原文的"i18n key"字段以 titleZh/titleEn 承载,已在代码注释备案)

## 五、回滚

双轨开关本身即回滚通道:localStorage 删 `inkos:studio:nav-layout-v2` 即回 V1;代码级回滚 = 还原 App 布局分支与 Sidebar zone 守卫(均为增量改动)。
