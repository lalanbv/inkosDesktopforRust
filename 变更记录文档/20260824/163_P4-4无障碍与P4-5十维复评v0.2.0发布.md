# 163 号变更:P4-4 无障碍收口 + P4-5 十维复评与 v0.2.0 发布(四期计划完成)

- 日期:2026-08-24
- 模块:全仓(studio / src-tauri / 发布物)
- 类型:精致化收口 + release(150 号四期计划的收官变更)
- 关联:152~162 号(P1~P4 前三包);145 号(v0.1.0 发布先例与流程)

## 一、P4-4 无障碍审计结论(对照 docs/i18n-a11y.md 清单)

- **图标钮 aria-label**:本轮新增的图标钮全部带标签/角色——主题钮(aria-label 随三态档位)、状态栏 SSE 重连/daemon 点(StatusDot aria-label)、dock 关闭/分隔条(role=separator)、通知铃铛(未读数入 label)、底部面板停止/跟随/最大化、TabStrip 关闭钮、拖拽分隔条(role=separator+aria-label);V1 存量侧栏折叠箭头(aria-label 展开/折叠书名)自 P1 前已有
- **骨架 aria-busy**:三型骨架均 `role="status" aria-busy`(152 号)
- **键盘可达**:e2e 全套 34 用例以键盘事件驱动(⌘K/P/B/J/⇧D/⇧F//、⌘1..9、Esc 链、Enter 选择、Tab 焦点在 cmdk 内建)——键盘-only 走查由 e2e 覆盖关键路径(面板导航/快速打开/标签切换/面板开合/专注进出)
- **reduced-motion**:全局 0.01ms 瞬时完成保留终态(162 号),stagger 不卡 opacity=0
- **e2e 补齐**:150/151 号点名的 command-palette/tabs/hotkeys/focus 均已有专测(155/159/162 号);axe 引入按"边际按需"未做(记录裁量)
- 残余(非 P0/P1):ChatPage 消息级打字机未做(162 号备案);`?` 单键速查未注册(mod+/ 已覆盖)

## 二、P4-5 十维复评(对照《参考案例对比分析.md》矩阵;证据=本轮变更)

| 维度 | 前 | 后 | 证据 |
| --- | --- | --- | --- |
| 导航收敛度 | 2 | **4.5** | 四区活动栏(nav-sections 24 page 全覆盖无孤儿)+ 按区面板 + 树过滤/拖宽(157/158) |
| 命令面板 | 1 | **4.5** | ⌘K 39 命令(18 导航+21 动作)+ ⌘P 内容层四类 + 最近/推荐/空态引导(152/155) |
| 快捷键体系 | 1 | **4.5** | HOTKEY_DEFS 单一注册表 18 组合 + ⌘/ 速查同源生成 + 输入框放行规则(155/162) |
| 加载反馈 | 2 | **4.5** | 启动壳/三型骨架(200ms 防闪)/控件 spinner 三级;e2e 断言无整屏 spinner(152) |
| 空态设计 | 2 | **4** | EmptyState 组件 + 书架/影游/会话空态带引导动作(152) |
| 多任务 | 2 | **4** | 标签页:VSCode 式预览语义/⌘数字/pin/持久化/深链(159);分屏未做(-1) |
| 状态可见性 | 3 | **4.5** | 状态栏(书·章·字数/SSE/daemon/生成)+ 底部任务流/日志 + 通知中心(160/161/162) |
| 主题与密度 | 3 | **4.5** | light/dark/auto 跟随系统 + 舒适/紧凑密度档 + 双主题资产保留(154/162) |
| 桌面原生融合 | 2 | **4** | macOS 融合标题栏/拖拽/交通灯/窗口记忆/菜单栏三入口同源(153/154/156);Win/Linux 冒烟待设备(-0.5) |
| 动效质感 | 3 | **4** | 动效 token 三档 + reduced-motion 全局尊重 + 既有呼吸/pulse 质感保留(162) |

**复评均值 ≈ 4.2 ≥ 4.0 目标 ✓**(改造前 2.4,标杆均值 4.4)。十二设计原则除 #8 工作区预设(四套面板布局一键切换,原计划未列入任何一期交付物)与 #4 的 Cmd+E 最近(由 ⌘P 内容层吸收)外全部落地。

## 三、v0.2.0 发布(本地脚本流程,对齐 149 号无 CI 决策与 145 号流程)

### 版本与产物

- `src-tauri/Cargo.toml` + `tauri.conf.json`:0.1.0 → **0.2.0**(engine 1.8.0 不变,studio/CLI/core 1.8.0 不变)
- `src-tauri/target/release/bundle/macos/inkosDesktop.app`(278MB dmg 口径):CFBundleExecutable=inkos-desktop ✓ 版本 0.2.0 ✓ ad-hoc 签名 ✓ **包内引擎 `node Resources/engine/dist/index.js --version` → 1.8.0**(自包含铁证,含全部 P1~P4 前端)✓
- `src-tauri/target/release/bundle/dmg/inkosDesktop_0.2.0_aarch64.dmg`(266MB)+ `.sha256`:hdiutil UDZO,CRC 校验 VALID ✓

### 构建链(全部可复现)

1. `./scripts/desktop-build-inkos.sh`(studio/cli/core 全量构建,UI 含 P1~P4)
2. `INKOS_ENGINE_PROD=1 ./scripts/desktop-package-engine.sh`(pnpm deploy 自包含,内置打包期冒烟 `--version` → 1.8.0 ✓)
3. `pnpm dlx @tauri-apps/cli@2 build --bundles app`(release 1m53s)
4. `hdiutil create -format UDZO` + `shasum -a 256` + `hdiutil verify`

### 冒烟

| 项 | 结果 |
| --- | --- |
| engine 独立 `--version` | 1.8.0 ✓ |
| .app Plist(executable/version) | inkos-desktop / 0.2.0 ✓ |
| .app 内引擎 | 1.8.0 ✓ |
| `cargo test --release --test e2e_startup -- --ignored` | **2/2 通过**(release 二进制真实启动) |
| dmg CRC + sha256 | VALID ✓ |
| 发布前全量门禁 | vitest 720 绿 / typecheck 双过 / e2e 34/34 / cargo test 过(162 号) |

### 待用户执行(外向动作不代推,145 号先例)

`git tag v0.2.0 && git push origin v0.2.0`(工作流已移除,Release 页上传 dmg/sha256 手动)。另有 153 号第三节的 macOS 真机走查清单与三平台冒烟(08-28 节点)。

## 四、四期计划总账

| 期 | 交付 | 记录 |
| --- | --- | --- |
| 规划 | 分析/方案/日程/两级分解 | 150/151 |
| P1 基础体验层(8 任务) | 骨架三级/启动壳/语言 Dialog/三页接入/⌘K MVP+动作层/顶栏三段化/空态 | 152 |
| P2 桌面融合层(7 包) | 融合标题栏/窗口记忆/主题 auto/快捷键分发/⌘P/速查/菜单栏 | 153~156 |
| P3 工作台深化(5 包) | 活动栏四区/侧栏增强/标签页/dock+底栏/状态栏 | 157~161 |
| P4 精致化(5 包) | 专注+打字机/密度+动效/通知+取消/a11y/复评+发布 | 162/163(本文) |

共 **25 个工作包、12 份变更记录、11 笔功能提交**,全程 vitest 594→720、e2e 0→34 用例随行。
