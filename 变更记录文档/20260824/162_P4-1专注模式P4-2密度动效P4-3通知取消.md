# 162 号变更:P4-1 专注模式 / P4-2 密度与动效规范 / P4-3 通知中心与任务取消

- 日期:2026-08-24
- 模块:packages/studio
- 类型:精致化收口(151 号 P4 前三个工作包)
- 关联:157~161 号(P3 全包);163 号起为 P4-4/5

## 〇、P4-3 前置:SSE cancel 契约预查结论(151 号标注的唯一后端风险点)

**取消生成全链路已存在,无需动任何后端**:
- 服务端 `POST /api/v1/sessions/:sessionId/abort?scope=all|chat`(server.ts):中止运行任务控制器 + `abortAgentSession` + `agent:aborted` SSE 广播
- 前端 chat store `abortSession(sessionId, scope)` 已就绪,ChatPage 停止钮已在用
- P4-3 的"取消"只剩 UI 面:底部面板任务流加停止钮(见下)。**发布周不动引擎的风险解除**。

## 一、P4-1 Focus Mode 与打字机模式

- `index.css`:`.focus-mode`(应用根类)——低对比环境变量覆盖(`--border/--muted/--secondary` 经 color-mix 半透明,双主题通用)+ 导航 chrome 隐藏(活动栏/侧栏/Sidebar/dock/标签条 display:none;**顶栏与状态栏保留**:窗口拖拽区与生成状态可达);`.typewriter-dim`(非当前段 0.55)
- N `hooks/use-focus-mode.ts`:preferences 持久化(`inkos:studio:focus-mode`,仅显式 "true" 恢复防会话残留);enter/exit/toggle
- 接入:App 根类 + ⌘⇧F 热键(注册表);**Esc 退出带守卫**——弹层(面板/快速打开/速查/语言选择)打开时 Esc 只关弹层不退专注;ChapterReader 打字机(专注时点击段落聚焦,其余段降透明);ChatPage 经全局 .focus-mode 环境层生效(消息级打字机未做,差异备案)
- 进入/退出为纯 CSS 类切换(无过渡,零闪烁)

## 二、P4-2 密度档与动效规范

- preferences:`density: "comfortable" | "compact"`(`inkos:studio:density`,默认舒适)+ 命令面板两条命令(界面密度:舒适/紧凑,经 CommandContext.setDensity);命令总数 37→39
- `index.css`:密度变量 `--row-h/--pad-y`(紧凑 ×0.7/×0.85)——**变量驱动不逐类覆盖**:SidebarItem/书行/会话行/新建会话行内边距改 `py-[var(--pad-y)]`(或 calc 派生);App 根挂 `.density-compact`
- 动效 token:`--motion-fast/base/slow`(120/200/320ms)落地
- **`@media (prefers-reduced-motion: reduce)` 全局**:所有动画/过渡 0.01ms 瞬时完成(**保留终态,stagger 不卡 opacity=0**)、平滑滚动关闭——同时满足 P4-1 的"reduced-motion 用户默认关闭平滑滚动"验收

## 三、P4-3 通知中心与任务可取消

- N `store/notifications/`:分级 info/warn/error、头插、上限 50、未读计数;纯函数内核 `applyNotificationPush/countUnread`(3 用例)
- N `components/NotificationCenter.tsx`:右下角铃铛(未读徽标 9+ 封顶)+ 非模态弹出面板(分级色点/时间/全部已读/清空);打开即全部已读;点击外部/Esc 关闭;**专注模式不打断**(铃铛常驻)
- App SSE 桥:seq 游标去重,`write:complete`→info(章节完成),错误类事件→error
- 底部面板:生成中显示**停止**钮(复用 abortSession/abort 契约;状态栏 spinner 与面板随 `agent:aborted`/流式状态自然一致收敛)

## 四、验证链

| 门禁 | 结果 |
| --- | --- |
| studio vitest | 81 文件 / 720 测试全绿(+3) |
| typecheck(client + server) | 0 错误 |
| e2e 全量 | **34/34**(新增 focus-mode 4 用例:专注进/出+chrome 隐藏+状态栏保留、打字机段落压暗、密度切换行高实测收缩(poll 防布局竞态)、通知铃铛空态/Esc) |

## 五、调试备案

- CSS display:none 断言须用 `toBeHidden()` 而非 `toHaveCount(0)`(元素仍在 DOM)
- ⌘P 过滤后 Enter 前必须先等 option 可见(条目异步到达窗口内 Enter 落空)
- 密度切换后行高测量用 `expect.poll`(类应用与 boundingBox 之间存在渲染竞态)
