# 151 号变更:UI 优化 P1~P4 任务实现分解(纯文档产出)

- 日期:2026-08-24
- 模块:开发时SpecCoding'sPlan/inkosDesktop/05_UI体验设计
- 类型:规划文档(无代码改动)
- 关联:150 号(分析规划总纲);本文为其实施级分解,同日完成 P1 代码勘察校准

## 一、产出(2 份文档)

| 文档 | 内容 |
| --- | --- |
| P1任务实现分解.md | P1 八任务到文件/接口/步骤/测试粒度:骨架三件套、启动壳改造(替换 App.tsx 156-162 行 spinner 分支)、语言选择器 Dialog 化、三页接骨架、命令面板 MVP(lib/commands 纯函数注册表 + store/recents 照抄 preferences 模式)、动作层 ≥30 条、顶栏三段化(lib/breadcrumb 纯函数)、EmptyState;含依赖图与回滚单元 |
| P2至P4任务分解与总WBS.md | P2(标题栏融合/窗口记忆/主题auto/快捷键分发器/快速打开/速查/菜单栏)、P3(活动栏四区/侧栏增强/标签页store+reducer/dock+底部面板/状态栏)、P4(Focus与打字机/密度与动效token/通知中心与可取消/a11y与e2e/十维复评发布)各四要素分解;四期估时汇总、关键路径、全局风险 Top3 |

## 二、关键勘察发现(分解校准依据)

1. `ui/command.tsx` 的 CommandDialog 全套封装已存在 → 命令面板 MVP 由"写底层"降级为"组合",P1-5 单日可完成;
2. `Nav` 接口在 App.tsx 与 Sidebar.tsx 重复声明 → P1 以 App 版为 ctx 传参,P3-1 统一到 lib/nav.ts(消除双份);
3. use-hash-route 存在「同 hash 重复赋值不触发 hashchange」历史陷阱(源码注释在案)→ P3-3 标签页必须以 store 为真相、hash 仅入站同步,并列为全局风险 Top1;
4. use-theme 现有"按时段默认主题"行为 → P2-3 auto 档需保留该默认,避免老用户体感突变;
5. index.css 动效体系完整但无 prefers-reduced-motion → P4-2 全局补齐,注意 stagger 的 forwards 终态不得卡死 opacity=0;
6. P4-3 取消生成可能触及后端(唯一潜在 Rust 改动)→ 预定 08-31 前预查 SSE cancel 契约,必要时提前执行。

## 三、验证

纯文档产出,无代码/测试影响。文档内引用的代码行号与文件行为均基于 08-24 当日勘察(App.tsx 156-162/184-226、use-hash-route.ts 全文、preferences/store.ts、index.css 221-324 等)。
