# 354 号：G6 末批——方向候选生成接入 LLM+BookCreate 打通+择机项清算（G6 完整收官）

- 日期：2026-09-12
- 类型：双端端点 + 建书打通 + 规划清算
- 规划依据：[328 规划](../20260911/328_对标调研分析与改进规划.md) G6 后半 + §7 目标拓展清算

## 做了什么

1. **方向候选生成接入 LLM（双端）**：
   - TS：`agents/director-agent.ts`（`DirectorAgent.generateDirections`：
     `buildDirectionCandidatesPrompt` → chat(0.8) → `parseDirectionCandidates`，
     标题组重做剔除生效）；`PipelineRunner.generateDirections` 便捷方法
     （agentCtxFor("director")，走 G16 任务路由与既有 modelOverrides 链）；
     `POST /api/v1/director/directions` 端点（校验 inspiration.premise 必填，
     director:start/complete/error 广播）。
   - Rust：`POST /api/v1/director/directions`（`build_direction_candidates_prompt`
     → `router.chat("director", …, 0.8)` → `parse_direction_candidates`），
     director:start/complete/error 广播对齐。
2. **驾驶舱与 BookCreate 打通**：建书成功后自动 `PUT /books/:id/director`
   写入灵感卡（premise=建书表单 Story brief，stage=directions）——新建书籍
   即在驾驶舱可见灵感卡与续跑建议；灵感卡落盘失败不阻断建书流程。
3. **bench:gate 静默窗补验**：349/352/353 三笔待补验清算——静默窗
   （loadavg 2.11/18≈12%）复跑通过，9 基准全绿。

## G11/G12/G13/G15 择机项清算

| 项 | 结论 | 依据与排期建议 |
| --- | --- | --- |
| G11 best-of-N 关键章 | **可启动**（前置 G3 已就位） | 依赖的八级判定/质量债务已落地；建议 N=2~3 稿并行+审查评分选优，与批量调度 qualityGates 联动；等真实写作反馈确认收益后排期（中型，TS+R+S）。 |
| G12 严格模式 | **缓行** | 七重闸门默认全开已在 328 号明确不采纳；现 input-governance 已覆盖写前校验主面。等重度作者反馈闸门需求再立项，避免对休闲用户加复杂度。 |
| G13 项目经验记忆 | **可启动**（前置 G4 已就位） | 经验条目注入 340 号 StyleBinding 的特征池 guidance 即可（/learn 语义）；建议与 G4 管线接线（detect/fix 参与）合并为一个批次（中型）。 |
| G15 Prompt 工作台增强 | **择机（S 单端，中型）** | 引用标签可视化/messages 预览/单提示词试运行均为 studio 产品面；排入下一轮 UI 迭代窗口，与 G16 设置面细化合并。 |

## 验收

- core：tsc 干净；vitest 217 文件 / 2006 用例全绿。
- studio：tsc 干净；vitest 90 文件 / 793 用例全绿。
- engine-rs：`INKOS_DUEL=1 cargo test` 22 个测试目标全 ok。
- bindings 158 绿；bench:gate 静默窗补验通过（loadavg≈12%）。

## 影响面

- 新增 1 文件（DirectorAgent）；修改 5 文件（runner、双端端点/路由、BookCreate）。
- 未触碰并行会话在途文件（package.json、rustbin.rs、audit-rust.mjs）。

## 里程碑

- **G6 三批（352 契约层 + 353 驾驶舱/会话 + 354 LLM 接线/建书打通/清算）收官——
  G6 完整收官，328 号对标规划 Phase A/B/C 全部主件落地。**
- 择机池：G11/G13（可启动）、G15（UI 窗口）、G12（缓行）、350a（write-next
  自动混合召回切换）。
