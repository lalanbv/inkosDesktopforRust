# 355 号：对标二轮精研与第二轮改进规划（R1–R9）

- 日期：2026-09-12
- 类型：调研 + 规划（无代码改动）
- 前置：328 号 v1 规划主件已由 330–354 号全量落地（本号核实）
- 产出：[对标调研分析与改进规划_v2.md](../../开发时SpecCoding'sPlan/inkosDesktop/07_产品演进规划/对标调研分析与改进规划_v2.md)

## 做了什么

1. **落地核实**：抽查源码确认 v1 十六项全部在位（`retrieval/` 四件套、`quality-governance.ts`、`promise-ledger.ts`、`info-gap-ledger.ts`、`deconstruction.ts`、`style-feature-engine.ts`、`context-source-tier.ts`、`resume-advice.ts`、`author-report.ts`、`MessageHud.tsx`、`director-agent.ts` 等），354 号验收数据（core 2006 / studio 793 / duel 22 目标全绿）采信。
2. **二轮实现层精研**（两仓重新浅克隆，读实现非文档转述）：
   - WNW：`urgency_utils`（紧迫度 0–100 容错 coerce）、`context_ranker`（层内确定性排序 recency×frequency×hook_bonus + debug 分）、`context_weights`（模板×阶段预算权重表）、`error_catalog`（code/severity 三分类结构化作者错误）、`quality_trend_report`（逐章审查分数沉淀→趋势）、`write_gates/`、`project_memory`、行为评测 fixtures。
   - ANWA：`ReaderExperienceContract`（逐章读者体验七问，写作/验收/修复三端共用）、张力曲线方案（逐章 conflictLevel/revealLevel→卷级曲线）、prisma 180 模型中的 PayoffLedgerItem（6 态+目标章窗+置信度）、CharacterResourceLedger（owner/holder+readerKnows+事件流）、ChapterArtifactSyncCheckpoint（contentHash 幂等回灌）、ModelRouteConfig、RagRetrievalTrace、GenerationContextAssembler（单包三视图+openingHint+compressionLog）、三库资产（题材基底/推进模式/世界样本）、标题工坊章名维度、反 AI 规则资产化、创作向导。
3. **二轮差距集与路线**：P0=R1 读者体验合同（挂 input-governance，含章名候选）、R2 张力曲线（settle 记 conflict/reveal 分+卷级曲线+告警）、R3 质量趋势与账本增强（分数沉淀+趋势端点+承诺章窗/紧迫度/置信度+回灌幂等）；P1=R4 三库资产、R5 反AI规则资产化+G13 经验记忆、R10 择机顺延（G11/350a）；P2=R6 排序器+组装补强、R7 错误目录、R8 行为评测、R9 创作向导。G12 继续缓行。

## 影响面

纯文档新增，不触碰源码；未卷入并行会话在途文件（package.json、rustbin.rs、audit-rust.mjs）。后续 R 项启动时按本档条目独立编号，golden 先行 + duel 双端 + bench:gate 纪律不变。
