# 466 号：基建惯例文档化——packages/studio/TESTING.md（445–461 备案收口）

- **日期**：2026-09-15
- **类型**：docs —— 沉淀惯例收敛为仓库内维护文档
- **关联**：451/455/456/457/458/459/462 号（交互测试惯例来源）、461 号（PAGE_SPEC 穷举闸）、437/440/442/449 号（走查脚手架）
- **提交**：`packages/studio/TESTING.md`（新，非 gitignore 范围已验证入库）+ 本记录

## 1. 文档内容（六节）

`packages/studio/TESTING.md` 收敛本会话沉淀的全部基建惯例：

1. **测试分层**：纯函数单测 / API 行为测试 / 组件静态冒烟（renderToString）/ 交互测试（`*.interaction.test.tsx` + 文件级 `@vitest-environment jsdom` docblock）四层职责与环境矩阵；
2. **交互测试惯例六条**：data-slot 定位、vi.fn 带参、act 包裹 store 变更、vi.spyOn confirm 打桩、useApi/fetchJson 分双 mock、userEvent.upload 直注文件输入（各条注明来源循环号）；
3. **路由 PAGE_SPEC 单一事实表**：mapped type 键穷举 = 编译闸，writable 语义与 round-trip 穷举测试说明；
4. **走查脚手架**：walkthrough-env 一键三件套、失败注入开关、fixture 幂等与 13 条基线注意、dist 重建与 449 热加载关系；
5. **门禁清单**：六条提交前命令（与根 package.json 脚本逐字核对）；
6. **已知约束**：npmmirror 无 audit 端点、no-store 中间件自动继承、INKOS_LLM_CONTEXT_WINDOW env。

## 2. 选位说明

- `docs/*` 被 gitignore（不入库），故落 `packages/studio/TESTING.md`（check-ignore 验证可入库）；
- 走查脚本头注已含用法细节，正文以引用方式衔接不重复。

## 3. 门禁与遗留

- 纯文档变更（零代码），门禁沿用绿态；入库性已验证。
- **待推送：433–466 共 34 笔**，请用户在 Fork 图形端推送后核验 origin/develop。
- 下一个编号自 **467** 起。
