# 457 号：ChapterWorkspacePanel + RecallTestCard 交互测试（+2 文件 +5 用例）

- **日期**：2026-09-15
- **类型**：test(infra 扩展) —— 交互测试覆盖面扩展（读面交互锁定，零缺陷）
- **关联**：448 号（章节工作台走查）、349 号（G1 召回测试）、451 号（交互基建）
- **提交**：两个 interaction 测试文件 + 本记录

## 1. ChapterWorkspacePanel.interaction.test.tsx（3 用例）

1. **工作台数据渲染**：brief 回填 + 系统计划 + 历史版本行（source/字数；版本行不展示裸 id，断言按实际渲染修正）；
2. **提示词保存**：编辑 textarea → 「保存提示词」→ PUT `/books/:id/chapters/:n/workspace/brief` 载荷为编辑后文本；
3. **灵感抽卡**：POST `/workspace/inspiration` → 渲染灵感卡片文案。

mock 模式：`useApi` 与 `fetchJson` 分别 mock（useApi 返回固定 workspace 快照；fetchJson 捕获载荷）。

## 2. RecallTestCard.interaction.test.tsx（2 用例）

1. **召回主链**：输入查询 → 「检索」→ POST `/books/:id/hybrid-search` 载荷断言 `{query, k:10}` → 融合排名渲染（模式「语义+词法混合」、命中 id、分数 0.8734、`·语义`/`·词法` 双源标记）；
2. **降级与空态**：mode=fts5-fallback → 「词法降级」标记 + 空融合结果 → 「无命中」空态。

## 3. 门禁

- studio：vitest **104 文件 845 用例全绿**（+2 文件 +5 测）+ `tsc --noEmit` 净（修 mock 签名 TS2554）。
- Rust 零改动。

## 4. 遗留

- **待推送：433–457 共 25 笔**，请用户在 Fork 图形端推送后核验 origin/develop。
- 下一个编号自 **458** 起。
