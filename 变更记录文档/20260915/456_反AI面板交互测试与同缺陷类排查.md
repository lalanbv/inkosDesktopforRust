# 456 号：AntiAiAndExperiencePanel 交互测试 + 剩余面板同缺陷类排查收口

- **日期**：2026-09-15
- **类型**：test(infra 扩展) —— 交互测试覆盖面扩展 + 缺陷类排查收口（零产品缺陷）
- **关联**：451/452/455 号（交互测试基建与扩展）、442 号（自动选中不回填缺陷类源头）、392 号（反AI种子）
- **提交**：`packages/studio/src/components/AntiAiAndExperiencePanel.interaction.test.tsx`（新）+ 本记录

## 1. AntiAiAndExperiencePanel 交互测试（+1 文件 +4 用例）

1. **种子徽标与规则列表渲染**（异步等待 GET 完成）；
2. **新增规则合并保存**：填模式+告警文案 →「存规则」→ PUT 载荷断言：种子规则保留（enabled 不变）+ 新增项入列；**id 派生语义锁定**——纯中文模式无 slug 字符 → `rule_<时间戳>` 兜底（测试首版误期 slug 化 id，红后修正并备案该语义）；
3. **停用切换**：PUT 载荷 `enabled` 翻转、其余规则保持；
4. **经验沉淀**：PUT `/experience` 载荷含条目文本，通知提示「同文本自动去重」（/learn 语义）。

## 2. 剩余面板同缺陷类排查收口（零残留）

以「自动选中不回填」/「回调读旧值」两类已知缺陷模式静态排查剩余面板：
- `setSelected` 自动选中模式全仓仅存 CodexPanel（452 修复）与 SeriesCanonPanel（442 修复），均已有回填+交互回归；
- StyleBindingCard 的 setSelected 为受控 select onChange（无独立编辑字段，不同模式非风险）；
- RecallTest/TaskRouting/NotificationCenter 无表单编辑回填面。
**结论：该缺陷类无残留实例；交互测试三面扩展（TimelineEdit→Codex/SeriesCanon→AntiAi/Backup）后基建进入维护态，新面板随实现随测。**

## 3. 门禁

- studio：vitest **102 文件 840 用例全绿**（+1 文件 +4 测）+ `tsc --noEmit` 净。
- Rust 零改动。

## 4. 遗留

- **待推送：433–456 共 24 笔**，请用户在 Fork 图形端推送后核验 origin/develop。
- 下一个编号自 **457** 起。
