# 471 号：QuickOpenPalette 交互测试——索引拉取/查询过滤/选中导航（+1 文件 +3 用例）

- **日期**：2026-09-15
- **类型**：test(infra 扩展) —— ⌘P 快速打开面板交互锁定（零缺陷）
- **关联**：451 号（交互基建）、P2-5（快速打开实现）、361 号（quick-open 纯函数）
- **提交**：`packages/studio/src/components/QuickOpenPalette.interaction.test.tsx`（新）+ 本记录

## 1. 测试内容（3 用例）

1. **开启渲染分组**：书籍项 + 会话项 + 影游项（mock `/interactive-films` 拉取）——书名在书籍项与会话 detail 多处出现，按 `getAllByText` 断言；
2. **章节过滤与导航**：输入章节标题「镜中醒来」→ 章节项出现（mock `/books/b1` 章节索引）→ 点击 → `onNavigate({page:"chapter", bookId:"b1", chapterNumber:1})` + `onOpenChange(false)`；
3. **无匹配空态**：垃圾查询 → 「没有匹配的内容」。

## 2. 基建备忘（jsdom 环境新增两桩）

- `ResizeObserver` 缺失——cmdk/radix Command 依赖，桩化 `observe/unobserve/disconnect`；
- `Element.scrollIntoView` 缺失——cmdk 选中项自动滚动依赖，桩化空函数。
两条已入 TESTING.md 交互惯例节的候选（本文件内注释即文档）。

## 3. 门禁

- studio：vitest **110 文件 875 用例全绿**（+1 文件 +3 测）+ `tsc --noEmit` 净。
- Rust 零改动。

## 4. 遗留

- **待推送：433–471 共 39 笔**，请用户在 Fork 图形端推送后核验 origin/develop。
- 下一个编号自 **472** 起。
