# 472 号：工程债打磨——overrides 分组整理 + jsdom 桩惯例入档 + 测试时序抖动确定性修复

- **日期**：2026-09-15
- **类型**：chore(deps/docs) + test —— 工程债清单打磨（含一处测试时序抖动确定性修复）
- **关联**：467 号（audit:npm 与 overrides 插入）、451/471 号（交互测试基建）、452 号（同款抖动首现）
- **提交**：pnpm-workspace.yaml + TESTING.md + action.test.ts + 本记录

## 1. pnpm-workspace.yaml overrides 分组整理

467 号插入把 467 条目（undici@8/uuid/@ai-sdk 注释）插进了 429 号块中间、`vite@7.3.1` 之前——顺序混杂易误读。整理为三组清晰分段：pi 系钉版（434）→ 429 精确钉批 → 467 audit:npm 批（undici@8/uuid + @ai-sdk 不覆写注释）。**锁文件零变化**（纯注释/顺序整理，`git diff pnpm-lock.yaml` 为空），解析行为不变。

## 2. TESTING.md 交互惯例补两条（471 沉淀）

- **radix/cmdk 系组件两桩**：`ResizeObserver`（空 observe/unobserve/disconnect）与 `Element.prototype.scrollIntoView`（空函数）——jsdom 缺失即 Command/Dialog 渲染崩；
- 多处出现文案用 `getAllByText(...).length` 断言；文本被子元素拆分时用 `findByText` 函数匹配器或直接查 `textContent`。

## 3. 测试时序抖动确定性修复

`action.test.ts`「任务运行中发消息」用例在满载全量跑时间歇失败（452/472 两次复现）：助手回复「任务还在跑。」在 `sendMessage` resolve 之后的微任务才写入，直接断言 `.at(-1)` 时序竞动。修复：改 `vi.waitFor` 轮询断言——全量跑与单文件跑均稳定绿。该抖动自 452 起「复跑全绿」的权宜定性就此终结为确定性修复。

## 4. 门禁

- studio：vitest **110 文件 875 用例全绿** + `tsc --noEmit` 净。
- pnpm install/lockfile 校验：overrides 重排零解析变化。
- Rust 零改动。

## 5. 遗留

- **待推送：433–472 共 40 笔**，请用户在 Fork 图形端推送后核验 origin/develop。
- 下一个编号自 **473** 起。
