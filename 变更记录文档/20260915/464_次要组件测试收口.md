# 464 号：ServiceQuickLinks + HotkeyCheatSheet 测试——次要组件覆盖清单收口

- **日期**：2026-09-15
- **类型**：test(studio) —— 覆盖清单最后两项（零缺陷）
- **关联**：451/455/456/457/458 号（交互测试系列）、G16/346 号（任务路由）、P2-6（速查表）
- **提交**：两个测试文件 + 本记录

## 1. ServiceQuickLinks.test.tsx（3 用例）

- 已知服务商（openrouter）→ 3 链接清单含官网/keys/models href；
- 未知服务商（custom:Mock）→ 空数组；
- detail 变体渲染 `target="_blank" rel="noreferrer"` 锚点；未知服务商渲染 null（无裸 URL 泄漏）。

## 2. HotkeyCheatSheet.test.tsx（3 用例，jsdom）

- 应用级动作行：`APP_ACTION_LABELS` 标签映射（mod+k → **⌘K** Mac 格式化）；
- 非 Mac：`Ctrl+K`（`+` 连接语义锁定）+ 命令注册表行同源出现（P2-6 设计：注册表加键速查自动出现）；
- 关闭态（open=false）不渲染弹层。

## 3. 门禁

- studio：vitest **109 文件 869 用例全绿**（+2 文件 +6 测）+ `tsc --noEmit` 净。
- Rust 零改动。

## 4. 覆盖清单收口声明

至此 studio 组件测试清单收口：主要交互面板（8 个 interaction 文件）+ 次要静态组件（ServiceQuickLinks/HotkeyCheatSheet/ErrorBoundary）全部覆盖；无状态纯展示组件（章节骨架/空态等）由既有冒烟测试覆盖。后续新组件随实现随测（基建与惯例见 451/456/462 号）。

## 5. 遗留

- **待推送：433–464 共 32 笔**，请用户在 Fork 图形端推送后核验 origin/develop。
- 下一个编号自 **465** 起。
