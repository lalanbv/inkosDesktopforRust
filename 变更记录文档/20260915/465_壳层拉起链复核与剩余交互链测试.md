# 465 号：Tauri 壳层引擎拉起链复核（一致性通过）+ 章节工作台/对照分屏交互测试补完

- **日期**：2026-09-15
- **类型**：test(verify + infra 扩展) —— 壳层一致性复核 + 最后两条交互链锁定（零产品缺陷）
- **关联**：441/461 号（引擎 env 与上下文窗口解析）、449 号（no-store/热加载）、458 号（ChapterReader 测试）
- **提交**：两个 interaction 测试文件扩展 + 本记录

## 1. Tauri 壳层引擎拉起链复核（一致性通过，零改动）

复核 `src-tauri`（rustbin.rs build_launch）引擎拉起契约：
- env = `INKOS_PORT` / `INKOS_PROJECT_ROOT` / `INKOS_STATIC_DIR` 三件；**LLM 端点 env（INKOS_LLM_*）刻意不注入**（rustbin.rs L106 注释：引擎侧 `effective_router` 热解析 inkos.json 服务项 + secrets 优先）——与 441/461 的上下文窗口解析设计**自洽**：桌面默认路径由引擎按内建模型卡解析窗口，无需壳层传递；
- `INKOS_PROJECT_ROOT` 不注入旧 supervisor 路径的备案（L56/L296）与 441 无冲突；
- 结论：壳层与 441 预算接线、445 缓存头、449 热加载**无衔接缺口**。

## 2. 最后两条交互链测试（+2 用例，全绿）

1. **对照分屏换章**（ChapterReader.interaction.test）：第 2 章起（初始对照章=前一章）→ splitNext → 断言 useApi 以 `/books/b1/chapters/2` 重新拉取 + `reader.compareChapter · 2` 渲染；
2. **灵感卡并入提示词**（ChapterWorkspacePanel.interaction.test）：抽卡 → 「并入提示词」→ brief textarea = 原提示词 + 空行 + 卡片文本（`join("\n\n")` 语义锁定）。

## 3. 门禁

- studio：vitest **109 文件 871 用例全绿**（+2）+ `tsc --noEmit` 净。
- 壳层零改动（复核性循环）；Rust 其余零改动。

## 4. 遗留

- **待推送：433–465 共 33 笔**，请用户在 Fork 图形端推送后核验 origin/develop。
- 交互测试清单最终收口（本号后剩余面均为纯展示或低价值，不再扩展）。
- 下一个编号自 **466** 起。
