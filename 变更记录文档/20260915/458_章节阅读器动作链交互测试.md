# 458 号：ChapterReader 动作链交互测试——通过/驳回/对照分屏（+1 文件 +3 用例）

- **日期**：2026-09-15
- **类型**：test(infra 扩展) —— 章节阅读器动作链交互锁定（零缺陷）
- **关联**：448 号（章节工作台走查）、457 号（工作台交互测试）
- **提交**：`packages/studio/src/pages/ChapterReader.interaction.test.tsx`（新）+ 本记录

## 1. 测试内容（3 用例）

1. **通过**：点击「通过」→ 断言 `postApi("/books/b1/chapters/1/approve")` + 导航回书页（`nav.toBook("b1")`）；
2. **驳回**：同链断言 `/reject` 端点与导航；
3. **对照分屏开关**：初始无分隔条 → 点击 toggle（`data-testid="reader-split-toggle"`）→ `data-testid="reader-split-divider"` 出现 + 文案切「reader.splitClose」→ 再点消失（开合两态断言）。

## 2. 门禁与备忘

- studio：vitest **105 文件 848 用例全绿**（+1 文件 +3 测）+ `tsc --noEmit` 净（修 postApiMock 零参签名 TS2554——vi.fn 实现带参才能断言调用参数）。
- Rust 零改动。

## 3. 遗留

- **待推送：433–458 共 24 笔**，请用户在 Fork 图形端推送后核验 origin/develop。
- 下一个编号自 **459** 起。
