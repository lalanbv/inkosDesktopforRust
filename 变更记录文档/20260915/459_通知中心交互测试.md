# 459 号：NotificationCenter 交互测试——未读徽标/已读/清空/Esc 关闭（+1 文件 +3 用例）

- **日期**：2026-09-15
- **类型**：test(infra 扩展) —— SSE 通知面交互锁定（零缺陷）
- **关联**：451/452/455/456/457/458 号（交互测试系列）、P4-3（通知中心实现）
- **提交**：`packages/studio/src/components/NotificationCenter.interaction.test.tsx`（新）+ 本记录

## 1. 测试内容（3 用例，真 zustand store 驱动）

1. **未读徽标计数与入列渲染**：真 store `pushNotification` 推 info+error 两条 → 徽标计数「2」→ 点击铃铛开面板 → 通知标题/detail 渲染 → **打开即全部已读**，徽标消失；
2. **清空通知**：→ store 数组归零 + 「暂无通知」空态；
3. **Esc 关闭**：面板开着 → 按 Escape → 面板关闭。

**基建备忘**：渲染后推送 store 变更须包 `act(...)`——否则 zustand 订阅的 DOM 刷新不落地（首版测试徽标断言 undefined 即此因）；渲染前推数据则无需 act。

## 2. 门禁

- studio：vitest **106 文件 851 用例全绿**（+1 文件 +3 测）+ `tsc --noEmit` 净。
- Rust 零改动。

## 3. 遗留

- **待推送：433–459 共 27 笔**，请用户在 Fork 图形端推送后核验 origin/develop。
- 交互测试覆盖：TimelineEditDialog/Codex/SeriesCanon/Backup/AntiAi/ChapterWorkspace/RecallTest/NotificationCenter——主要交互面全部有锁定；次要纯展示组件（ServiceQuickLinks 等）为无状态面不再扩展。
- 下一个编号自 **460** 起。
