# 468 号：对照分屏跳转输入框交互测试（+1 用例，输入解析语义锁定）

- **日期**：2026-09-15
- **类型**：test(studio) —— 对照分屏最后一条交互链锁定（零缺陷）
- **关联**：462 号（分屏换章用例）、465 号（交互测试最终扩展）
- **提交**：`packages/studio/src/pages/ChapterReader.interaction.test.tsx`（扩展）+ 本记录

## 1. 用例内容

跳转输入框（`data-testid="reader-split-jump"`，Enter/失焦触发 `commitJump`）：
- 输入数字「1」+ Enter → `onChapterChange(1)` → useApi 以 `/books/b1/chapters/1` 重新拉取对照章 + 输入框清空；
- 输入非法「abc」+ Enter → **不发非法请求**，且 `commitJump` 对非法输入同样清空输入框（本次测试首版断言「保留 abc」系误判组件实际行为——commitJump 无条件清空，为正确 UX，断言已按实际行为修正）。

## 2. 门禁

- studio：vitest **109 文件 872 用例全绿**（+1）+ `tsc --noEmit` 净。
- Rust 零改动。

## 3. 遗留

- **待推送：433–468 共 36 笔**，请用户在 Fork 图形端推送后核验 origin/develop。
- 下一个编号自 **469** 起。
