# 451 号：studio 交互测试基建引入 + TimelineEditDialog 草稿透传回归测试（450 备案收口）

- **日期**：2026-09-15
- **类型**：test(infra) —— 测试盲区基建补齐
- **关联**：450 号（草稿透传缺陷，备案交互测试缺位）、442 号（同缺陷类先例：自动选中不回填）
- **提交**：`packages/studio/src/pages/BookTimeline.interaction.test.tsx`（新）+ package.json/lockfile + 本记录

## 1. 基建引入

- 新增 devDeps：`jsdom ^30`、`@testing-library/react`、`@testing-library/user-event`（React 19 兼容线；npmmirror 镜像滞后走 npmjs registry，444 号先例）。
- vitest 环境保持 node 缺省不变；新测试文件以 **`// @vitest-environment jsdom` 文件级 docblock** 启用 DOM——97 个既有 node 环境测试零扰动，交互测试按需逐文件启用。
- 命名约定：`*.interaction.test.tsx` 与静态冒烟测试（renderToString）文件分离。

## 2. 回归测试（450 号缺陷锁定）

`BookTimeline.interaction.test.tsx` 两用例：
1. **草稿透传主测**：userEvent 输入节拍标题「夜探镜渊」+备注 → 点击 `data-slot="timeline-write-from-beat"` → 断言 `onWriteFromBeat` 以 `{ title: "夜探镜渊", note: "苏檀夜探镜渊，镜灵现身。" }` 调用——450 号修复前该断言必挂（回调无参调用）。
2. **行为不变测**：未编辑时点击透传 target 原值。

**红绿验证**：临时还原 450 修复前版 BookTimeline.tsx → 两测全红；恢复修复版 → 全绿。回归测试有效性实证。

基建备忘：字段定位用 `data-slot`（组件惯例）而非 testing-library 惯用的 `data-testid`/`getByTestId`；`afterEach(cleanup)` 需显式从 vitest 导入。

## 3. 门禁

- studio：vitest **98 文件 831 用例全绿**（+1 文件 +2 测）+ `tsc --noEmit` 净。
- 新依赖仅 devDeps（不进产物），Rust 零改动。

## 4. 遗留

- **待推送：433–451 共 19 笔**，请用户在 Fork 图形端推送后核验 origin/develop。
- 交互基建后续可扩展至 Codex/SeriesCanon 等表单面板（同「回调读旧 props」风险面）。
- 下一个编号自 **452** 起。
