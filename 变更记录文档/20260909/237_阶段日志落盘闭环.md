# 237 号：inkos.log 消费端闭环——write-next 阶段日志落盘（TS fileSink 对应面）

- 日期：2026-09-09
- 分支：develop
- 关联：210 号（日志通道初建——本批扩展覆盖面）、127 号（on_log 装配）、TS `createFileSink` 三通道 logger
- 推送核验：origin/develop 仍停 6bc65564——**201–236 共 36 提交待推送**；本批次后本地领先 37。两项默认值无新答复。

## 一、缺口

TS pipeline 的 logger 挂三 sink（scopedSseSink + console + **fileSink→inkos.log**）——各阶段叙事（撰写/审稿/settle 等）全部落盘。Rust 的 `on_log` 装配（books_routes `with_event_broadcasts`）只广播 SSE **不落盘**——210 号修复后 inkos.log 仍只有连写失败/中止/单章失败三个失败点写入，**常规阶段叙事缺失**，LogViewer/doctor 的日志面在默认引擎下内容稀薄（TS 端每章都有完整阶段流水）。

## 二、修复

`with_event_broadcasts` 增 `project_root` 参数——`on_log` 回调在 SSE 广播之外同步 `append_log_event`（210 号截头轮转通道，失败忽略）。语义对齐 TS 三通道（SSE/console/file）中的 file 通道；127 号「books 面不带 sessionId」的负载形态不变。126 号 SSE 事件序测试补 inkos.log 落盘断言。

## 三、验证

- engine：lib **1315**、集成 **196**（+阶段日志落盘断言）、clippy 零告警、INKOS_DUEL=1 duel **10/10 真跑**。
- TS 零改动。

## 四、遗留

agent_production 生产任务链的 on_log 未接线（生产链日志走任务快照 logs 字段，210/222 号备案面）——如需阶段叙事也落 inkos.log 另批评估。
