# 251 号：真实引擎冒烟回归——核心端点全通过

- 日期：2026-09-09
- 分支：develop
- 走查环境：生产形态引擎（fixture 项目 b1 三章 + 4768 端口）+ 真实 HTTP 调用
- 推送核验：origin/develop 仍停 6bc65564——**201–250 共 50 提交待推送**；本批随 251 后本地领先 52。两项默认值无新答复。

## 一、冒烟覆盖与结果

| 端点/链路 | 结果 |
|---|---|
| GET /books | ✓ fixture 书正确返回 |
| POST /sessions | ✓ 200（timestamp-random 校验后创建） |
| POST /agent（LLM 不可达） | ✓ 502 AGENT_LLM_ERROR + transcript 四事件（session_created/request_started/user/request_failed——237 号失败轮持久化生效） |
| POST /books/:id/resync/:chapter | ✓ 500（无 LLM 预期形态） |
| GET /logs | ✓ 空态 JSON 正确 |
| POST /books/:id/audit/:chapter | ✓ 500（LLM 依赖预期） |
| materials HTTP 端点 | ✓ **双端均无独立端点**——素材 ingest/retrieve 仅走 agent 工具（一致性确认，此前猜测 URL 404 属正常） |

## 二、验证

真实 HTTP 冒烟；自动化门禁维持全绿（241–250 号）。

## 三、遗留

无新增。
