# 247 号：retrievalTrace 检索溯源字段——233/244 号备案的可观测性收尾

- 日期：2026-09-09
- 分支：develop
- 关联：233 号（semanticSelectedIds 溯源备案）、244 号（语义精简层）、TS MemoryRetrievalTrace
- 推送核验：origin/develop 仍停 6bc65564——**201–246 共 46 提交待推送**；本批次后本地领先 47。两项默认值无新答复。

## 一、内容

`MemorySelection` 增 `retrieval_trace: Option<MemoryRetrievalTrace>`（TS `MemoryRetrievalTrace` 对应面，camelCase 序列化）：
- `engine: "sqlite-fts5-bm25"`、`query`（goal+outline+mustKeep 合成检索串）；
- `candidates[]`（全量 BM25 候选的 id/kind/source/score——在 ranked_hits 消耗前快照）；
- `semanticSelectedIds`（语义精选结果；回退路径省略键）。

索引不可用时 trace 为 None（与既有失败静默语义一致）。

## 二、验证

- engine：lib **1325**、集成 **197**、clippy 零告警、INKOS_DUEL=1 duel **10/10 真跑**。
- TS 零改动。

## 三、遗留

无新增。
