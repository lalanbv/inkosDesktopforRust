# 224 号：project_tools 文件三件 + book_session_store 并发面轻量复核——零缺口

- 日期：2026-09-09
- 分支：develop
- 关联：105 号（文件三件作用域）、121 号（list 摘要对跑勘误）、220 号（删除守卫——本批复核其边界）
- 推送核验：origin/develop 仍停 6bc65564——**201–223 共 23 提交待推送**。两项默认值无新答复。

## 一、project_tools 文件三件（read/ls/grep，518 行）

| 审计点 | 结论 |
|---|---|
| 防逃逸（safe_join） | ✓ canonical 存在路径 starts_with 校验 + 不存在路径词法父目录回溯；root 不存在时词法形态一致（/tmp symlink 场景两侧同 canonicalize） |
| 读上限 | ✓ read 1MB；非 UTF-8（二进制）被 read_to_string 天然拒绝 |
| grep 上界 | ✓ 深度 3、500 文件、200 命中三重上限；隐藏目录/node_modules/target 跳过；扩展名白名单（md/txt/json/ts/js/rs/yaml/yml） |
| 作用域 | ✓ 书会话走 books/ 作用域三件（105 号），chat 回退项目根作用域 + material 双件 |

**备案（安全向行为差异，不改）**：绝对路径输入 TS `resolve` 语义下被拒绝（escapes 报错），Rust 裁为 root 相对路径尝试（不存在 → read failed）——Rust 更严，无安全缺口。

## 二、book_session_store 并发面（374 行）

| 审计点 | 结论 |
|---|---|
| 事件原子性 | ✓ 全部写点经 append_transcript_events per-session 串行锁（220 号确认）；rename/migrate 的 load→append→load 三步并发交错为「后写者赢」，与 TS appendTranscriptEvents 同构 |
| 删除竞态 | ✓ rename/migrate 前置 load 对已删会话天然防护（None 返回）；**备案**：load 与 append 之间的极窄窗口理论可复活 transcript——TS updateBookSessionMetadata 同样无守卫，双端一致不修；220 号守卫只覆盖聊天轮收尾（TS appendSessionMessagesUnlessDeleted 对应面），范围一致 |
| list | ✓ `.jsonl`/`.json` 双形态收集 + sort/dedup、bookId 过滤（null 精确匹配）、updatedAt 降序、playMode 未设省略键（121 号） |
| createAndPersist | ✓ 幂等（同 id 直接返回）；kind/playMode 变更走 metadata 事件 |

## 三、验证

纯复核批次（零代码改动）：engine 门禁于 221 号已绿（lib 1310/集成 196/clippy 0/duel 10/10），core 1917 / studio 793 / 双 typecheck 已绿。

## 四、遗留

无新增。
