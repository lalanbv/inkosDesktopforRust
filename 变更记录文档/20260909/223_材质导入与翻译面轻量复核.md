# 223 号：材质导入解析边界 + translation 模块轻量复核——零缺口

- 日期：2026-09-09
- 分支：develop
- 关联：83 号（materials 移植）、216 号（references 子系统——本批复核其上游依赖面）、70/75 号（translation e2e）
- 推送核验：origin/develop 仍停 6bc65564——**201–222 共 22 提交待推送**。两项默认值无新答复。

## 一、材质导入解析边界（materials.rs，950 行）

| 审计点 | 结论 |
|---|---|
| 来源大小限制 | ✓ `MAX_SOURCE_BYTES = 18MB`，超限显式报错 |
| 路径安全 | ✓ `safe_child_path` 防目录穿越；markdown/manifest 落 `.inkos/materials/{id}.md/.json` |
| URL 来源 | ✓ scheme 白名单（http/https）+ 20s 超时 + no_proxy；非 http(s) 显式拒绝 |
| 素材 id 生成 | ✓ `ISO 时间戳(:. → -)+ "-" + slug(title)`；slug 白名单 `\p{L}\p{N}` 折 `-`、80 UTF-16 截断、空兜底 "material"——不可能产出路径分隔符或 `..` |
| 与 references 兼容 | ✓ 216 号 `assert_material_id`（≤240/防穿越）与实际生成形态兼容；`markdownPath` 由 `to_posix_relative` 生成、与 `load_material_asset` 的 id 对齐校验闭环 |
| 截断面 | ✓ title ≤120 UTF-16、excerpt ≤1600 |
| 非原子写 | 无风险——id 唯一时间戳，md/manifest 均为新文件，不存在覆盖旧值场景（区别于 221 号配置覆盖写） |

## 二、translation 模块（2372 行 + routes 493）

| 审计点 | 结论 |
|---|---|
| 守门覆盖 | ✓ lib 层 translation 23 测试（text.rs 分段核心 11 个）+ e2e 双模块（translations70_e2e / translation75_e2e 的 create/confirm 流） |
| 上传/导出面 | ✓ `build_epub_for_test` 等测试入口在位；214 号 EPUB 全检已覆盖导出结构 |

## 三、验证

纯复核批次（零代码改动）：engine 门禁于 221 号已绿（lib 1310/集成 196/clippy 0/duel 10/10），core 1917 / studio 793 / 双 typecheck 已绿。

## 四、遗留

无新增。
