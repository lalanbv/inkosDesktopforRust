# 241 号：LocalSearchIndex 词法检索内核移植——use_skill query 分支清偿

- 日期：2026-09-09
- 分支：develop
- 关联：240 号（use_skill 移植——本批清偿其 query 备案）、233 号（memory 语义层同族依赖——本批为其提供候选集基建前提）
- 推送核验：origin/develop 仍停 6bc65564——**201–238 共 38 提交待推送**；本批次后本地领先 40。两项默认值无新答复。

## 一、移植内容（`utils/local_search.rs`，TS retrieval/local-search.ts 284 行）

1. **FTS5 投影**：rusqlite（bundled，memory_db 同依赖）external-content 表 + AI/AD/AU 三触发器，SQL 逐字对齐 TS migrate()。
2. **BM25 检索**：`bm25(fts, 5.0, 1.0)`（title 权重 5.0）ASC 排序 + scope/kinds 过滤 + limit 1..=200；score = -rank。
3. **replace_scope**：SHA-256 content hash 去重增改 + 消失 id 删除 + 事务回滚。
4. **分词器**：NFKC + split_word_bounds（ICU 近似——备案分数不逐字一致）+ Latin/数字单字符过滤 + 相邻汉字 bigram 追加 + 连字符复合词追加。
5. **splitMarkdownForSearch**：空行分块（块内无空行）+ 标题继承（标题块不产段）+ 字符偏移。
6. **buildMatchQuery**：去重 64 词、`"tok"` OR 连接、内引号转义。

## 二、use_skill query 分支清偿

`retrieve_skill_resources`：skill 目录文本文件（浅层+references/、跳隐藏、扩展名白名单）→ markdown 分段 → 内存 BM25（:memory:）→ top-4 相关段；details 增 `query`/`retrievedResources`（path/heading/charStart/charEnd/body/score）；文本追加「Relevant static references:」段。240 号备案清偿。

## 三、验证

- engine：lib **1324**（+5：FTS5 可用性/检索往返/scope 删除/分段标题继承/汉字 bigram+连字符、query 检索端到端）、集成 **196**、clippy 零告警、duel 真跑 10/10。
- TS 零改动。
