# 251 号：真实 LLM 错误链走查——错误码面/失败轮持久化/溯源三重验证

- 日期：2026-09-09
- 分支：develop
- 关联：218 号（错误码面）、237 号（失败轮持久化）、240 号（sessionId 安全校验）
- 走查环境：生产形态引擎 + 仓库根真实项目配置（LLM 端点为无效占位 127.0.0.1:9）+ 真实 HTTP 调用
- 推送核验：origin/develop 仍停 6bc65564——**201–248 共 48 提交待推送**；本批随 249 后本地领先 51。两项默认值无新答复。

## 一、走查覆盖与结果

| 链路 | 结果 |
|---|---|
| sessionId 安全校验 | ✓ 非 timestamp-random 形态（`walkthrough-251`）被静默拒绝并生成新 id（防任意文件名注入；合规 id 正常） |
| **失败轮持久化（237 号）** | ✓ transcript 四事件齐全：session_created + request_started（input）+ message(user) + **request_failed**（error 文本）——用户消息在 LLM 失败后保留 |
| **错误码面（218 号）** | ✓ 502 `AGENT_LLM_ERROR`，消息含 reqwest 传输词汇（error sending request）→ 分类正则真实命中 |
| 错误文本溯源 | ✓ 消息精确定位 url（127.0.0.1:9/chat/completions）——诊断可据 |

## 二、观察（非缺陷）

仓库根 inkos.json 的 LLM 为占位配置（baseUrl=example.invalid/noop-model）——直接跑 engine bin 时 effective_router 回退启动 router（127.0.0.1:9 占位端点）。真实 LLM 使用走 Tauri 壳或配置 services。此为配置态而非代码问题。

## 三、验证

本批为真实 HTTP 走查（curl），零代码改动；自动化门禁于 248 号已绿。
