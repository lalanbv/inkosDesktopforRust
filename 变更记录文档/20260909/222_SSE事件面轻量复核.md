# 222 号：SSE 事件面轻量复核——事件名全集对齐确认 + 生产链日志实时性备案

- 日期：2026-09-09
- 分支：develop
- 关联：172 号（优雅停机排空）、174 号（task:snapshot 对账）、127 号（on_log 装配备案复核通过）、66 号（Lagged 消费）
- 推送核验：origin/develop 仍停 6bc65564——**201–221 二十一提交均未推送**；本批次后本地领先 22。两项默认值无新答复。

## 一、复核范围与结论

| 复核点 | 结论 |
|---|---|
| **事件名全集对齐** | ✓ TS 广播 66 个事件名经 Python 全量字符串求交（跨行调用形态无遗漏）在 Rust 源码**全部命中，零缺口**。注：grep 逐行对照会因 Rust 多行调用形态漏报 28+ 个——机械对照须用全量提取 |
| Hub 语义 | ✓ broadcast 256 容量、无订阅者静默（TS 同语义）、Lagged→ping 跳过、shutdown 独立 watch + 排空后 EOF（172 号）、连接即 ping + task:snapshot 对账（174 号）、30s keep-alive |
| 事件负载守门 | ✓ duel `sse_event_face_duel` 持续守门 |
| `log` 事件 sessionId 归属 | ✓ books 面双端一致不带（127 号备案复核属实：TS books write-next pipeline 无 sessionIdForSSE）；chat 页 stream listener 靠 `sessionMatchesEvent` 过滤的语义双端同构 |
| 引擎特有事件 | `engine:shutdown`（bin 停机）/ `task:snapshot`（SSE 重连快照，TS 为流直发形态）——进程形态差异，合理 |

## 二、备案（不实施）

**生产任务链的实时 log SSE**：TS confirmed 任务链经 scopedSseSink 发带 sessionId/executionId 的实时 `log` 事件（chat 页实时可见）；Rust 生产链日志走任务快照（make_on_progress → logs 持久化 → task:snapshot/快照重放），chat 页收批可见、无数据丢失但缺实时流。改进面：生产链装配带 executionId 的 SSE log（UI `eventExecutionId` 分支已支持按 id 定位工具卡）。留待真实需求驱动。

## 三、验证

纯复核批次（零代码改动）：engine 门禁于 221 号已绿（lib 1310/集成 196/clippy 0/duel 10/10），core 1917 / studio 793 / 双 typecheck 已绿。
