# 255 号：全量测试与代码审查日——两处修复 + 真实引擎/浏览器双冒烟全过

- 日期：2026-09-09（跨零点至 09-10 凌晨）
- 分支：develop
- 范围：全量测试（Rust+Node）+ 静态检查 + 近期提交代码审查（253/254 号、246 号补）+ 真实引擎 HTTP 冒烟 + 真实浏览器 UI 走查

## 一、全量测试结果（全绿）

| 门禁 | 结果 |
|---|---|
| cargo test（INKOS_DUEL=1） | ✓ 1612 通过 0 失败（lib 1329 + e2e 197 + golden 76 + duel 10/10 真跑） |
| cargo clippy --all-targets | ✓ 0 告警 0 错误 |
| pnpm -r test | ✓ 2943 通过 0 失败（core 1918 / studio 793 / cli 232） |
| pnpm -r typecheck | ✓ 三包全过 |
| playwright e2e | ⚠ `test:e2e` 脚本存在但无 playwright.config/用例——实际未搭建（遗留项） |

> **【2026-09-10 更正】上表末行结论有误**：e2e 套件（playwright.config.ts + 19 个 spec + fixtures + global-setup）自 161–166 号起即存在且被 git 跟踪。当时 shell 工作目录停留在 engine-rs/，`ls packages/studio/e2e` 相对路径落空导致误判。e2e 实跑结果见 257 号。遗留项 1（补齐/移除 e2e）作废。

## 二、代码审查发现与修复（本批改动）

1. **[已修] 全角/半角冒号移植偏差**：`interactive_film.rs` `build_film_authoring_context` 的「角色档案：」写成半角 `:`，与 TS `film-context.ts` 逐字契约不符，且单测断言照实现抄写错了。该字符串进 LLM 提示词，属移植守门缺口。已改回全角并修正断言（interactive_film.rs 3 行）。
2. **[已修] 生产路径调试残留**：`local_search.rs` `search()` 错误分支残留 `eprintln!("SEARCH row/query_map error")` 与 `#[allow(clippy::never_loop)]` 脚手架（246 号补调试期产物）。已还原为静默吞错（对齐 TS 尽力而为语义）。
3. **[记录不动] 提交信息错位**：8a775c1c（信息写"clippy 修复补漏"）实际内容是 254 号上下文纯函数+单测；868097b3（信息写"上下文纯函数"）只含文档。两者信息互换。内容本身无问题；如需修整须 rebase 改写（本地领先未推送范围内可行）。
4. **[记录不动] 测试调试残留**：`local_search.rs` 的 `debug_tests`/`debug_heading_tests` 模块（println 探针、一处无断言）与 `INKOS_LS_DEBUG` cfg(test) 块为调试期产物，测试态无副作用，建议后续批次清理。
5. **[记录不动] rustfmt 未启用**：`cargo fmt --check` 在 benches/hot_paths.rs 有格式漂移；本仓以 clippy 零告警为标准、未全量启用 fmt（全量格式化会产生 219 文件巨量 diff，不建议在本批做）。
6. **[正面确认]** 252 号 duel 污染 dist 修复正确（占位 index.html 仅缺失时写）；六类 delta builder 与 TS 逐字一致；retrieve_material BM25 分段/去重/limit×4 截断与 TS 语义对齐，hash 增量投影事务化。

## 三、真实引擎 HTTP 冒烟（生产形态，4767 端口，fixture 书 b1 三章）

| 端点 | 结果 |
|---|---|
| GET /api/v1/health | ✓ `{"ok":true,"backend":"rust-engine"}` |
| GET /books、/books/b1 | ✓ fixture 书与 3 章正确返回 |
| POST /sessions | ✓ 会话创建 |
| POST /agent（无 LLM 配置） | ✓ 502 AGENT_LLM_ERROR；transcript 四事件持久化（session_created/request_started/message/request_failed——237 号链路生效） |
| GET /logs | ✓ 空态正确 |
| 静态面 / + /assets/*.js | ✓ SPA 页与资源 200 |

## 四、真实浏览器 UI 走查（IAB 真实点击，1280×720）

| 页面/链路 | 结果 |
|---|---|
| 首页/仪表盘 | ✓ 侧栏/书列表「压测书b1·仙侠·3章·进行中」/无 LLM「去配置」引导/命令面板 |
| SSE | ✓ 页脚「实时」标记全程在线 |
| 书详情（点击书卡导航） | ✓ 写作入口（写下一章/审计/导出）+ 上下文面板 3 章列表（◆ 状态）+ 核心文件/角色 |
| 章节深链 `#/book/b1/chapter/3` | ✓ 章节工作台直达：面包屑/通过/驳回/对照分屏/本章提示词/随机抽卡/历史版本空态/正文渲染（42 字符·1 分钟阅读·本章完） |
| 写下一章（无 LLM 配置） | ✓ toast「请先选择一个模型」+「配置模型 →」引导，页面不炸 |
| 环境诊断 | ✓ 六项检查正确（LLM 失败=无配置预期呈现；书籍健康「无写作阻塞问题」） |
| 日志页 | ✓ 「暂无日志」空态正确 |

证据截图：`gui-test-screenshots/t1_home.png`、`t3_book_detail.png`、`t4_chapter_workbench_retake.png`、`t5_write_next_no_llm.png`、`t6_env_diagnosis.png`、`t7_logs_empty.png`。

走查工具性观察（非产品缺陷）：①IAB 截图相对 DOM 约滞后一帧，连拍两张可规避；②Playwright locator click 在本应用因入场动画判定不可稳定点击而超时，真实坐标点击（cua）全部正常——真人交互无碍；③242 号「首页预览遮罩」本次复核：elementFromPoint 证实书卡命中正常，无遮罩拦截。

## 五、验证

修复后回归：cargo test 全量 1612/0 + clippy 0。Node 侧未受本批改动影响（仅 engine-rs 两文件）。

## 六、遗留

1. ~~playwright e2e 配置与用例缺失~~ **作废（2026-09-10 更正：结论有误，套件自 161–166 号即存在，见文首更正说明）**。
2. `debug_tests`/`debug_heading_tests`/`INKOS_LS_DEBUG` 调试残留清理（建议 246 号后续批）。→ **已随 257 号清理**。
3. 8a775c1c / 868097b3 提交信息错位——未推送前可 rebase 改写。
4. ⚠ 走查期间检测到另一会话并行开发 `film_authoring_tools.rs` 接线（agent_route/chat_prompts/agent_production/interactive_film_routes + 新文件），本批所有测试证据在其写入前产生、未受污染；提交时注意与本批两文件修复（interactive_film.rs、local_search.rs）区分归组。→ 已随 256 号（f2378060）提交。
