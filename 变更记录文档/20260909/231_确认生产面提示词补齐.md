# 231 号：确认生产面 clarify/staging 提示词补齐（chat_prompts 扩展）

- 日期：2026-09-09
- 分支：develop
- 关联：230 号（chat/book/edit 三面——本批扩展五个面）
- 推送核验：origin/develop 仍停 6bc65564——**201–230 共 30 提交待推送**；本批次后本地领先 31。两项默认值无新答复。

## 一、架构洞察（缩小移植范围）

TS 的 confirmed 分支提示词（「用户已点击确认…立即调用 short_fiction_run」等）在 Rust 架构下**聊天循环不可达**：Rust（67/74/84 号）确认后由 agent_production 直接执行生产链，不回聊天循环。因此 confirmed 分支提示词属死代码，不移植；只移植聊天循环可达的 **clarify/staging 分支**（5 面）。

## 二、移植内容（chat_prompts.rs 扩展）

| 面 | 分支 | 提示词要点 |
|---|---|---|
| book-create | staging | 分阶段聊清草案；propose_action 即确认卡；暂定核心冲突提炼；默认 200/3000；createBook 结构化字段 |
| short | clarify | short_run/generate_cover 分流；shortRun 字段（title 恒填/language 产出语言/charsPerChapter 每章语义 zh 900-1200 默认 1000、en 600-800 词默认 650） |
| script | clarify | script_create 规格（title/sourceKind/targetFormat/episodeCount…）；sourcePath 先 read；不替用户决定改编强度 |
| storyboard | clarify | 分镜粒度/画幅/镜头上限；storyboardCreate 字段；「待用户后续调整」语义 |
| interactive-film | clarify | interactiveFilmCreate 字段；不默认 RPG 数值/战斗公式/装备系统 |

`build_system_prompt` 分发扩展五个 SessionKind；play 有世界时仍由调用方先行覆盖（80 号）。

## 三、验证

- engine：lib **1315**（chat_prompts 分发测试扩展 5 面断言）、集成 **196**、clippy 零告警、INKOS_DUEL=1 duel **10/10 真跑**。
- TS 零改动；core 1917 / studio 793 / 双 typecheck 已绿。

## 四、遗留

interactive-film-authoring 面提示词（依赖 authoring 工具族 set_world_anchor 等的移植状态）另批评估。
