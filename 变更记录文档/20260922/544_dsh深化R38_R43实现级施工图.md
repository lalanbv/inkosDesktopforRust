# 544 号：dsh 对标 v7 深化——R38–R43 实现级施工图（deepseek-ai/deepseek-harness）

日期：2026-09-22
提交：本文件与施工图同批路径限定提交（纯文档，零代码改动）
类型：docs(规划)，产出 `开发时SpecCoding'sPlan/inkosDesktop/07_产品演进规划/dsh深化R38_R43实现级施工图.md`

## 选题来路

用户指示「继续深化分析」deepseek-harness 对标。541 号 v7 为文档级对标+路线立项；
本篇把双侧证据升级到源码级，为 R38–R40（P0）出实现级施工图、R41–R43（P1）出深化设计。
与并行会话的 542/543 号（Pi 深化施工图系列）同构同日，R 段零重叠。

## 调研面（全部当日实测）

- dsh sparse-clone 源码实读：packages/core/tools/src/index.ts（1955 行：ToolDefinition :216、
  三态调度器 prepare→dispatch→post-result→final-result :1348-1461、单调守卫 :706-749、
  restriction 编译 :682-701）、packages/spill/spill-policy/src/index.ts（227 行全文）、
  packages/core/scope/src/index.ts（204 行全文）、schema.ts defineTool :545、
  ts-types.ts schema→TS 投影器
- 本地源码级二次提取：InteractionTool（project_tools.rs:164）与 LoopToolExecutor
  （agent_loop.rs:88）**抽象已存在**、ChatToolRouter（agent_route.rs:1407）if 链+
  deps 在场+PRODUCTION_MUTATION_TOOL_NAMES 三重隐式门控、六 family 分发函数签名、
  production_bindings:25-32、sse.rs broadcast:49、mod.rs 四层工厂 :224/234/249/260、
  write_next 阶段函数锚点、streaming_client.rs:199-205 无重试备案注释原文、
  agent-tools.ts 32 工厂、agent-session.ts:1147、use-sse.ts 常量数组

## 头号结论（对 v7 的三点修正/强化）

1. **R38 降险为收拢**：Rust 侧 schema 结构体+执行器 trait+循环抽象已存在，缺的是
   「schema 与执行器同位化 + 三重隐式门控显式化 + 双端工具面机械对照」，非新造抽象。
2. **dsh 三态调度器是 R39 的实现形态答案**（有序 pre+守卫→工具体→有序 post），
   单调守卫「只 deny/abstain 无 allow」语义照搬；本仓不需要其并行批执行。
3. **spill-policy 227 行=R42 完整验收规格**：委托先行/read 豁免防回环/通知预留进
   预算/尽力而为/装载期校验五条边界语义逐条转测试向量。
4. schema 双投影（native+PTC 同源生成）印证 R40：工具目录必须生成不可手写。

## 施工图要点（R38–R40 实现级）

- R38：ToolDef trait（meta/available/execute/mutation_kind/is_concurrency_safe）+
  OnceLock 静态表 + ToolCtx 借用注入（沿 202-204 号借用纪律）；**六批 family 迁移、
  每批双跑一致性护航禁一次性 flip**；debug/tools 端点（parameters 键排序规范化 sha256）
  + 差分器工具面维度（首跑出豁免清单再出清，沿 487→491 节奏）；TS 侧新增
  buildChatToolSet 统一出口不强改 32 工厂。
- R39：pre（loopback/segment 归一 ToolGuard+Ask 闸位预留本轮 Deny 实现）→
  around（tokio timeout 协作取消+is_retryable 重试对齐 R25 链不计 failover+
  RunLog 记 took_ms/attempt/timed_out/error_kind=**清偿 streaming_client 备案**）→
  post（PostHook 派链，首消费 R42）；LoopToolExecution 扩正交上报四字段。
- R40：三目录生成（tools=双端活体、endpoints=Rust 四层工厂穷举测试+常量数组、
  SSE=双端常量单一事实源）docs/catalog/ 入库+verify 挂 gate:ts 第 8 步；
  next-change-no.mjs（当日目录+git log+R 段占用表三源）。

## R41–R43 深化 + 封界

R41 SurfaceFold O(1) 追加/O(surface) 快照+usage 锚点三态，接线 prepare_write_input
（函数名锚点——**并行会话正在改 write_next.rs，行号以 543 号 HEAD 快照仅作导航**）；
R42 落盘 0700/wx/随机名+read/ls/grep 豁免+工件差分器存在性对照；R43 会话 JSONL
九事件词汇+derive_messages golden+「模型可见即已记录」debug 断言+差分器会话面。
R31 传输层守卫 vs R39 工具层守卫两套向量两份文件；R41 排 R33 之后（沿 543 号修订序）。

## 验收与状态

纯规划轮：无代码改动，无门禁义务。施工批次：R38a（registry+两 family+差分维度）
→R38b（余四 family+TS 出口）→R39（管线）→R40（目录+编号脚本），每批独立编号、
门禁 8 项+新维度全绿收口；R41–R43 按 M2 逐项立项。下一号自 545 起。
