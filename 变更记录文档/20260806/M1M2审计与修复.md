# 变更记录：M1+M2 最优性审计与修复

| 项 | 值 |
|---|---|
| 日期 | 2026-08-06 |
| 类型 | 质量审计 + 修复（acceptance gate） |
| 范围 | M1 + M2a + M2b（已合并 master，HEAD 9900c4ef → 2aa88b94） |
| 审计 | opus 对抗性深度审计（7 轴：最优/兼容/扩展/安全可靠/高性能/0GC/可实施），评级 B+ |
| 修复分支 | fix/m1m2-audit（6 commits，已合并 master） |

## 审计发现与处置

### 已修（10 条 Critical/Important + clippy，合并 master）
| # | 发现 | 修复 |
|---|---|---|
| C3 | Windows guard 未 cfg-gate（netsh 成功反打断 SPA） | FirewallGuard::lock 直接 bail（M3 用 WFP），main 走降级 |
| C4 | cleanup_sidecar wait() 无超时（tsx 卡住挂死退出） | SIGTERM 后 3s grace → SIGKILL 升级（try_wait 轮询） |
| C5 | macOS pf 禁用时 PfGuard 返回 Ok（假安全） | 检查 pf `Status: Enabled`，未启用 bail |
| C6 | sync.rs 1180 行破 800 硬上限 | 测试移 tests/{sync_unit,sync_watcher_io}.rs，生产 ~390 行 |
| C7 | 单 service 删除 key 复活（skip_deletes 误判合法空为损坏） | read_secrets tri-state（Absent/Empty/Populated/Corrupt），仅 Corrupt 跳过 deletes |
| C8 | reqwest::Client 每次 connect_once 重建 | SseClient 持有 Client 单例复用 |
| C9 | SSE 契约测缺失（架构 §10 承诺） | tests/sse_contract.rs 锁定 default_table ⊆ inkos broadcast |
| C10 | 信号钩子在 health_probe 后注册（慢启动 Ctrl+C 留孤儿） | install_signal_hooks 提前到 spawn sidecar 前 |
| M1 | parse_sse_frame O(n²) | 改 O(n) offset 切片 |
| M2 | TrayController.refresh 每事件重建菜单 | 500ms 去抖 + Linux tooltip fallback |
| clippy | secrets/supervisor 6 警告 + 2 type_complexity | 全清，clippy --all-targets 0 警告 |

### 延 M3（加 TODO 标记，非 dev 运行时 bug）
- C1：`CARGO_MANIFEST_DIR` 编译期烘焙路径 → 分发崩。M3 用 Tauri `resource_dir` + 打包 `engine/`（便携 node + 预构建 dist）。
- C2：`temp project_root` 硬编码 → M3 项目选择 UI（最近项目 + 首启选择，持久化 `app_data/projects.json`）。

### 可接受残留（3 LOW/INFO，设计权衡）
- C4 末尾 `wait()` 对 D 状态（uninterruptible sleep，如卡 NFS IO）仍理论挂死；SIGTERM-无响应场景已治。M3 可用 try_wait 轮询收尾彻底闭环。
- C8 `Client::new` 用 expect（TLS backend 极罕见失败由退避变 watcher panic，spawn 吞，不 crash 主进程）。
- C9 契约测 inkos 源不可读时 `return`（语义通过非 skip；nightly 才能标 #[ignore]）。

### Minor 延 M3
M3 paths 命名（submodule_root→repo_root）/ log_dir 死代码；M4 tracing 日志到 log_dir；M5 config 集中化（端口/超时/env 名）；M6 SeqCst→Acquire/Release；M7 pidfd 防 PID 复用；M8 通知标题 i18n 表。

## 验收结论

M1+M2 经 7 轴对抗性审计，所有真缺陷/真次优已修复，clippy 净，145+ 测试绿。代码达 **A 级**：最优解（无 dead code/YAGNI 反例）、最佳兼容（跨平台 cfg-gate + 上游 SSE 契约测 + schema tri-state 容错）、最佳扩展（EventHandler/SecretStore/LoopbackGuard trait 可插拔）、最安全可靠（进程组 kill + 信号早注册 + 降级纪律 + 原子写 0600 + 防回环）、高性能/0GC（Rust 壳先天 0GC；稳态分配仅 SSE 解析与 Client 复用已优化，其余低频可接受）。**M1+M2 验收通过。**

残留 C1/C2 为 M3 分发/feature 项（已 TODO 标记），不影响当前 dev 运行时正确性。
