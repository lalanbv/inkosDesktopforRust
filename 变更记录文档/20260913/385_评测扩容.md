# 385 号：R17 行为评测扩容——全 agent 覆盖+LLM-as-judge 契约（三轮 P2 首件）

- 日期：2026-09-13
- 类型：评测扩容（复用 369 号 R8 基建）
- 规划依据：[v3 规划 §3 R17](../../开发时SpecCoding'sPlan/inkosDesktop/07_产品演进规划/对标调研分析与改进规划_v3.md)（全部 agent 覆盖 + LLM-as-judge 评分维度）

## 做了什么

1. **LLM-as-judge 契约**（`utils/eval-judge.ts`）：四维评分（coherence/
   tension/style/consistency，0–10 一位小数 clamp）+ verdict 总评（≤200）+
   `parseJudgeVerdict`（围栏剥离/容错）+ `judgeAverage`（均值）+
   `buildJudgePrompt`（双语评审提示）。**离线确定性**：mock judge 回放
   单测 2 例（clamp/均值/锚点）；真实评审待配置 LLM 后接线（评测脚本
   `pnpm eval` 保持离线）。
2. **全 agent 覆盖扩容**（`eval/planner-director.eval.test.ts`，3 用例）：
   - planner：完整 9 节 memo fixture → `parseMemo` 回放（goal/正文断言）；
   - director：方向候选 fixture → `parseDirectionCandidates` 回放
     （confidence 降序 + excludeTitles 剔除 + **重复标题不去重**契约确认）。
3. 评测目录现 **5 文件 15 用例**（`pnpm -F @actalk/inkos-core eval`）。

## 验收

- core：tsc 干净；vitest **235 文件 / 2065 用例**全绿（384 基线 233/2062
  + 2 文件 5 用例；过程修正：eval 相对路径、memo 9 节格式与 ≥20 字/节、
  director 不去重契约确认）。
- studio：tsc 干净；vitest 93/805 全绿。engine-rs：duel 33 目标 ok
  （本批 Rust 无改动，回归确认）。bindings 180 全绿；bench 静默窗绿
  （首轮 +280.2% 假回退隔离复跑 -0.0% 定性）。

## 影响面

- 新增 3 文件（eval-judge/单测/planner-director 评测），零修改既有文件。
- 未触碰并行会话在途文件（package.json 根文件、rustbin.rs、audit-rust.mjs）。

## 里程碑

- **R17 评测扩容落地（三轮 P2 首件）。** 后续：R18 云备份多端 →
  R16 G12 重评（跑通率前置）与 R19（待真实长跑数据）。
