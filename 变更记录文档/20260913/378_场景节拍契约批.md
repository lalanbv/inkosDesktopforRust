# 378 号：R11 契约批——场景节拍驱动写作（三轮 P0 第二件）

- 日期：2026-09-13
- 类型：契约层 + 双端镜像 + golden 差分
- 规划依据：[v3 规划 §3 R11](../../开发时SpecCoding'sPlan/inkosDesktop/07_产品演进规划/对标调研分析与改进规划_v3.md)（Scene Beats——章内 scene 粒度节拍驱动生成，对齐 Novelcrafter Scene Beats 工作流）

## 做了什么

1. **SceneBeat/SceneBeatPlan 双端同形**：`{id, title ≤60, description ≤200,
   exitHook? ≤120}`；场景 clamp ≤6；解析（围栏剥离+首个 JSON 对象）+
   clamp+空行跳过+全无效 undefined；行 id 缺省 s1..sN。
2. **提示词构造双端**：`buildSceneBeatsPrompt`（planner 产节拍 JSON，
   zh/en）+ `buildSceneBeatsWriterBlock`（writer 注入块：节拍顺序清单+
   exit 标注+「不得跳过或合并节拍」纪律行）。
3. **开关**：`book.writing.sceneBeats`（TS zod + Rust BookWritingConfig，
   默认关——关闭时行为与既有链路一致）。
4. **golden**：`scene-beats-vectors.json`（parse×3 含 clamp 与无效、
   prompt×2 双语锚点、writerBlock×2 含 exit 标注）；TS 3 用例与 Rust
   3 差分读同文件全绿（第二十五守门域）。

## 接线预告（379 号）

planner 调用点挂 buildSceneBeatsPrompt（开关开启时）→ parse → writer
prompt 注入 buildSceneBeatsWriterBlock；本批为纯契约+开关。

## 验收（golden×8 + duel + 双端全绿）

- core：tsc 干净；vitest **232 文件 / 2060 用例**全绿（377 基线 231/2057
  + 1 文件 3 用例）。
- engine-rs：`INKOS_DUEL=1 cargo test` **32 个测试目标全 ok**（+1 新增
  差分；writing.sceneBeats 开关字段触发 golden_leaf 等多处构造点补齐——
  含批量脚本误插两处由编译器抓出修正）。
- studio：tsc 干净；vitest 802 全绿。bindings **180** 全绿；bench 静默窗
  通过（首轮 +524.9% 假回退与负载拒跑后隔离复跑绿）。

## 影响面

- 新增 4 文件（TS 契约/golden 测试/共享向量/Rust 镜像+差分），修改
  5 文件（scene_beats/mod.rs/book.ts/book.rs/index.ts）。
- 未触碰并行会话在途文件（package.json 根文件、rustbin.rs、audit-rust.mjs）。

## 里程碑

- **R11 契约批落地（三轮 P0 第二件）。** 接线批（379 号）挂
  planner/writer 调用点后 R11 收官；随后 R12 sqlite-vec（P0 末件）。
