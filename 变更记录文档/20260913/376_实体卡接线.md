# 376 号：R10 接线批——实体卡注入+存储端点（三轮 P0）

- 日期：2026-09-13
- 类型：管线接线 + 端点（双端镜像）
- 规划依据：[v3 规划 §3 R10](../../开发时SpecCoding'sPlan/inkosDesktop/07_产品演进规划/对标调研分析与改进规划_v3.md)；契约层见 [375 号](./375_实体卡契约批.md)

## 做了什么

1. **G2 层级注册双端**：`codex/` source 前缀 → user-reference（40，正典
   事实参考可压缩）；golden context-priority tier 组追加 1 例。
2. **composer 注入双端**：`loadCodexEntries`——读
   `story/entity_codex.json`（缺失/解析失败空数组零打扰），以
   goal+memo 正文为场景文本 `matchCodexCards` 命中（取前 8 卡）→
   `renderCodexBlock`（source=`codex/<首卡名>`）并入优先级排序链。
3. **存储端点双端**：`GET/PUT /api/v1/books/:id/codex`
   （story/entity_codex.json `{version:1, cards[]}` 全量读写；PUT 校验
   cards 数组）。
4. **UI/名册派生**：377 号收尾（本批上下文受限，卡片编辑面板与名册确认
   自动派生留待；存储/注入/端点链已闭环——手工编辑 entity_codex.json
   即可生效）。

## 验收（duel + 双端全绿）

- core：tsc 干净；vitest **231 文件 / 2057 用例**全绿（G2 golden tier +1）。
- studio：tsc 干净；vitest 91 文件 / 802 用例全绿。
- engine-rs：`INKOS_DUEL=1 cargo test` **31 个测试目标全 ok**。
- bench/bindings 未重跑（本批无热点代码与新增导出变更；375 基线有效）。

## 影响面

- 修改 6 文件（context-source-tier 双端+golden/composer 双端/server.ts/
  books_state_routes.rs/mod.rs）。
- 未触碰并行会话在途文件（package.json 根文件、rustbin.rs、audit-rust.mjs）。

## 里程碑

- **R10 接线主链落地**（注入+端点）；UI 编辑面与名册派生留 377 收尾。
  此后 R11 场景节拍（v3 P0 第二件）。
