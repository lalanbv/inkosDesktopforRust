# 379 号：R11 接线批——planner 产节拍+writer 感知链路（R11 收官）

- 日期：2026-09-13
- 类型：管线接线（双端镜像）
- 规划依据：[v3 规划 §3 R11](../../开发时SpecCoding'sPlan/inkosDesktop/07_产品演进规划/对标调研分析与改进规划_v3.md)；契约层见 [378 号](./378_场景节拍契约批.md)

## 做了什么

1. **TS planner 接线**（`agents/planner.ts`）：`writing.sceneBeats` 开启时，
   renderIntentMarkdown 之后追加二次 LLM 调用——`buildSceneBeatsPrompt`
   （goal/outlineNode/3 拍）→ `parseSceneBeatPlan` → 
   `buildSceneBeatsWriterBlock` 追加到 intentMarkdown 尾部 → 写盘。writer
   经 chapterIntent 消费该块**零改动**（379 设计核心：节拍块进 intent 即
   全链生效，含 persisted plan 重放）。失败/关闭零打扰。
2. **Rust planner 接线**（`agents/planner.rs` plan_chapter 内同位置同构）；
   `PlanChapterInput` 增 `book_writing: Option<&BookWritingConfig>` 传递
   开关（write_next/books_routes/测试构造点全链补齐）。
3. **执行语义**：节拍块随 persisted plan 保存——手动审查模式/重放/续跑
   一致看到同一节拍集；audit 沿既有维度审查，不新增审查面。

## 验收（duel + 双端全绿）

- core：tsc 干净；vitest **232 文件 / 2060 用例**全绿。
- studio：tsc 干净；vitest 91 文件 / 802 用例全绿。
- engine-rs：`INKOS_DUEL=1 cargo test` **32 个测试目标全 ok**（PlanChapterInput
  测试构造点补 book_writing 由编译器抓出）。
- bindings **180** 全绿；bench:gate 静默窗通过（title_dedup +280% 假回退
  隔离复跑回基线 -0.0% 定性，未动基线）。

## 影响面

- 修改 4 文件（planner.ts/planner.rs/write_next.rs/books_routes.rs）。
- 未触碰并行会话在途文件（package.json 根文件、rustbin.rs、audit-rust.mjs）。

## 里程碑

- **R11 场景节拍驱动写作完整收官**（378 契约 + 379 接线）——**三轮 P0
  两件（R10 实体卡 / R11 场景节拍）全部收官**。下一步：R12 sqlite-vec
  真向量检索（P0 末件）。
