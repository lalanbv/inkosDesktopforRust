# 538 号：全量门禁复验与 536 号注释撞号清偿——「全量测试/代码审查/真人模拟」会话目标收官

日期：2026-09-21
提交：15ab0dce（注释清偿）+ 925b1242（fixture 清单纠偏）+ 本文件同批路径限定提交
类型：验证+fix(engine/scripts)，对 536 号 HEAD（9e8c9b0a）全量复验

## 选题

会话目标「全量测试、代码审查、模拟真人测试、确保功能正常没有 BUG」。
起点=536 号 HEAD、工作区干净、其上无新落提交（复验中途并行会话入库 537 号，
见下）。全程单线跑门禁，未同机并行重负载（531 号教训遵守）。

## 门禁矩阵（全绿）

- TS：`pnpm -r build` ✓ → `pnpm -r typecheck` ✓ → `pnpm -r test` ✓
  **core 248 文件 2124 + studio 117 文件 894 + cli 40 文件 218 = 3236 用例
  零失败**（日志中 sub_agent "failed" 行均为错误路径 mock 预期输出）；
- `node scripts/audit-npm.mjs`：**0 advisory**；
- 活体套件：`node-fallback-smoke.mjs` 双引擎腿全绿（fixture 一致性/director
  PUT{patch}/task-routing 往返/SPA+API no-store/run-log 计数）；
  `engine-contract-diff.mjs` **41 端点 0 分歧**（落盘工件内容面零备案硬门禁过；
  BM25 恒定比例因子 1.1646 持续按 528 号豁免）；`export-epub-smoke.mjs`
  双引擎结构校验绿；
- Rust：engine-rs `cargo test --all-targets` **1807 用例零失败**（统计落盘
  解析，无管道截断）；src-tauri **576 用例零失败**；`pnpm clippy:gate`
  双 crate **0 告警**；
- `INKOS_DUEL=1 cargo test --test strangler_duel`：**10/10**；
- `pnpm verify:engine-bindings`：**186 导出不变**；
- `pnpm bench:gate`：**9/9 基准全部阈值内**（首轮 +478%~542% 三项漂移为
  负载噪声，见教训 3）。

### src-tauri 576 vs 578 口径闭环

536 号记录记「src-tauri 578 绿」，本轮 `--all-targets` 实测 576——补跑
`cargo test --doc` 得 **2 个 doc-test**：576+2=578，纯计入口径差，零失败，
无回归。

## 代码审查（532→536 提交面独立复审）

- **535 号**（studio ToolExecutionSteps）：通过零缺陷——防御式解析完备
  （kind 门/trim/空 skillId 拒/path 必填）、正确消费 534 段级元数据、
  use_skill 入 pipeline 白名单展示为主卡合理；与并行会话复审结论一致；
- **536 号**：坐实 **8 处注释撞号**（该批开档自编 535、写档时撞号改 536，
  代码注释未随改）——agent_route.rs 2 + skills/mod.rs 3 +
  production_bindings.rs 3，全部为该批自身改动自引用，git 考古会误导航到
  ff9c86e7（studio 面）。与 534 号注释撞号同类缺陷（536 号自己清偿过
  534 的 3 处，却漏了自己这 8 处）。**15ab0dce 清偿**；
- **537 号**（并行会话，a28e088a，复验中途入库）：Rust 零引用 pub 函数
  扫描——with_segment_guard 真死删除+三处备案注释。clippy:gate（-D
  warnings，对含其 WIP 的合并树跑过）证实删除后编译零引用成立，通过；
- 536 号 Rust 改动本体（turn_seed 预置/Vec 首插序/原位替换）复审无新缺陷，
  语义与 TS Map/Set 对齐正确，测试断言覆盖预置/原位更新/追加/轮末丢弃。

## 模拟真人 GUI 测试（浏览器黑盒自动化，5 测试点全过零功能缺陷）

生产形态走查环境（`walkthrough-env.mjs`：mock LLM + inkos-engine-server
静态服 studio dist + fixture 书「镜花水月」），截图证据存
`gui-test-screenshots/`：

- **T1 首页**：写作数据图表/运行遥测「累计 11·失败 0」与 fixture 一致/
  SSE 实时绿点，深色渲染无错位；
- **T2 伏笔池**：分类 chips（全部/悬念/物品/情感/世界观）+ 4 条台账
  彩色 kind 徽标；点「悬念」4→1 条收窄正确；
- **T3 承诺账本**：H04（已开启）置顶排序正确、kind 徽标/开启·推进·期望章
  计数完整；「悬念」过滤 4→1（H01）正确；
- **T4 市场雷达（533 号复验闭环）**：侧栏点击→URL 变 `#/radar`；
  `reload()` 后保持 `#/radar` 且完整渲染（面包屑+扫描历史，截图留证）；
  历史条目回放→市场概要+2 推荐卡→「从该选题开书」→跳 `#/book/new` 且
  输入框预填推荐卡完整选题文案（508b→512 链活体验证）；
- **加测 ⌘K 命令面板**：combobox 展开，「最近访问」准确反映本次导航序
  （新建书籍/市场雷达/镜花水月…）；
- **发现 1 处文档缺陷（非功能）**：`walkthrough-fixture.mjs` 走查清单
  第 3 条称设置页「同页顶部运行遥测」——RunLogPanel 实挂 Dashboard
  （R26/403 号移挂），清单措辞过期。925b1242 纠偏。

## 提交

- `15ab0dce` fix(engine)：8 处「535 号」注释改「536 号」（路径限定 3 文件）；
- `925b1242` fix(scripts)：walkthrough-fixture 清单第 3 条纠偏。

## 教训

1. **撞号清偿要照镜子**：536 号清偿了 534 的注释撞号，自己留下 8 处同类
   ——改号动作必须全量 grep 新旧两号（`旧号` in code + `新号` in code
   双向核对），只改记忆中的"知道的那几处"必漏；
2. **GUI 走查清单会随功能搬家过期**：fixture 清单声称的面板位置与组件
   实际挂载点脱节（RunLogPanel 设置页→Dashboard）——清单条目宜标注
   挂载组件名，走查发现"清单说的位置没有"先查源码定性再下缺陷结论；
3. **bench 噪声教训三度证实**（257/391 后）：本轮三项 +478%~542% 漂移，
   低谷隔离复跑全部回基线（-8.9%/-3.8%/-11.5%）——噪声源是会话宿主自身
   （ZCode Renderer 浏览器面板渲染 100%+ CPU）+ Spotlight 开机索引风暴；
   GUI 测试与 bench 同机 sequencing 时，**先关浏览器面板再跑 bench**；
   严禁 --update 洗基线；
4. **并行会话三重警戒全程有效**：开工查目录+git log、改动期辨 workload
   归属（工作区出现 537 号 WIP 立即识别避让）、收口前最后一刻再查号
   （537 中途入库占号，本号改取 538）。

## 门禁豁免备案

无。bench 本轮实测通过，无需豁免（536 号对聊天路由 skill 解析的豁免
备案继续有效）。
