# 211 号：inkos.log 截头轮转——210 号 backlog 实施（双端对齐）

- 日期：2026-09-07
- 分支：develop
- 关联：210 号（日志通道落地——轮转为其遗留 backlog）
- 推送核验：origin/develop 仍停 6bc65564——**201–210 十提交均未推送**；本批次后本地领先 11。两项默认值无新答复。

## 一、实施

210 号落地的日志通道是纯 append——长跑无上限增长。本批补**截头轮转**（双端同语义）：

- **上限**：512KB 触发，按行边界保留尾部 256KB（get_logs 只取尾 100 行，截头零信息损失）；temp+rename 原子替换；上限/保留窗参数化（供测试注入小值）。
- **时机**：每次 append 前检查（一次 `metadata`/`statSync` 调用，开销可忽略）；**轮转失败静默不阻断追加**（最坏情况只是文件继续增长）。
- Rust `utils/log_file.rs::rotate_if_oversized`（`append_log_event_checked` 开头调用）；Node `logger.ts::rotateIfOversized`（`createFileSink.write` 内 append 前调用）。

## 二、验证

- Rust 单测 +1（`rotates_head_when_oversized`：40 行超限文件 → 轮转后接近保留窗、首部行被截、尾行保留、所有行仍完整 JSON；未超限不动文件）。
- Node 测试 +1（注入 400B/200B 上限，12 条写后：文件有界、line-0 被截、line-11 在、行完整可解析）——断言语义注意：轮转在 append **前**检查，文件最坏 = 上限 − ε + 单条余量（这是有界且自愈的，非缺陷）。
- 全门禁：engine lib **1286**（+1）/ clippy 零告警 / duel **10/10** / core vitest **198 文件 1879**（+1）/ studio vitest **89 文件 790** / 双端 typecheck。

## 三、结论

日志通道三件套齐备（210 写入 + 211 有界）：任务级失败持久可诊断、磁盘占用有上界、截头零信息损失（读侧只看尾部）。
