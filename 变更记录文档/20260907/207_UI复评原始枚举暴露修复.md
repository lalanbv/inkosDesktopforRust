# 207 号：UI/产品面缺口分析（第二轮复评）——原始枚举暴露修复

- 日期：2026-09-07
- 分支：develop
- 关联：197 号（首轮 UI 复评）、195 号（doctor 预警——state-degraded 可见性延伸）、198 号（bookId 暴露同类先例）
- 推送核验：origin/develop 仍停 6bc65564——**201–206 六提交均未推送**；本批次后本地领先 7。两项默认值无新答复。

## 一、走查（第二轮，距 197 号隔 9 批次）

生产形态栈：engine bin（INKOS_STATIC_DIR=studio/dist 直连 4569）+ 富 fixture（3 章含 state-degraded / timeline 双线 / 中文书名）。覆盖面：Dashboard（193 回填 CTA ✓ 在位）、书籍工作台、书籍设置页（189 autoBeats 开关 ✓ / 197 审查按钮 i18n ✓）、章节路由、doctor 入口、视觉截图（布局无重叠溢出，analyze_image 判读）。

### 问题清单

| # | 问题 | 严重度 | 处置 |
|---|---|---|---|
| 1 | **genre 原始枚举暴露**：书卡/设置页头部显示「OTHER」（`{book.genre}` + CSS uppercase） | 中（用户可见，198 号同类） | **本批修复** |
| 2 | **章节状态 `state-degraded` 无本地化**：设置页章节表第 3 章显示「STATE-DEGRADED」大写，同列其他状态（待审核/已通过）均已本地化——`translateChapterStatus` 映射缺 8 个后端枚举值 | 中 | **本批修复** |
| 3 | **chapter 页深链缺失**：`page:"chapter"`（及 analytics/truth/doctor 等）是 state-only 路由——`HASH_PAGES` 集合不含、`routeToHash` 无映射；章节阅读器刷新即回首页、URL 无法分享 | 中（内容页产品缺口） | **backlog** |

## 二、实施

### 章节状态 i18n 全集（BookDetail.tsx + use-i18n.ts）

- `translateChapterStatus` 补齐后端 `ChapterStatus` 全集 8 个缺映射：`state-degraded`/`card-generated`/`drafting`/`auditing`/`audit-passed`/`revising`/`rejected`/`published`（i18n 键 `chapter.*` 双语）。
- `STATUS_CONFIG` 补 4 个配色：state-degraded（琥珀 + AlertTriangle，呼应 195 号预警色系）、audit-passed（绿 + ShieldCheck）、rejected（红 + X）、published（紫 + BookOpen）。

### genre 显示名映射（新 lib/genre-labels.ts）

- `GENRE_LABELS` 覆盖 `packages/core/genres/` 内置题材全集 15 项（id → zh/en 双语名），未知 id 回退原值。
- Dashboard 书卡与 BookDetail 头部 chip 接入：**语言跟书语言**（中文书 → 「其他」「都市」，英文书 → Other/Urban——genre 语义属于作品而非 UI），同时移除 chip 的 CSS uppercase（本地化名不做大写变形）。
- ImportManager 的内联 4 项 genre 选项未动（功能面不同，避免本批扩散）。

## 三、验证

- studio typecheck（双 tsconfig）✓、vitest 88 文件 786 用例全过、dist 重建。
- 浏览器复验（生产形态 engine + 新 dist）：设置页 genre「OTHER」→「其他」、第 3 章「STATE-DEGRADED」→「状态降级」（琥珀 chip）；EN 界面切换后 UI 文案英文化、书卡 genre 按书语言保持中文——符合设计意图。
- **冒烟排障备忘**：engine 静态面 `with_static_face` 启动时一次性读缓存 index.html（双端同款设计，生产 dist 固定无碍）——**dev 直连场景 rebuild 后须重启 engine**，否则 index.html 引用旧 hash 资源 404 → 白屏（本次复验首跑踩到，重启即恢复；非缺陷，记录为冒烟操作规范）。

## 四、backlog（本轮新增）

- **chapter 深链**（问题 #3）：`#/book/:id/chapter/:num` 三处补齐（parseHash regex + routeToHash 分支 + HASH_PAGES 集合）；analytics/truth/doctor 等系统页维持 state-only（doctor 前例）。
- 「节拍沉淀成功无主动 UI 提示」——维持等真实使用反馈（192 号既定结论）。
