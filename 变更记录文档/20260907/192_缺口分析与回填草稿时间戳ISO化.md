# 192 号：新一轮缺口分析 + 回填草稿时间戳 ISO 化（用户可见缺陷修复）

- 日期：2026-09-07
- 分支：develop
- 关联：184/190 号（回填向导与写入粒度）、189/191 号（节拍沉淀双端）

## 一、缺口分析（192 号复评结论）

推送核验：origin/develop 仍停在 173 号（37958463），本地领先 19 提交，推送仍待用户在 Fork 图形端操作。

本轮自主复评（真实写作流暂无新用户反馈，以下为代码面/体验面扫描所得）：

1. **【本批修复】回填草稿时间戳 epoch 毫秒**：Rust `extract` 生成草稿时 `updated_at` 用 `utc_now_ms()`，该值会被 `apply` 原样渲染进 `series_backfill.md` 头部的「抽取于 …」（190 号 E2E 实测观察到 `抽取于 1788729154010`）。Node 端 extract 本就使用 `new Date().toISOString()`——单端缺陷、用户可见。
2. **【backlog·低优】回填向导入口可发现性**：Dashboard 的「回填设定」快捷入口仅在系列分组 header 出现（书须有 `series` 字段）；散书用户只能从导入工具页（`#/import/backfill`）进入。可考虑给无系列书的项目也提供常驻入口。
3. **【backlog·低优】节拍沉淀成功无主动 UI 提示**：开启 `autoTimelineBeats` 后沉淀结果只在时间线页可见（write-next 完成横幅不提及）。可接受，待真实使用反馈决定是否加提示。
4. **【backlog·可选】抽取 prompt 类别扩展**：目前固定 worldview/character/plot/style 四类（184 号语义）。
5. 代码面 TODO 扫描：仅 `models/play.rs` 存在 TODO，无紧急项；192 号不处理。

## 二、本批变更（改进项 1）

- **`engine-rs/src/models/series_backfill.rs`**：`parse_items_text` 的 `updated_at` 由 `interaction::session::utc_now_ms()`（epoch 毫秒字符串）改为 `utils::utc_time::utc_now_iso()`（ISO 8601，与 Node extract 的 `toISOString()` 双端对齐）；注释说明缘由。
- **测试**：`parse_items_text_extracts_json_from_fenced_output` 追加 updatedAt ISO 格式断言（`20` 前缀 + `T` + `Z` 结尾）。
- **兼容性**：既有旧草稿文件中的 epoch 值不再改写（草稿为短生命周期检查点，重新抽取即愈；不改渲染函数以维持 Rust/TS/Node 三方渲染逐字对齐）。duel normalize 本就剥除 updatedAt，契约面无影响。

## 三、验证

| 门禁 | 结果 |
| --- | --- |
| engine-rs cargo test（全目标） | 1267 lib + 194 + 70 + 10 全绿 |
| clippy --all-targets -D warnings | 零告警 |
| duel（INKOS_DUEL=1） | 10/10（updatedAt 属归一剥离字段，无契约分歧） |
| Node 端 | 零改动（本就正确） |

## 四、后续

backlog 新增 2/3/4 项见上；下批优先级随用户真实写作反馈调整。
