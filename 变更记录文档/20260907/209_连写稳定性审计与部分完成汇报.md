# 209 号：连续写作（多章连写）稳定性审计——章间失败的部分完成汇报

- 日期：2026-09-07
- 分支：develop
- 关联：175 号（abort 阶段边界）、202 号（装配循环外化——内存面前提）、205 号（LLM 瞬时重试——失败率前置缓解）
- 推送核验：origin/develop 仍停 6bc65564——**201–208 八提交均未推送**；本批次后本地领先 9。两项默认值无新答复。

## 一、审计（agent_production 多章循环 + Node writeChapters 对照）

| 面 | 结论 |
|---|---|
| **abort 检查点** | 覆盖充分：循环头检查 + 章内阶段边界（175 号 config.abort 透传）双保险 ✓ |
| **内存增长** | 无问题：`ChapterPipelineResult` 不含正文（仅号/题/审计遥测）；agents/ctx 循环外装配复用（202 号）✓ |
| **keep_going 停止语义** | 已完善：非 ready-for-review 停止并结构化汇总（stoppedStatus/details）✓ |
| **章间失败处理** | **双端共同缺陷：一章失败 `?`/throw 直接终止——错误信息丢弃已写 k 章的成果汇报**（章已真实落盘，用户只见裸错误，不知道前几章资产存在） |

## 二、实施（双端同语义）

### Rust（agent_production.rs）

- 循环内 `.map_err(...)` 增强为 `chapter_error_text_with_partial`：results 非空时错误文本变为「已完成 k/n 章并落盘（第 X 章至第 Y 章）；后续章节失败：<原错误>」；首章前失败维持原文案（不添噪音）。
- 循环头 abort 同步增强 `abort_text_with_partial`：中途中止携带「已完成 k 章并落盘」摘要。
- 两个纯函数 helper + 2 单测（空成果原文案 / 3 成果中英摘要断言）。

### Node（pipeline/runner.ts writeChapters）

- 同语义 `withBatchProgress`（模块级 helper）：`_writeNextChapterLocked` 失败与 `throwIfOperationAborted` 中止两处包裹——已有成果时错误消息前缀「Completed and persisted k/n chapter(s) (chapters X-Y); a later chapter failed/operation aborted: <原错误>」；首章前原样抛出。
- 测试 +2（chapter-batch.test.ts）：第 2 章失败错误含 `1/2 chapter(s) (chapters 11-11)` 摘要；首章失败错误保持 `^plan chapter failed$` 原样。

## 三、验证

- 全门禁：engine lib **1284**（+2）/ 集成 **194+70+10**、clippy --all-targets -D warnings 零告警、INKOS_DUEL=1 strangler_duel **10/10**、core vitest **197 文件 1876 用例**（+2）全过、core typecheck ✓。
- 本批次纯文本增强（错误消息层），无 IO/协议变化——单测直接覆盖语义，无需浏览器冒烟。

## 四、结论与遗留

连写稳定性三面（abort/内存/失败处理）审计闭合：前两面健康，失败汇报缺陷双端修复。错误文本形态双端一致（zh/en 各自完整）。后续可选：把部分完成信息建模进结构化 details（当前 `is_error` 错误路径无 details 面——错误路径的 ToolOutcome 结构是另一改动面，等真实需要再议）。
