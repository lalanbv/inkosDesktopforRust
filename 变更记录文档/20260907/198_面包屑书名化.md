# 198 号：章节工作台面包屑书名化——消除 bookId 暴露（197 号复评 backlog）

- 日期：2026-09-07
- 分支：develop
- 关联：197 号（UI 复评问题 2）、163 号（导航一致性维度）

## 一、变更内容

- **`pages/ChapterReader.tsx`**：
  - 新增 `useApi<BookData>("/books/{bookId}")` 取书籍配置；
  - 面包屑中段由原始 `bookId` 改为 `bookData?.book.title ?? bookId`——书名加载完成前回退 id（避免闪烁/失败态），保留 `max-w-[120px]` truncate。修复点即 197 号走查问题 2：「Books / b1 / # 1」与顶栏面包屑「冒烟书 / Chapter 1」不一致。
- **测试**（`ChapterReader.test.tsx` +2）：
  - 书籍配置就绪 → 面包屑渲染「冒烟书」且不出现裸 `>b1<`；
  - 配置未加载 → 回退显示 bookId（渐进增强语义固定）。

## 二、验证

| 门禁 | 结果 |
| --- | --- |
| studio typecheck（双 tsconfig） | 干净 |
| studio vitest 全量 | 88 文件 785 用例全绿（+2） |
| 浏览器复验（vite → Rust 引擎） | 章节工作台面包屑「Books / 冒烟书 / # 1」，bookId 不再出现 |

## 三、后续

backlog：仅余「节拍沉淀成功 UI 提示」（等真实使用反馈）。
