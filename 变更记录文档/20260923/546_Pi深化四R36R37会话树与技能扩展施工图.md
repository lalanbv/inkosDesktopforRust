# 546 号：Pi 对标深化四——R36 会话树 + R37 自扩展技能施工图

- **日期**：2026-09-23
- **类型**：docs(规划)——只出设计稿与施工图，零代码改动
- **上游证据**：earendil-works/pi `packages/coding-agent/src` 源码实读（session-manager.ts 2010 行 / skills.ts 509 行 / core/extensions/ 7 文件 / migrations.ts 315 行），GitHub raw 拉取，工作副本 /tmp/pi-r4/
- **产出**：`开发时SpecCoding'sPlan/inkosDesktop/07_产品演进规划/对标调研深化四_R36R37会话树与技能扩展施工图.md`

## 一、上游实体实锤（本轮新提取）

1. **会话树**：11 种条目类型全部继承 `{type,id,parentId,timestamp}`；`branch(branchFromId)`=纯内存 leaf 指针移动（文件树不动，append 时 parentId=当前 leaf 成新枝）；`branchWithSummary` 追加 `branch_summary{fromId,summary}` 桥接弃用路径上下文；**leaf 不落盘**（分支持久性靠 branch 后必 append）。
2. **CompactionEntry**：`summary + firstKeptEntryId（树内稳定引用，非下标）+ tokensBefore + systemMessage（边界完整 prompt/工具状态快照）+ details/usage/fromHook`。
3. **迁移先例**：v1→v2 加载时 in-place 顺序链化补 id/parentId + firstKeptEntryIndex→Id 换算；v2→v3 仅 role 改名；header.version 递进幂等。
4. **技能面**：SKILL.md frontmatter name≤64/description≤1024/**与本仓 AgentSkillSchema 限制逐字一致**（双源趋同零对齐成本）；`formatSkillsForPrompt` 渐进披露（系统提示只投 name/description/location 三元组，正文模型按需 read）；装载序 user→project→显式路径、同名首胜+collision 诊断；`disable-model-invocation` 对模型隐藏。
5. **扩展宿主**：jiti 运行时 TS 加载=全信任进程内执行，印证 v6 不采纳裁决（InkOS 声明式权限+WASM 为安全优势位）。

## 二、本仓现状写实（对照基线）

- transcript：`sessions/{id}.jsonl` appendFile-only；6 事件判别联合；**MessageEvent 已带 uuid/parentUuid（消息粒度链原料在），事件级树语义无**（无分支 API/head 指针/恢复=线性回放）；request 三事件包装为上游没有的语义层。
- 技能：装载时点=会话创建（agent-session.ts:1056）——**会话粒度天然热载，进程内 registry 冻结**；Rust skill_routes+skills/mod 读面已镜像，写面无。
- 写入工具先例：`write_truth_file`（assertSafeTruthFileName+确定性落盘）= R37 作用域写工具模板；agent 无通用 write_file（维持）。

## 三、两项施工图核心裁决

### R36（transcript 树化，只出设计稿）

- **v6 修正：「schema v2 草案」→ 零版本增量加法式设计**——BaseEvent 加 `parentSeq: optional`（缺省=线性链语义，旧文件零改写解析），判别联合加 `branch_moved{fromSeq,toSeq}`；版本字面量保持 1，v2 递进通道备而不用。对照 Pi 当年 v1→v2 破坏式重构：本仓 seq 已供全序，只补分支偏离位。
- head 持久性走事件流（branch_moved 落盘+重启 replay 重建）——区别于 Pi 内存 leaf（InkOS 会话为跨引擎重启的服务端长生命周期对象）。
- 与 R32 协同锁定：未来 compaction 事件携带 `firstKeptSeq`（=firstKeptEntryId 等价物），切点沿树路径解释→分支各自压缩状态互不污染；R32 落地按本稿字段位执行免二次迁移。
- 不做清单：UI 树浏览器/跨文件 parentSession 谱系/labels/context_edit/custom 系条目/设置类条目。

### R37（自扩展技能创作链）

- 三段基建全活（external-loader/registry/use_skill golden），唯一缺口=agent 生成 SKILL.md 的写入链与治理门。
- 四段链路：prompt 告知规范 → `author_skill{name,description,body}` 作用域工具（安全名 `^[a-z0-9][a-z0-9-]{0,63}$`+保留前缀+落 `{projectRoot}/skills/{name}/SKILL.md` 零改动即被拾取）→ 人审确认门 → **会话粒度热载（当前会话冻结=防同会话自我即时劫持；下会话自动生效；管理面 reload 端点按 449 号 mtime 先例、人侧触发）**。
- 治理：名称保留表/description 即信任边界（渐进披露位审查口）/RunLog `inkos.skill.authored` 预留/disable-model-invocation 复用上游同名字段。
- 双端：写面 TS 先行 Rust 备案（498→500 先例）；use_skill 既有 golden 不动；验收=author_skill→新会话装载→resolve→use_skill 全链 e2e+三拒绝态+门禁矩阵。

## 四、纪律与衔接

- 编号三查：0923 目录新建时为空、昨日最大 544、git 最新 545（ebb1a153）→ 546 无撞；**工作区有并行会话未提交变更（packages/cli+core 的 package.json 与多测试文件、engine-rs ops_routes.rs）——提交严格路径限定只带本轮两文档**。
- 实施顺序不变 R30→R31→R33→R32→R36/R37 随批；本轮产出即 v6 为 R36 预设的交付物「设计稿」本体。
- 0GC/安全/兼容性核对：两域均无 Rust 热路径改动；树化不扩攻击面；旧 transcripts 双端零改写兼容（zod optional / serde default 同构，518 号教训适用）。
