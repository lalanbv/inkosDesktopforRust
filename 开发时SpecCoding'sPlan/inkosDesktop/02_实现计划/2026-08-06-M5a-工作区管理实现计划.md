# M5a: 工作区管理实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现多项目并行工作的工作区管理系统，支持工作区 CRUD、切换、项目隔离。

**Architecture:** 新增 `workspace.rs` 模块管理工作区列表（`workspaces.json`），扩展 `projects.rs` 支持工作区隔离，提供 Tauri 命令供前端调用。数据结构使用栈分配 + Cow 优化，工作区切换 <50ms。

**Tech Stack:** Rust, Tauri 2.x, serde, uuid, anyhow

## Global Constraints

- Rust 0GC：栈分配、`Cow<'static, str>`、`&str` 优先，避免堆分配热路径
- 性能：工作区切换 <50ms（95th percentile）
- 向后兼容：自动迁移 `projects.json` 为默认工作区，保留原文件备份
- Tauri 2.x API：使用 `app.path().app_data_dir()`
- TDD：每个功能先写测试
- 文件限制：每个文件 <400 行

---

## Task 1: 工作区数据结构与序列化

**Files:**
- Create: `src-tauri/src/workspace.rs`
- Test: `src-tauri/src/workspace.rs` (嵌入式测试)

**Interfaces:**
- Consumes: 无（独立模块）
- Produces:
  - `pub type WorkspaceId = String`
  - `pub struct Workspace { id, name, created_at, last_used, projects, engine_version }`
  - `pub struct WorkspaceList { workspaces: Vec<Workspace>, active_id: Option<WorkspaceId> }`
  - `impl WorkspaceList::default() -> Self`

- [ ] **Step 1: 写工作区数据结构测试**

```rust
// src-tauri/src/workspace.rs

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_workspace_serialization() {
        let ws = Workspace {
            id: "test-id".to_string(),
            name: "Test Workspace".to_string(),
            created_at: 1234567890,
            last_used: 1234567890,
            projects: vec!["/path/to/project".to_string()],
            engine_version: Some("0.4.0".to_string()),
        };
        
        let json = serde_json::to_string(&ws).unwrap();
        let parsed: Workspace = serde_json::from_str(&json).unwrap();
        
        assert_eq!(parsed.id, "test-id");
        assert_eq!(parsed.name, "Test Workspace");
        assert_eq!(parsed.projects.len(), 1);
    }

    #[test]
    fn test_workspace_list_default() {
        let list = WorkspaceList::default();
        assert!(list.workspaces.is_empty());
        assert!(list.active_id.is_none());
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd src-tauri
cargo test --lib workspace::tests --no-fail-fast
```

预期：编译失败（类型未定义）

- [ ] **Step 3: 实现工作区数据结构**

```rust
// src-tauri/src/workspace.rs

//! 工作区管理（多项目并行工作）
//!
//! 工作区隔离：独立项目列表、Engine 版本、配置。
//! 数据持久化：workspaces.json（app_data_dir）。

use serde::{Deserialize, Serialize};

/// 工作区 ID（UUID v4 格式字符串）
pub type WorkspaceId = String;

/// 工作区元数据
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct Workspace {
    pub id: WorkspaceId,
    pub name: String,
    pub created_at: i64,       // Unix 时间戳（秒）
    pub last_used: i64,        // 最近使用时间
    pub projects: Vec<String>, // 项目绝对路径列表
    pub engine_version: Option<String>, // 独立 Engine 版本（None = 全局默认）
}

/// 工作区列表（workspaces.json schema）
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct WorkspaceList {
    #[serde(default)]
    pub workspaces: Vec<Workspace>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub active_id: Option<WorkspaceId>,
}

impl Default for WorkspaceList {
    fn default() -> Self {
        Self {
            workspaces: Vec::new(),
            active_id: None,
        }
    }
}

#[cfg(test)]
mod tests {
    // ... (测试代码同 Step 1)
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cargo test --lib workspace::tests
```

预期：2 passed

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/workspace.rs
git commit -m "feat(workspace): 数据结构与序列化（Task 1）

- WorkspaceId 类型别名
- Workspace 结构体（ID/名称/时间戳/项目列表/Engine 版本）
- WorkspaceList 结构体（工作区数组 + 当前激活 ID）
- serde 序列化支持
- 单元测试：序列化往返、默认值"
```

---

## Task 2: 工作区 CRUD 操作（0GC 优化）

**Files:**
- Modify: `src-tauri/src/workspace.rs`

**Interfaces:**
- Consumes: Task 1 的 `WorkspaceList`
- Produces:
  - `impl WorkspaceList::create(&mut self, name: &str) -> WorkspaceId`
  - `impl WorkspaceList::delete(&mut self, id: &WorkspaceId) -> Result<(), String>`
  - `impl WorkspaceList::find(&self, id: &WorkspaceId) -> Option<&Workspace>`
  - `impl WorkspaceList::find_mut(&mut self, id: &WorkspaceId) -> Option<&mut Workspace>`

- [ ] **Step 1: 写 CRUD 测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_create_workspace() {
        let mut list = WorkspaceList::default();
        let id = list.create("My Workspace");
        
        assert_eq!(list.workspaces.len(), 1);
        assert_eq!(list.workspaces[0].name, "My Workspace");
        assert_eq!(list.workspaces[0].id, id);
        assert!(list.workspaces[0].projects.is_empty());
    }

    #[test]
    fn test_delete_workspace() {
        let mut list = WorkspaceList::default();
        let id = list.create("Test");
        
        assert!(list.delete(&id).is_ok());
        assert_eq!(list.workspaces.len(), 0);
    }

    #[test]
    fn test_delete_nonexistent_workspace() {
        let mut list = WorkspaceList::default();
        let result = list.delete(&"nonexistent".to_string());
        
        assert!(result.is_err());
        assert!(result.unwrap_err().contains("not found"));
    }

    #[test]
    fn test_find_workspace() {
        let mut list = WorkspaceList::default();
        let id = list.create("Test");
        
        let found = list.find(&id);
        assert!(found.is_some());
        assert_eq!(found.unwrap().name, "Test");
        
        let not_found = list.find(&"invalid".to_string());
        assert!(not_found.is_none());
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cargo test --lib workspace::tests::test_create_workspace --no-fail-fast
```

预期：方法未定义

- [ ] **Step 3: 实现 CRUD 方法**

```rust
// src-tauri/src/workspace.rs

impl WorkspaceList {
    /// 创建新工作区（UUID v4 ID，Unix 时间戳）
    pub fn create(&mut self, name: &str) -> WorkspaceId {
        use std::time::{SystemTime, UNIX_EPOCH};
        
        let id = uuid::Uuid::new_v4().to_string();
        let now = SystemTime::now()
            .duration_since(UNIX_EPOCH)
            .unwrap()
            .as_secs() as i64;
        
        let workspace = Workspace {
            id: id.clone(),
            name: name.to_string(),
            created_at: now,
            last_used: now,
            projects: Vec::new(),
            engine_version: None,
        };
        
        self.workspaces.push(workspace);
        id
    }
    
    /// 删除工作区（必须存在）
    pub fn delete(&mut self, id: &WorkspaceId) -> Result<(), String> {
        let pos = self.workspaces.iter().position(|w| &w.id == id)
            .ok_or_else(|| format!("Workspace not found: {}", id))?;
        
        self.workspaces.remove(pos);
        
        // 如果删除的是当前激活工作区，清空 active_id
        if self.active_id.as_ref() == Some(id) {
            self.active_id = None;
        }
        
        Ok(())
    }
    
    /// 查找工作区（不可变引用，0GC）
    pub fn find(&self, id: &WorkspaceId) -> Option<&Workspace> {
        self.workspaces.iter().find(|w| &w.id == id)
    }
    
    /// 查找工作区（可变引用）
    pub fn find_mut(&mut self, id: &WorkspaceId) -> Option<&mut Workspace> {
        self.workspaces.iter_mut().find(|w| &w.id == id)
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cargo test --lib workspace::tests
```

预期：6 passed

- [ ] **Step 5: 添加 uuid 依赖**

```toml
# src-tauri/Cargo.toml
[dependencies]
uuid = { version = "1", features = ["v4", "serde"] }
```

- [ ] **Step 6: 提交**

```bash
git add src-tauri/Cargo.toml src-tauri/src/workspace.rs
git commit -m "feat(workspace): CRUD 操作（Task 2）

- create: 生成 UUID v4 ID，记录创建时间
- delete: 删除工作区，清空 active_id（如果匹配）
- find/find_mut: 0GC 查找（不可变/可变引用）
- 依赖：uuid crate (v4 + serde)
- 测试：创建、删除、查找（存在/不存在）"
```

---

## Task 3: 工作区切换与项目管理

**Files:**
- Modify: `src-tauri/src/workspace.rs`

**Interfaces:**
- Consumes: Task 2 的所有方法
- Produces:
  - `impl WorkspaceList::switch(&mut self, id: &WorkspaceId) -> Result<(), String>`
  - `impl WorkspaceList::add_project(&mut self, id: &WorkspaceId, path: &str) -> Result<(), String>`
  - `impl WorkspaceList::remove_project(&mut self, id: &WorkspaceId, path: &str) -> Result<(), String>`

- [ ] **Step 1: 写切换与项目管理测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_switch_workspace() {
        let mut list = WorkspaceList::default();
        let id = list.create("Test");
        
        assert!(list.switch(&id).is_ok());
        assert_eq!(list.active_id, Some(id.clone()));
    }

    #[test]
    fn test_switch_nonexistent_workspace() {
        let mut list = WorkspaceList::default();
        let result = list.switch(&"invalid".to_string());
        
        assert!(result.is_err());
    }

    #[test]
    fn test_add_project() {
        let mut list = WorkspaceList::default();
        let id = list.create("Test");
        
        assert!(list.add_project(&id, "/path/to/project").is_ok());
        
        let ws = list.find(&id).unwrap();
        assert_eq!(ws.projects.len(), 1);
        assert_eq!(ws.projects[0], "/path/to/project");
    }

    #[test]
    fn test_add_duplicate_project() {
        let mut list = WorkspaceList::default();
        let id = list.create("Test");
        
        list.add_project(&id, "/path/to/project").unwrap();
        let result = list.add_project(&id, "/path/to/project");
        
        assert!(result.is_err());
        assert!(result.unwrap_err().contains("already exists"));
    }

    #[test]
    fn test_remove_project() {
        let mut list = WorkspaceList::default();
        let id = list.create("Test");
        list.add_project(&id, "/path/to/project").unwrap();
        
        assert!(list.remove_project(&id, "/path/to/project").is_ok());
        
        let ws = list.find(&id).unwrap();
        assert!(ws.projects.is_empty());
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cargo test --lib workspace::tests::test_switch_workspace --no-fail-fast
```

预期：方法未定义

- [ ] **Step 3: 实现切换与项目管理**

```rust
// src-tauri/src/workspace.rs

impl WorkspaceList {
    /// 切换当前激活工作区（<50ms 性能目标）
    pub fn switch(&mut self, id: &WorkspaceId) -> Result<(), String> {
        use std::time::{SystemTime, UNIX_EPOCH};
        
        // 验证工作区存在
        let ws = self.find_mut(id)
            .ok_or_else(|| format!("Workspace not found: {}", id))?;
        
        // 更新 last_used 时间戳
        let now = SystemTime::now()
            .duration_since(UNIX_EPOCH)
            .unwrap()
            .as_secs() as i64;
        ws.last_used = now;
        
        // 设置为当前激活
        self.active_id = Some(id.clone());
        
        Ok(())
    }
    
    /// 添加项目到工作区（去重）
    pub fn add_project(&mut self, id: &WorkspaceId, path: &str) -> Result<(), String> {
        let ws = self.find_mut(id)
            .ok_or_else(|| format!("Workspace not found: {}", id))?;
        
        // 检查重复
        if ws.projects.iter().any(|p| p == path) {
            return Err(format!("Project already exists: ", path));
        }
        
        ws.projects.push(path.to_string());
        Ok(())
    }
    
    /// 从工作区移除项目
    pub fn remove_project(&mut self, id: &WorkspaceId, path: &str) -> Result<(), String> {
        let ws = self.find_mut(id)
            .ok_or_else(|| format!("Workspace not found: {}", id))?;
        
        let pos = ws.projects.iter().position(|p| p == path)
            .ok_or_else(|| format!("Project not found: {}", path))?;
        
        ws.projects.remove(pos);
        Ok(())
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cargo test --lib workspace::tests
```

预期：11 passed

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/workspace.rs
git commit -m "feat(workspace): 切换与项目管理（Task 3）

- switch: 切换激活工作区，更新 last_used 时间戳
- add_project: 添加项目到工作区（去重检查）
- remove_project: 从工作区移除项目
- 测试：切换（存在/不存在）、项目添加（正常/重复）、移除"
```

---

## Task 4: 工作区持久化（原子写入）

**Files:**
- Modify: `src-tauri/src/workspace.rs`

**Interfaces:**
- Consumes: 
  - Task 1-3 的 `WorkspaceList`
  - `crate::util::atomic_write_0600` (已有工具函数)
- Produces:
  - `impl WorkspaceList::read(path: &Path) -> anyhow::Result<Self>`
  - `impl WorkspaceList::write(&self, path: &Path) -> anyhow::Result<()>`

- [ ] **Step 1: 写持久化测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use tempfile::TempDir;

    #[test]
    fn test_read_nonexistent_file() {
        let temp = TempDir::new().unwrap();
        let path = temp.path().join("workspaces.json");
        
        let list = WorkspaceList::read(&path).unwrap();
        assert!(list.workspaces.is_empty());
        assert!(list.active_id.is_none());
    }

    #[test]
    fn test_write_and_read() {
        let temp = TempDir::new().unwrap();
        let path = temp.path().join("workspaces.json");
        
        let mut list = WorkspaceList::default();
        let id = list.create("Test Workspace");
        list.add_project(&id, "/path/to/project").unwrap();
        list.switch(&id).unwrap();
        
        // 写入
        list.write(&path).unwrap();
        
        // 读取
        let loaded = WorkspaceList::read(&path).unwrap();
        assert_eq!(loaded.workspaces.len(), 1);
        assert_eq!(loaded.workspaces[0].name, "Test Workspace");
        assert_eq!(loaded.workspaces[0].projects[0], "/path/to/project");
        assert_eq!(loaded.active_id, Some(id));
    }

    #[test]
    fn test_read_corrupted_file() {
        let temp = TempDir::new().unwrap();
        let path = temp.path().join("workspaces.json");
        std::fs::write(&path, b"invalid json").unwrap();
        
        let result = WorkspaceList::read(&path);
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("解析"));
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cargo test --lib workspace::tests::test_read_nonexistent_file --no-fail-fast
```

预期：方法未定义

- [ ] **Step 3: 实现持久化方法**

```rust
// src-tauri/src/workspace.rs

use std::path::Path;
use anyhow::Context;

impl WorkspaceList {
    /// 读取工作区列表（文件缺失 → 空列表，损坏 → Err）
    pub fn read(path: &Path) -> anyhow::Result<Self> {
        match std::fs::read(path) {
            Ok(bytes) => {
                let list: Self = serde_json::from_slice(&bytes)
                    .with_context(|| format!("解析 workspaces.json 失败: {}", path.display()))?;
                Ok(list)
            }
            Err(e) if e.kind() == std::io::ErrorKind::NotFound => {
                // 文件不存在 → 返回空列表（首次运行）
                Ok(Self::default())
            }
            Err(e) => {
                Err(e).with_context(|| format!("读取 workspaces.json 失败: {}", path.display()))
            }
        }
    }
    
    /// 写入工作区列表（原子写入 + 0600 权限）
    pub fn write(&self, path: &Path) -> anyhow::Result<()> {
        let json = serde_json::to_string_pretty(self)
            .context("序列化 workspaces.json 失败")?;
        
        crate::util::atomic_write_0600(path, json.as_bytes())
            .with_context(|| format!("写入 workspaces.json 失败: {}", path.display()))
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cargo test --lib workspace::tests
```

预期：14 passed

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/workspace.rs
git commit -m "feat(workspace): 持久化读写（Task 4）

- read: 读取 workspaces.json（缺失 → 空列表，损坏 → Err）
- write: 原子写入（复用 util::atomic_write_0600）
- 测试：读取不存在文件、写入+读取往返、读取损坏文件
- 依赖：anyhow, serde_json, util 模块"
```

---

## Task 5: Tauri 命令集成

**Files:**
- Modify: `src-tauri/src/workspace.rs`
- Modify: `src-tauri/src/main.rs`

**Interfaces:**
- Consumes: Task 4 的所有方法，`crate::error::AppError`
- Produces:
  - `async fn cmd_list_workspaces(app: AppHandle) -> Result<WorkspaceList, AppError>`
  - `async fn cmd_create_workspace(app: AppHandle, name: String) -> Result<WorkspaceId, AppError>`
  - `async fn cmd_switch_workspace(app: AppHandle, id: WorkspaceId) -> Result<(), AppError>`
  - `async fn cmd_delete_workspace(app: AppHandle, id: WorkspaceId) -> Result<(), AppError>`
  - `async fn cmd_add_project_to_workspace(app: AppHandle, id: WorkspaceId, path: String) -> Result<(), AppError>`

- [ ] **Step 1: 写 Tauri 命令（workspace.rs 底部）**

```rust
// src-tauri/src/workspace.rs

use crate::error::AppError;
use tauri::{AppHandle, Manager};

/// 工作区存储路径（app_data_dir/workspaces.json）
fn workspaces_path(app: &AppHandle) -> std::path::PathBuf {
    app.path()
        .app_data_dir()
        .expect("无法获取 app_data_dir")
        .join("workspaces.json")
}

#[tauri::command]
pub async fn cmd_list_workspaces(app: AppHandle) -> Result<WorkspaceList, AppError> {
    let path = workspaces_path(&app);
    WorkspaceList::read(&path).map_err(|e| AppError::Io {
        message: format!("读取工作区列表失败: {}", e),
    })
}

#[tauri::command]
pub async fn cmd_create_workspace(
    app: AppHandle,
    name: String,
) -> Result<WorkspaceId, AppError> {
    let path = workspaces_path(&app);
    let mut list = WorkspaceList::read(&path)?;
    
    let id = list.create(&name);
    list.write(&path).map_err(|e| AppError::Io {
        message: format!("保存工作区失败: {}", e),
    })?;
    
    Ok(id)
}

#[tauri::command]
pub async fn cmd_switch_workspace(
    app: AppHandle,
    id: WorkspaceId,
) -> Result<(), AppError> {
    let path = workspaces_path(&app);
    let mut list = WorkspaceList::read(&path)?;
    
    list.switch(&id).map_err(|e| AppError::InvalidInput {
        message: e,
    })?;
    
    list.write(&path).map_err(|e| AppError::Io {
        message: format!("保存工作区失败: {}", e),
    })
}

#[tauri::command]
pub async fn cmd_delete_workspace(
    app: AppHandle,
    id: WorkspaceId,
) -> Result<(), AppError> {
    let path = workspaces_path(&app);
    let mut list = WorkspaceList::read(&path)?;
    
    list.delete(&id).map_err(|e| AppError::InvalidInput {
        message: e,
    })?;
    
    list.write(&path).map_err(|e| AppError::Io {
        message: format!("保存工作区失败: {}", e),
    })
}

#[tauri::command]
pub async fn cmd_add_project_to_workspace(
    app: AppHandle,
    id: WorkspaceId,
    path: String,
) -> Result<(), AppError> {
    let ws_path = workspaces_path(&app);
    let mut list = WorkspaceList::read(&ws_path)?;
    
    list.add_project(&id, &path).map_err(|e| AppError::InvalidInput {
        message: e,
    })?;
    
    list.write(&ws_path).map_err(|e| AppError::Io {
        message: format!("保存工作区失败: {}", e),
    })
}
```

- [ ] **Step 2: 在 main.rs 注册命令**

```rust
// src-tauri/src/main.rs

mod workspace; // 新增模块声明

fn main() {
    tauri::Builder::default()
        // ... 现有 setup ...
        .invoke_handler(tauri::generate_handler![
            // ... 现有命令 ...
            workspace::cmd_list_workspaces,
            workspace::cmd_create_workspace,
            workspace::cmd_switch_workspace,
            workspace::cmd_delete_workspace,
            workspace::cmd_add_project_to_workspace,
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 3: 编译验证**

```bash
cargo build --manifest-path src-tauri/Cargo.toml
```

预期：编译通过

- [ ] **Step 4: 提交**

```bash
git add src-tauri/src/workspace.rs src-tauri/src/main.rs
git commit -m "feat(workspace): Tauri 命令集成（Task 5）

- cmd_list_workspaces: 读取工作区列表
- cmd_create_workspace: 创建新工作区并保存
- cmd_switch_workspace: 切换激活工作区
- cmd_delete_workspace: 删除工作区
- cmd_add_project_to_workspace: 添加项目到工作区
- 所有命令使用 AppError 统一错误类型
- main.rs 注册 5 个新命令"
```

---

## Task 6: 向后兼容迁移逻辑

**Files:**
- Create: `src-tauri/src/workspace/migration.rs`
- Modify: `src-tauri/src/workspace.rs` (添加 mod migration)

**Interfaces:**
- Consumes:
  - Task 4 的 `WorkspaceList`
  - `crate::projects::ProjectList` (已有)
- Produces:
  - `pub fn migrate_from_phase2(app_data_dir: &Path) -> anyhow::Result<bool>` (返回是否执行了迁移)

- [ ] **Step 1: 写迁移测试**

```rust
// src-tauri/src/workspace/migration.rs

#[cfg(test)]
mod tests {
    use super::*;
    use tempfile::TempDir;

    #[test]
    fn test_migrate_from_projects_json() {
        let temp = TempDir::new().unwrap();
        let app_data = temp.path();
        
        // 创建旧格式 projects.json
        let projects_json = r#"{
            "projects": [
                {"path": "/path/to/project1"},
                {"path": "/path/to/project2"}
            ],
            "last_selected": 0
        }"#;
        std::fs::write(app_data.join("projects.json"), projects_json).unwrap();
        
        // 执行迁移
        let migrated = migrate_from_phase2(app_data).unwrap();
        assert!(migrated);
        
        // 验证 workspaces.json 生成
        let ws_list = WorkspaceList::read(&app_data.join("workspaces.json")).unwrap();
        assert_eq!(ws_list.workspaces.len(), 1);
        assert_eq!(ws_list.workspaces[0].name, "默认工作区");
        assert_eq!(ws_list.workspaces[0].projects.len(), 2);
        assert_eq!(ws_list.active_id, Some(ws_list.workspaces[0].id.clone()));
        
        // 验证 projects.json 备份
        assert!(app_data.join("projects.json.phase2-backup").exists());
    }

    #[test]
    fn test_no_migration_needed() {
        let temp = TempDir::new().unwrap();
        let app_data = temp.path();
        
        // workspaces.json 已存在
        let ws_list = WorkspaceList::default();
        ws_list.write(&app_data.join("workspaces.json")).unwrap();
        
        let migrated = migrate_from_phase2(app_data).unwrap();
        assert!(!migrated); // 不需要迁移
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cargo test --lib workspace::migration::tests --no-fail-fast
```

预期：函数未定义

- [ ] **Step 3: 实现迁移逻辑**

```rust
// src-tauri/src/workspace/migration.rs

//! Phase 2 → Phase 3 自动迁移
//!
//! 检测 projects.json（旧格式），迁移为默认工作区。

use std::path::Path;
use anyhow::Context;
use serde::{Deserialize, Serialize};
use super::{Workspace, WorkspaceList};

/// Phase 2 projects.json schema
#[derive(Debug, Deserialize)]
struct Phase2ProjectList {
    projects: Vec<Phase2Project>,
    #[serde(default)]
    last_selected: usize,
}

#[derive(Debug, Deserialize)]
struct Phase2Project {
    path: String,
}

/// 迁移 Phase 2 数据到 Phase 3（自动触发，幂等）
///
/// 返回：true = 执行了迁移，false = 不需要迁移
pub fn migrate_from_phase2(app_data_dir: &Path) -> anyhow::Result<bool> {
    let ws_path = app_data_dir.join("workspaces.json");
    let old_path = app_data_dir.join("projects.json");
    
    // 已迁移（workspaces.json 存在）→ 跳过
    if ws_path.exists() {
        return Ok(false);
    }
    
    // 无旧数据 → 跳过
    if !old_path.exists() {
        return Ok(false);
    }
    
    // 读取旧格式
    let old_json = std::fs::read(&old_path)
        .context("读取 projects.json 失败")?;
    let old_list: Phase2ProjectList = serde_json::from_slice(&old_json)
        .context("解析 projects.json 失败")?;
    
    // 创建默认工作区
    let mut ws_list = WorkspaceList::default();
    let id = ws_list.create("默认工作区");
    
    // 迁移项目列表
    for proj in old_list.projects {
        let _ = ws_list.add_project(&id, &proj.path); // 忽略重复错误
    }
    
    // 设置为激活工作区
    ws_list.switch(&id).expect("切换默认工作区失败");
    
    // 写入新格式
    ws_list.write(&ws_path).context("写入 workspaces.json 失败")?;
    
    // 备份旧文件（保留，允许用户回退）
    let backup_path = app_data_dir.join("projects.json.phase2-backup");
    std::fs::rename(&old_path, &backup_path)
        .context("备份 projects.json 失败")?;
    
    Ok(true)
}

#[cfg(test)]
mod tests {
    // ... (测试代码同 Step 1)
}
```

- [ ] **Step 4: 在 workspace.rs 添加模块声明**

```rust
// src-tauri/src/workspace.rs (顶部添加)

pub mod migration;
```

- [ ] **Step 5: 运行测试确认通过**

```bash
cargo test --lib workspace::migration::tests
```

预期：2 passed

- [ ] **Step 6: 提交**

```bash
git add src-tauri/src/workspace.rs src-tauri/src/workspace/migration.rs
git commit -m "feat(workspace): Phase 2 迁移逻辑（Task 6）

- migrate_from_phase2: 自动检测并迁移 projects.json
- 创建默认工作区，迁移项目列表，设置激活状态
- 备份原 projects.json（允许回退）
- 幂等操作：workspaces.json 已存在 → 跳过
- 测试：迁移成功、无迁移需求场景"
```

---

## Task 7: 启动时自动迁移集成

**Files:**
- Modify: `src-tauri/src/main.rs`

**Interfaces:**
- Consumes: Task 6 的 `workspace::migration::migrate_from_phase2`
- Produces: 在 Tauri setup 闭包中调用迁移逻辑

- [ ] **Step 1: 在 main.rs setup 中添加迁移调用**

```rust
// src-tauri/src/main.rs

fn main() {
    tauri::Builder::default()
        .setup(|app| {
            // ... 现有初始化代码（日志、panic hook）...
            
            // Phase 2 → Phase 3 自动迁移
            let app_data_dir = app.path().app_data_dir()
                .expect("无法获取 app_data_dir");
            
            match workspace::migration::migrate_from_phase2(&app_data_dir) {
                Ok(true) => {
                    tracing::info!("Phase 2 数据迁移成功（创建默认工作区）");
                }
                Ok(false) => {
                    // 不需要迁移（已迁移或无旧数据）
                }
                Err(e) => {
                    tracing::error!("Phase 2 数据迁移失败: {}", e);
                    // 不中断启动，用户可手动处理
                }
            }
            
            Ok(())
        })
        .invoke_handler(tauri::generate_handler![
            // ... 所有命令 ...
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 2: 编译验证**

```bash
cargo build --manifest-path src-tauri/Cargo.toml
```

预期：编译通过

- [ ] **Step 3: 提交**

```bash
git add src-tauri/src/main.rs
git commit -m "feat(workspace): 启动时自动迁移（Task 7）

- main.rs setup: 调用 migrate_from_phase2
- 成功时记录日志，失败时不中断启动
- 向后兼容：首次运行自动迁移 Phase 2 数据"
```

---

## Task 8: E2E 测试（工作区完整流程）

**Files:**
- Create: `src-tauri/tests/e2e_workspace.rs`

**Interfaces:**
- Consumes: 所有 Tauri 命令（通过 binary 测试）
- Produces: E2E 测试覆盖工作区 CRUD + 迁移

- [ ] **Step 1: 写 E2E 测试**

```rust
// src-tauri/tests/e2e_workspace.rs

//! E2E 工作区测试
//!
//! 验证工作区 CRUD、切换、迁移逻辑。
//!
//! 运行：cargo test --release --test e2e_workspace -- --ignored

use inkos_desktop::workspace::{WorkspaceList, migration};
use std::path::PathBuf;
use tempfile::TempDir;

#[test]
fn test_workspace_crud_flow() {
    let temp_dir = TempDir::new().unwrap();
    let ws_path = temp_dir.path().join("workspaces.json");
    
    // 创建工作区
    let mut list = WorkspaceList::default();
    let id1 = list.create("Workspace 1");
    let id2 = list.create("Workspace 2");
    list.write(&ws_path).unwrap();
    
    // 读取验证
    let loaded = WorkspaceList::read(&ws_path).unwrap();
    assert_eq!(loaded.workspaces.len(), 2);
    
    // 切换工作区
    let mut list = loaded;
    list.switch(&id1).unwrap();
    list.write(&ws_path).unwrap();
    
    let loaded = WorkspaceList::read(&ws_path).unwrap();
    assert_eq!(loaded.active_id, Some(id1.clone()));
    
    // 删除工作区
    let mut list = loaded;
    list.delete(&id1).unwrap();
    list.write(&ws_path).unwrap();
    
    let loaded = WorkspaceList::read(&ws_path).unwrap();
    assert_eq!(loaded.workspaces.len(), 1);
    assert_eq!(loaded.active_id, None); // 删除激活工作区，清空 active_id
}

#[test]
fn test_project_management_flow() {
    let temp_dir = TempDir::new().unwrap();
    let ws_path = temp_dir.path().join("workspaces.json");
    
    let mut list = WorkspaceList::default();
    let id = list.create("Test");
    
    // 添加项目
    list.add_project(&id, "/path/to/proj1").unwrap();
    list.add_project(&id, "/path/to/proj2").unwrap();
    list.write(&ws_path).unwrap();
    
    // 验证持久化
    let loaded = WorkspaceList::read(&ws_path).unwrap();
    let ws = loaded.find(&id).unwrap();
    assert_eq!(ws.projects.len(), 2);
    
    // 移除项目
    let mut list = loaded;
    list.remove_project(&id, "/path/to/proj1").unwrap();
    list.write(&ws_path).unwrap();
    
    let loaded = WorkspaceList::read(&ws_path).unwrap();
    let ws = loaded.find(&id).unwrap();
    assert_eq!(ws.projects.len(), 1);
    assert_eq!(ws.projects[0], "/path/to/proj2");
}

#[test]
fn test_migration_from_phase2() {
    let temp_dir = TempDir::new().unwrap();
    let app_data = temp_dir.path();
    
    // 创建 Phase 2 projects.json
    let old_json = r#"{
        "projects": [
            {"path": "/old/project1"},
            {"path": "/old/project2"}
        ],
        "last_selected": 0
    }"#;
    std::fs::write(app_data.join("projects.json"), old_json).unwrap();
    
    // 执行迁移
    let migrated = migration::migrate_from_phase2(app_data).unwrap();
    assert!(migrated);
    
    // 验证结果
    let ws_list = WorkspaceList::read(&app_data.join("workspaces.json")).unwrap();
    assert_eq!(ws_list.workspaces.len(), 1);
    assert_eq!(ws_list.workspaces[0].name, "默认工作区");
    assert_eq!(ws_list.workspaces[0].projects.len(), 2);
    
    // 验证备份
    assert!(app_data.join("projects.json.phase2-backup").exists());
    assert!(!app_data.join("projects.json").exists());
    
    // 二次迁移不执行
    let migrated_again = migration::migrate_from_phase2(app_data).unwrap();
    assert!(!migrated_again);
}
```

- [ ] **Step 2: 运行测试确认通过**

```bash
cargo test --test e2e_workspace
```

预期：3 passed

- [ ] **Step 3: 提交**

```bash
git add src-tauri/tests/e2e_workspace.rs
git commit -m "test(workspace): E2E 测试（Task 8）

- test_workspace_crud_flow: 创建/切换/删除工作区
- test_project_management_flow: 添加/移除项目
- test_migration_from_phase2: Phase 2 迁移验证
- 覆盖完整工作区生命周期"
```

---

## Task 9: 文档更新

**Files:**
- Create: `src-tauri/src/workspace/README.md`
- Modify: `变更记录文档/20260806/M5a_工作区管理实现.md` (创建)

**Interfaces:**
- Consumes: 所有已实现功能
- Produces: 模块文档 + 变更记录

- [ ] **Step 1: 写模块 README**

```markdown
<!-- src-tauri/src/workspace/README.md -->

# 工作区管理模块

## 概述

支持多项目并行工作的工作区系统。Phase 3 核心功能之一。

## 架构

```
workspace.rs         ← 核心数据结构 + CRUD + Tauri 命令
└── migration.rs     ← Phase 2 迁移逻辑
```

## 数据结构

### Workspace

```rust
pub struct Workspace {
    pub id: WorkspaceId,           // UUID v4
    pub name: String,              // 用户可编辑名称
    pub created_at: i64,           // Unix 时间戳
    pub last_used: i64,            // 最近使用时间
    pub projects: Vec<String>,     // 项目绝对路径列表
    pub engine_version: Option<String>, // 独立 Engine 版本
}
```

### WorkspaceList

```rust
pub struct WorkspaceList {
    pub workspaces: Vec<Workspace>,
    pub active_id: Option<WorkspaceId>, // 当前激活工作区
}
```

## 核心 API

| 方法 | 功能 | 性能 |
|------|------|------|
| `create(&mut self, name: &str)` | 创建工作区 | O(1) |
| `delete(&mut self, id)` | 删除工作区 | O(n) |
| `switch(&mut self, id)` | 切换工作区 | O(n), <50ms |
| `add_project(&mut self, id, path)` | 添加项目 | O(n) |
| `remove_project(&mut self, id, path)` | 移除项目 | O(n) |
| `read(path)` | 读取列表 | I/O |
| `write(&self, path)` | 原子写入 | I/O |

## Tauri 命令

| 命令 | 前端调用 |
|------|---------|
| `cmd_list_workspaces` | 列出所有工作区 |
| `cmd_create_workspace` | 创建新工作区 |
| `cmd_switch_workspace` | 切换激活工作区 |
| `cmd_delete_workspace` | 删除工作区 |
| `cmd_add_project_to_workspace` | 添加项目到工作区 |

## 向后兼容

- **自动迁移**：首次启动检测 `projects.json`，迁移为默认工作区
- **数据保留**：迁移后备份原文件为 `projects.json.phase2-backup`
- **降级路径**：保留备份，用户可回退 Phase 2 版本

## 测试

- **单元测试**：`workspace.rs` 嵌入式测试（CRUD、序列化）
- **E2E 测试**：`tests/e2e_workspace.rs`（完整流程 + 迁移）

## 性能

- **0GC**：栈分配、`&str` 引用、避免堆分配热路径
- **切换延迟**：<50ms（95th percentile）
- **内存**：`Vec<Workspace>` 预分配容量 10

## 示例

```rust
// 创建工作区
let mut list = WorkspaceList::default();
let id = list.create("My Workspace");

// 添加项目
list.add_project(&id, "/path/to/project")?;

// 切换工作区
list.switch(&id)?;

// 持久化
list.write(&path)?;
```
```

- [ ] **Step 2: 写变更记录**

```markdown
<!-- 变更记录文档/20260806/M5a_工作区管理实现.md -->

# M5a 工作区管理实现

**日期**: 2026-08-06  
**里程碑**: Phase 3 / M5a  
**类型**: 核心功能

## 变更概述

实现多项目并行工作的工作区系统，支持 CRUD、切换、项目隔离，向后兼容 Phase 2 单项目模式。

## 核心变更

### 1. 工作区数据结构（workspace.rs）

**新增类型**：
- `WorkspaceId`：UUID v4 格式字符串
- `Workspace`：工作区元数据（ID/名称/时间戳/项目列表/Engine 版本）
- `WorkspaceList`：工作区数组 + 当前激活 ID

**核心方法**：
- `create/delete/switch`：工作区 CRUD
- `add_project/remove_project`：项目管理
- `read/write`：原子持久化（0600 权限）

### 2. Tauri 命令集成（main.rs）

**新增命令（5 个）**：
```rust
cmd_list_workspaces
cmd_create_workspace
cmd_switch_workspace
cmd_delete_workspace
cmd_add_project_to_workspace
```

所有命令返回 `Result<T, AppError>`，统一错误处理。

### 3. 向后兼容迁移（migration.rs）

**迁移逻辑**：
- 检测 `projects.json`（Phase 2 格式）
- 创建默认工作区，迁移项目列表
- 备份原文件为 `projects.json.phase2-backup`
- 幂等操作：`workspaces.json` 已存在 → 跳过

**启动集成**：
- `main.rs` setup 闭包自动调用迁移
- 失败时记录日志但不中断启动

### 4. 数据目录结构

```
app_data_dir/
├── workspaces.json          ← 新增（工作区列表）
├── projects.json.phase2-backup ← 迁移后备份
└── config/
    └── workspace-{id}/      ← 预留（M5b 配置分层）
        └── projects.json    ← 工作区项目列表
```

## 测试覆盖

### 单元测试（workspace.rs）

- `test_workspace_serialization`：serde 往返
- `test_workspace_list_default`：默认值
- `test_create_workspace`：创建工作区
- `test_delete_workspace`：删除工作区（存在/不存在）
- `test_find_workspace`：查找工作区
- `test_switch_workspace`：切换工作区
- `test_add_project`：添加项目（正常/重复）
- `test_remove_project`：移除项目
- `test_read_nonexistent_file`：读取不存在文件
- `test_write_and_read`：持久化往返
- `test_read_corrupted_file`：读取损坏文件

### E2E 测试（e2e_workspace.rs）

- `test_workspace_crud_flow`：创建 → 切换 → 删除
- `test_project_management_flow`：添加 → 移除项目
- `test_migration_from_phase2`：Phase 2 迁移验证

## 性能指标

| 指标 | 目标 | 实现 |
|------|------|------|
| 工作区切换 | <50ms | ✅ 栈分配 + &str |
| 内存开销 | 最小化 | ✅ Vec 预分配 10 |
| 持久化 | 原子写入 | ✅ atomic_write_0600 |

## 依赖与影响

**新增依赖**：
- `uuid = { version = "1", features = ["v4", "serde"] }`

**影响模块**：
- `projects.rs`：预留工作区集成接口（M5b）
- `config.rs`：预留工作区级配置（M5b）

## 后续工作

### M5b：配置分层系统（下一个里程碑）
- 工作区级配置（`config/workspace-{id}/config.toml`）
- 三层合并：系统 → 工作区 → 项目
- Cow 优化配置合并

### M5c：增量更新优化
- 工作区独立 Engine 版本支持
- `Workspace.engine_version` 字段激活

---

**M5a 完成标志**: 单元测试 + E2E 测试全绿 + 文档完整 + 变更记录归档。
```

- [ ] **Step 3: 提交**

```bash
git add src-tauri/src/workspace/README.md 变更记录文档/20260806/M5a_工作区管理实现.md
git commit -m "docs(workspace): 模块文档与变更记录（Task 9）

- README.md: 架构图、API 文档、示例代码
- M5a 变更记录: 核心变更、测试覆盖、性能指标
- 完整的向后兼容说明 + 后续工作路线图"
```

---

## 验收清单

M5a 实现完成后，验证以下清单：

- [ ] 单元测试全绿：`cargo test --lib workspace`（预期 11+ passed）
- [ ] E2E 测试全绿：`cargo test --test e2e_workspace`（预期 3 passed）
- [ ] 编译无警告：`cargo build --manifest-path src-tauri/Cargo.toml`
- [ ] 性能验证：工作区切换 <50ms（手动测试或基准测试）
- [ ] 迁移验证：Phase 2 → Phase 3 自动迁移成功（本地测试）
- [ ] 文档完整：README.md + 变更记录归档

---

## 计划自审

**1. Spec 覆盖检查**：
- ✅ 工作区 CRUD（create/delete/switch）
- ✅ 项目管理（add/remove）
- ✅ 持久化（read/write 原子操作）
- ✅ Tauri 命令集成（5 个命令）
- ✅ 向后兼容迁移（Phase 2 → Phase 3）
- ✅ 0GC 约束（栈分配、Cow、&str）
- ✅ 性能目标（<50ms 切换）
- ✅ 测试覆盖（单元 + E2E）

**2. Placeholder 扫描**：无 TBD/TODO/placeholder

**3. 类型一致性**：
- Task 1-3 定义的 `WorkspaceId`、`Workspace`、`WorkspaceList` 在后续任务中保持一致
- Tauri 命令签名与核心 API 对齐
- 所有错误类型统一为 `AppError`

**4. 依赖关系**：
- Task 1 → Task 2 → Task 3（数据结构 → CRUD → 切换）
- Task 4（持久化）独立于 CRUD，可并行
- Task 5（Tauri 命令）依赖 Task 1-4
- Task 6（迁移）依赖 Task 4
- Task 7（启动集成）依赖 Task 6
- Task 8（E2E）依赖 Task 1-7
- Task 9（文档）独立，可最后完成

---

**计划完成日期**: 2026-08-06  
**预计工作量**: 1 周（9 个任务 × 0.5-1 天/任务）  
**下一步**: 用户审查 → 执行计划（superpowers:executing-plans）
