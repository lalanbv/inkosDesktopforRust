# 首批落地实施计划（WBS，v1）

> 日期：2026-08-25　|　编号：170 号
> 上游：[重构优化总体方案_v1.md](./重构优化总体方案_v1.md) §3 工作流
> 本批次 = W-A1 + W-A2 + W-A3 + W-B1 + W-C1 + W-D1 + W-D2（文档三件套）

## 任务分解

### T1　engine-rs：回环守卫 middleware（W-A1）

| 步骤 | 内容 | 产出/验收 |
|------|------|-----------|
| T1.1 | 新建 `engine-rs/src/server/loopback_guard.rs`：`LoopbackGuard` middleware（axum `middleware::from_fn_with_state` 或 tower layer）+ `GuardConfig::from_env()`（读 `INKOS_ENGINE_LOOPBACK_GUARD`/`INKOS_ENGINE_ALLOWED_ORIGINS`）+ 回环主机判定纯函数 `is_loopback_host(&str)`（localhost/127.0.0.1/[::1]/[::]，剥端口） | 单测矩阵 11 用例（方案 §W-A1 表） |
| T1.2 | `inkos-engine-server` bin 装配：守卫层包在 CORS 之外（`.layer(guard).layer(cors)` 顺序验证：guard 先执行） | bin 内装配 + 测试经 router 层直接验证（见 T1.3） |
| T1.3 | `router_books` 不挂守卫（duel/单测零扰动）；守卫测试用独立 mini-router（守卫+CORS+一个 echo 路由）锁定行为，含「拦截响应无 CORS 头」断言 | tests 全绿 |
| T1.4 | 敏感端点复核：`GET /api/v1/services/:service/secret` 在守卫后已不可被远端 Origin 命中 | 集成式用例（mini-router 挂真实 handler 可选） |

### T2　engine-rs：静态面安全头（W-A3）

| 步骤 | 内容 | 验收 |
|------|------|------|
| T2.1 | `static_routes` 响应注入 `X-Content-Type-Options: nosniff` / `Referrer-Policy: no-referrer` / `X-Frame-Options: DENY`（仅静态面，API 面不动） | 测试断言三头 |

### T3　engine-rs：静态面 Bytes 化 + SSE 复核结论（W-B1 修正版）

| 步骤 | 内容 | 验收 |
|------|------|------|
| T3.1 | **实施期复核**：SsePayload Arc 化经代码级验证为零收益（axum `Event` 持有 `Cow<'static, str>`，yield 处仍须物化 String）——**取消该改动**，结论登记入方案 W-B1 | 方案文档更新 |
| T3.2 | 静态面 SPA 回退 `index.html` 缓存 `Vec<u8> → axum::body::Bytes`（消每请求整页 memcpy；读取转移所有权零拷贝） | 静态面既有测试全绿（语义不变） |

### T4　engine-rs：health 后端标识（W-C1）

| 步骤 | 内容 | 验收 |
|------|------|------|
| T4.1 | `/api/v1/health` 响应加 `"backend":"rust-engine"`（AppState 增字段，默认值保持 Node 兼容语义：字段为加法） | health 测试更新 |

### T5　studio(Node)：守卫同步（W-A2）

| 步骤 | 内容 | 验收 |
|------|------|------|
| T5.1 | `packages/studio/src/api/server.ts` 增 Hono middleware，同规则同 env 名 | vitest 4 用例（恶意 403/回环放行/缺头放行/off 放行） |
| T5.2 | 既有 server.test.ts 全量回归 | 全绿 |

### T6　验证与收尾（W-D1/D2）

| 步骤 | 内容 | 验收 |
|------|------|------|
| T6.1 | `engine-rs` cargo test 全量 | 全绿 |
| T6.2 | `src-tauri` cargo test | EXIT:0 |
| T6.3 | `packages/studio` vitest（api 面） | 全绿 |
| T6.4 | 变更记录 170 号归档 `变更记录文档/20260825/` | 文件落位 |
| T6.5 | 文档可靠性校验（对照代码逐条核验三件套中的可检验断言） | 校验记录写入变更记录 |

## 依赖与顺序

T1→T4 串行（同 crate，避免编译冲突）；T5 可与 T1 并行但为控制验证复杂度按序执行；T6 最后。

## 规模预估

engine-rs 新增 ~350 行（含测试）、改动 ~40 行；server.ts 新增 ~80 行（含导出与注释）；文档三件套 + 变更记录。
